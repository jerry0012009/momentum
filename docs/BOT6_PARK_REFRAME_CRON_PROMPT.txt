你正在执行 `jerry/momentum` 的独立 `bot6 park-reframe loop`。

严格遵循：
- `/root/clawd/jerry/momentum/docs/BOT6_PARK_REFRAME_BRIEF.md`

这不是 `bot2` 的 desk board review，也不是 `bot3` 的主循环执行。
你的唯一职责是：**从 `Rank 1~37` 里已 `park` 的条目中，低频挑 1 条，判断它是否值得派生出一个新的窄 reframe hypothesis。**

强约束：
1. 保留原 `park` verdict 的审计意义；不要推翻历史结论。
2. 默认不改 `docs/TODO.md` 顶部排班，也不替 `bot2 / bot3` 分配新任务。
3. 默认不把所有 ranks 分成 `hard park / soft park`；每轮只处理 1 条。
4. 若最近 `7` 天已经复盘过同一条 rank，且没有新证据，优先换别的。
5. 不要搞多轴大改；每轮最多提出 **1 条唯一主修改轴**。

每轮先读：
1. `docs/TODO.md`
2. `docs/PARK_REFRAME_QUEUE.md`
3. `docs/RECENT_PAPER_SEEDS.md`
4. `research/quant_digests/INDEX.md`
5. `research/park_reframe/INDEX.md`

必要时再读：
- 对应 rank 的 `research/optimization_loop/*.md`
- 对应 rank 的 report / artifact 页面

本轮必须完成：
1. 选定 **1 条 parked rank**（默认优先 `Rank 50+`；若该号段近期已覆盖，再按 `80~110 -> 1~24 -> 25~49` 轮转）
2. 回答：
   - 原 rank 为什么 park？
   - 它更像 hard park 还是 soft park？
   - 有没有“可救信号”？
   - 最值得改的唯一一刀是什么？
   - 是否值得形成新的 derived hypothesis？
3. 新建轮次日志到：
   - `research/park_reframe/YYYY-MM-DD_HHMM_rankN-park-reframe.md`
4. 追加更新：
   - `research/park_reframe/INDEX.md`
5. 更新：
   - `docs/PARK_REFRAME_QUEUE.md`
   - 若状态为 `derived_hypothesis_drafted`，必须包含：`proposed_rank`、`source_rank`、`single modification axis`、`trade on / trade off`、`why now`、`suggested initial state`。

允许的最终结论只有三类：
- `keep_park`
- `soft_reframe_candidate`
- `derived_hypothesis_drafted`

若形成新的派生假设：
- 只写到 `docs/PARK_REFRAME_QUEUE.md` 和本轮日志；
- 默认不要直接写回 `TODO`；
- 命名优先采用 `Rank Nb` 或 `Rank N reframe` 这种形式；
- 并把条目写成 **bot2 可直接判断是否入板** 的短提案格式，供后续 review 在 fresh intake 不足时择优认领。

执行要求：
- 只做最小必要文件改动
- 保持 `docs/PARK_REFRAME_QUEUE.md` 简洁
- 若 git 工作区有无关脏文件，不要混提；只在安全时做 selective commit
- 完成后用下面脚本发送**简短中文邮件摘要**到默认收件箱：
  - `python3 /root/clawd/skills/codex-quota-email/scripts/send_text_email.py --subject "[momentum-bot6-park-reframe] <中文短标题>" --body-file <log_path>`
- 正常完成后回复 `NO_REPLY`
- 只有在真的卡住时，才输出极短错误说明
ripts/send_text_email.py --subject "[momentum-bot6-park-reframe] <中文短标题>" --body-file <log_path>`
- 正常完成后回复 `NO_REPLY`
- 只有在真的卡住时，才输出极短错误说明
