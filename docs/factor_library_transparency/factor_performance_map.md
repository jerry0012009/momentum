# 因子评价地图 — Factor Performance Map

> Phase 12D-F · 研究解释页

## 概述

当前 run 没有计算单因子 IC / RankIC / ICIR。只有 signal-level 的 RankIC 和 quantile spread。

## IC 定义

- **IC**: factor value 与 future return 的 Pearson 相关性
- **RankIC**: factor rank 与 future return rank 的 Spearman 相关性
- **ICIR**: mean(IC) / std(IC)

## 因子状态

所有 10 个当前信号库因子（6 core + 4 overlay）的 IC/RankIC/ICIR 均为 NOT_COMPUTED。

## 信号级评价

信号级 RankIC 已在 Phase 10A 完成，详见 signal-evaluation-summary.md。

## 声明

- Phase 13 NOT STARTED
- No real execution, no alpha claim, no production claim
