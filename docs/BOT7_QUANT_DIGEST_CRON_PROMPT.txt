请为 `/root/clawd/jerry/momentum` 产出 1 篇新的短研究笔记。

目标：服务于 Crypto 短周期量化研发（默认 `5m / 15m`，也接受 `1m / 3m` 的更快高强度 alpha），默认仍优先从 **论文 / 仓库** 中找可快速复现的思路。

bot7 的默认目标不是围绕某个固定形态（如 breakout / retest）持续内循环，而是持续寻找并拆解可复现的 alpha，作为后续复现与实盘素材池。

但当前也允许一个更灵活的读法：
- 不要求必须复刻论文里“最核心 / 最出名”的主结论；
- 若论文 / 仓库里有某个 **更适合我们 desk 的旁支想法**（例如过滤层、regime gate、仓位管理、配对/统计套利、多空双开、risk overlay、确认/否决条件），也可以把它单独拎出来讨论；
- 只要它能在 `1m / 3m / 5m / 15m` 上较快做出最小实验、快速复现、快速验证，就值得进入研究池。

也就是说：默认仍优先 `paper / repo based`，但不要把自己锁死在“只能抄论文 headline alpha”。若当前主线材料大多只提供 filter / confirmation，而缺少可独立复现的 alpha，新 intake 应主动补 `mean reversion / cross-sectional / relative value / stat-arb / pairs` 方向的论文与仓库。

本轮选题默认优先级：**可独立复现且可直接落地为完整策略（entry/exit/sizing/risk/cost）的 raw alpha 候选 > 可独立复现的 raw alpha 候选 > 可清楚拆成 raw alpha + regime/filter 的候选 > 可同时服务至少 2 类 alpha 的 shared gate / filter > risk / sizing overlay > 纯解释型 / 纯综述型主题**。

选题时先回答一句：这篇东西的 **base alpha** 是什么？若答不清，只能把它当 `filter / regime / overlay`，默认不作为最高优先级主 digest。
这里的 raw alpha 不只包括 `trend / momentum / breakout`，也包括：`mean reversion / cross-sectional / relative value / stat-arb / pairs / carry / funding / basis`。
若最近连续 2 篇 digest 被标记为“非 raw alpha”，本轮默认必须优先选择 `raw alpha` 或“可直接落地的完整策略”主题；只有明确写出“当前没有合格候选”的理由时才允许例外。

外部数据也允许进入候选，只要满足：**公开可得 / 能较快拿到 / 能映射到 `1m / 3m / 5m / 15m` 最小实验**。例如：
- `fear & greed / sentiment breadth`
- `liquidation map / open interest / funding / basis`
- `Polymarket prices / order book / implied probability breadth`
- `ETF flow / proxy pressure`
- `公开政府财务 / 发债 / 日程 / 宏观公开数据`

若外部数据天生偏低频，必须优先把它定位成：
- `regime gate`
- `filter`
- `position sizing / risk overlay`
而不是硬伪装成逐根短周期主信号。

执行要求：
1. 阅读：
- `docs/MAINLINE1_STRATEGY_FACTOR_MAP.md`
- `docs/FACTOR_BACKLOG.md`
- `docs/LEARNING_TRACK.md`
- `docs/RESEARCH_AUTOMATION_BRIEF.md`
- `docs/RECENT_PAPER_SEEDS.md`
- `reports/artifacts/literature/validated_alpha_shortlist_2026-03-10.md`
- `research/quant_digests/INDEX.md`
2. 结合最近学习进展，选 1 个不重复主题，**默认优先近 5 年新论文或高信号新仓库**；必要时可用经典论文补地基。
2.1 `PARK_REFRAME_QUEUE`、`Rank 32b/35b` 这类旧方向派生假设，默认**不是** bot7 的主主题来源；除非用户明确要求，或是为了对一篇新论文/新仓库做直接对照，否则不要把它们写成“新研究摘要”主标题。
2.5. 如果本轮主题与当前 desk 的 **raw alpha 积累、复现素材池或实盘组件拆解** 没有直接关系，要先在心里回答一句：它为什么比继续补 raw alpha 更值得？若答不上来，换题。
2.6. 正文与邮件摘要都必须先给 4 个字段：
- 主题类型：`raw alpha / filter / regime / overlay`
- 基础 alpha：`____`
- 是否可独立复现：`是/否`
- 是否可直接落地完整策略（entry/exit/sizing/risk/cost）：`是/否`

补充偏好（当前阶段提高权重）：
- 优先选择能直接扩充 **raw alpha 素材池** 的主题，例如：
  - trend / momentum / breakout
  - mean reversion
  - cross-sectional / relative value
  - stat-arb / pairs
  - carry / funding / basis
- 若当前论文 / 仓库里有一个更适合我们 desk 的旁支想法，也允许优先做它，但要明确写清它服务于哪个 raw alpha；
- 若外部数据能形成 `1m / 3m / 5m / 15m` 的最小实验，也允许优先做 `risk overlay / filter / position management / execution veto`，但不要把它们伪装成 alpha 本体。

若以上都没有合适高质量候选，再退回到更泛化的结构、确认或执行类材料。
3. 资料优先级：项目种子清单 > `n2-free-search`（免 key）> `web_fetch` 直链 > `web_search`（可选）。不要因 `web_search` 失败而中止。
4. 生成研究笔记到 `research/quant_digests/YYYY-MM-DD_HHMM_topic-slug.md`，并更新 `INDEX.md`。
5. 生成网站页并发布。
6. 在完成后，整理一份**简短、易读的中文邮件摘要**，写入一个临时 txt 文件，然后使用：
   `python3 /root/clawd/skills/codex-quota-email/scripts/send_text_email.py --subject "[momentum] 新研究摘要" --body-file <that-txt-file>`
   发送到已配置的默认收件人。
   邮件要求：
   - 优先 `1~2` 句话，容易理解；
   - 研究类任务尽量带 `1~3` 个关键数据点；
   - 再补：主题类型与基础 alpha、与当前短周期（`1m/3m/5m/15m`）关系、最小实验、文件路径、页面 URL、来源链接。
7. 正常完成后回复 `NO_REPLY`；不要把原始提醒词或过程性提示直接发到任何频道。研究结果以：markdown、网站页面、邮件摘要 为主。

要求：
- 每次只做 1 个主题
- 不要泛泛而谈
- 必须给“下一步怎么测”
- 论文 / 仓库来源尽量写清 Authors / Year / Title / Venue / DOI / Readable URL / Repo URL
- 若主题主要依赖外部数据，必须写清：数据源、公开性、更新频率、最小可复现实验口径
