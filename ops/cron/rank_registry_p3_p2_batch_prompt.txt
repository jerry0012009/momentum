你是 `jerry/momentum` 的 rank registry 批处理执行器。

目标：
- 基于现有 `rank_registry_p3_p2` 数据结构，按 15 分钟节奏串行推进 28 个 rank 的双页报告落地：
  - `report.html`
  - `decomposition.html`

关键约束：
1. 不新建新的并行任务体系；复用现有 registry/artifacts/factors 结构。
2. 每轮只推进 1 个 pending rank（由状态文件控制）。
3. 报告口径必须同时覆盖：
   - 既有研究结果可展示
   - P2/P3 策略类型 + baseline + 组件拆分
   - 策略原理人话解释
   - 信号定义（候选触发 vs 最终执行）
4. 只要本轮有真实推进，必须同步刷新 rank_registry 总表并发布站点。

执行步骤（严格按顺序）：
1. `cd /root/clawd/jerry/momentum`
2. 运行单轮推进：
   `python3 scripts/run_rank_p3_p2_report_batch_once.py`
3. 若返回 `ok=true` 且 `action` 不是 `all_done/skip_already_running`，发布站点：
   `OPENCLAW_PUBLISH_SKIP_BUILDS=1 bash scripts/publish_report_site.sh`
4. 若有文件变更，提交最小 commit：
   - `reports/site/factors/rank*/report.html`
   - `reports/site/factors/rank*/decomposition.html`
   - `reports/site/factors/rank_registry_p3_p2/report.html`
   - `reports/artifacts/rank_registry/p3_p2_generated_reports_manifest.csv`
   - `reports/artifacts/rank_registry/p3_p2_report_batch_state.json`
   commit message 建议：
   - `feat(rank-registry): advance batch <rank> dual-page report`
5. 正常完成后回复 `NO_REPLY`。

异常策略：
- 如果某个 rank 构建失败，状态会被标记为 `blocked`；不要跳过错误伪装成功。
- 如果无 pending 任务，直接 `NO_REPLY`。
