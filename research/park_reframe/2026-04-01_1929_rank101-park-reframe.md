# 2026-04-01 19:29 UTC — Rank 101 park reframe review

## Context
- Loop: `bot6 park-reframe`
- Scope this round: revisit exactly one parked rank without overturning the original `park` verdict
- Selected rank: `Rank 101 / 3-step volume dry-down long-bias gate`
- Selection reason:
  - 当前轮转默认先看 `50+`，其中 `50~79` 与 `80~110` 最近 7 天已覆盖过 `Rank 79 / 84 / 67`
  - `Rank 101` 上次 park-reframe 复盘是 `2026-03-25 04:57 UTC`，已超过 7 天，符合“最近 7 天优先换别的”的约束
  - 它属于典型“有局部 residual signal、但很容易被误包装成还能救活 standalone gate”的 parked rank，适合低频复核“最近新证据有没有把它推到值得正式 draft 新提案”

## Read set
- `docs/TODO.md`
- `docs/PARK_REFRAME_QUEUE.md`
- `docs/RECENT_PAPER_SEEDS.md`
- `research/quant_digests/INDEX.md`
- `research/park_reframe/INDEX.md`
- prior park-reframe logs:
  - `research/park_reframe/2026-03-25_0457_rank101-park-reframe.md`
  - `research/park_reframe/2026-04-01_1729_rank67-park-reframe.md`
  - `research/park_reframe/2026-04-01_1529_rank26-park-reframe.md`
- needed evidence:
  - `research/optimization_loop/2026-03-19_2233_rank101-volume-drydown-clean-replication.md`
  - `research/quant_digests/2026-04-01_1747_1h-oversold-volume-bounce-alpha.md`

## What the original rank was trying to do
- 把 `3-step volume dry-down + low-volume budget` 写成一条可审计的 `long-bias gate`
- 原始目标不是做独立 raw alpha，而是给 `Fib retest / EMA continuation` 这类 long-side hold-quality 提供更干净的放行条件，并顺手检查 short 镜像是否也成立
- 换句话说，原 Rank 101 试图回答的是：**回踩时成交量如果连续干掉，是否说明抛压吸收得更充分，从而更值得做 long continuation / reclaim**

## Why Rank 101 was parked originally
原始审计文件：
- `research/optimization_loop/2026-03-19_2233_rank101-volume-drydown-clean-replication.md`

核心结论很清楚：
- 最好看的组合臂是 `dv3_lv80`
  - `long baseline ≈ -11.71bps`
  - `long dv3_lv80 ≈ +0.12bps`
  - `win_rate ≈ 48.15%`
  - `positive_asset_ratio ≈ 66.67%`
- 但改善几乎完全靠**极端砍样本**换来：
  - `retention_vs_baseline ≈ 3.41%`
  - 实际只剩 `54` 笔 long 样本
- 跨资产并不统一：
  - `ETH ≈ +20.53bps`
  - `SOL ≈ +1.67bps`
  - `BTC ≈ -22.89bps`
- short 镜像明显失败：
  - `short dv3_lv80 ≈ -24.71bps`

翻成人话：
- 这不是“volume dry-down 完全没信息”
- 但把它写成可广泛复用的 standalone hard gate，不诚实
- 它更像一个**long-side residual note**，不是可以直接排队的新主角，所以原 verdict 必须保持 `park`

## Hard park or soft park?
**当前更像：`soft park`，但比 3 月下旬又更偏硬一点。**

为什么还不是 hard park：
- long side 的 pocket 不是幻觉，`dry-down + low-volume` 至少说明“回踩吸收完成”这类语义有一点残余解释力
- 它也确实优于“只看 low-volume”的更粗写法，说明信息不只是单纯的低量

为什么比普通 soft park 更硬：
- 原 rank 的最好结果仍主要靠 `3.41%` retention 这种极窄切样本存活
- `BTC` 仍然负，short 镜像也没有留下可复用对称性
- 最近新增证据没有把它推向“shared gate 候选”，反而在把 `volume-confirmed event` 主题重新拉回**新的 raw-alpha family**

## Is there any salvage signal?
**有，但仍然不够支撑新的 derived hypothesis。**

还留得住的可救信号只有一条主线：
- `dry-down` 本身比 `low-volume only` 更有信息
- 而且这个信息只偏 long side，更像“抛压衰减后的 hold-quality / absorption note”

但最近新证据同时又把它进一步钉死在“不是这条 rank 的 reframe”上：
- `research/quant_digests/2026-04-01_1747_1h-oversold-volume-bounce-alpha.md` 说明，volume 主题最近更诚实、也更值得 intake 的写法，是 **shock + abnormal volume -> bounce** 这种完整 raw alpha skeleton
- 也就是说，volume 不是没有边，而是更可能坐落在 **事件驱动的 oversold-bounce alpha** 上，而不是原 Rank 101 这种极窄、极稀疏的 shared long gate 上

因此当前的“可救信号”反而指向：
- 保留 Rank 101 的审计意义：它证明了 `dry-down > low-volume-only`
- 但不要把它误写成值得再起 `Rank 101b` 的 queue 项
- 如果未来真要做 volume 主题，更应作为新的 raw-alpha family 单独 intake，而不是从 Rank 101 身上继续榨第二层故事

## The single best cut
如果还保留唯一一刀，最值得改的仍然是：

- **single modification axis:** `demote 3-step dry-down from standalone long-bias gate into a long-side hold-quality note only, and stop pretending it is a queue-facing shared gate`

具体含义：
- 不再试图把 `dv3_lv80` 包装成新的 shared allow/deny gate
- 只承认它在 long side 的局部 residual meaning：回踩时若量能是连续干掉的，说明吸收完成的概率可能更高
- 它更像 future implementation note / evidence note，而不是一个值得 bot2 直接认领的新编号

但也正因为如此，本轮不再 draft：
- 这条唯一诚实修改轴，本质上是**降级角色**，不是长出一条可独立排队的新 hypothesis
- 再写 `Rank 101b` 很容易把 residual note 硬包装成 queue-facing candidate

## Is a new derived hypothesis worth drafting?
**不值得。**

原因：
1. 原 `park` blocker 仍然没被消化：`+0.12bps` 对应 `3.41%` retention，太像切样本美化；
2. 最新相关证据把 volume 主题继续推向新的 raw-alpha 家族，而不是原 Rank 101 的窄 reframe；
3. 当前最诚实的处理方式仍是：把 Rank 101 留在 `soft park` 语义里，作为 long-side hold-quality residual note，而不是生成 `Rank 101b`。

## Direct answers required by bot6 brief
- **原 rank 为什么 park？**
  - 因为 clean replication 虽然留下了 long-side `dv3_lv80` 的薄 pocket，但改善主要靠极端 retention，跨资产不统一，short 镜像也失败，不够诚实写成 standalone gate。
- **它更像 hard park 还是 soft park？**
  - `soft park`，但已更偏硬；因为 residual signal 很窄，且主要是 evidence-note 级别。
- **有没有“可救信号”？**
  - 有：`dry-down` 比 `low-volume-only` 更像 long-side 吸收完成线索。
- **最值得改的唯一一刀是什么？**
  - 把 `3-step dry-down` 从 standalone long-bias gate 降级成 long-side hold-quality note，不再假装它是 queue-facing shared gate。
- **是否值得形成新的 derived hypothesis？**
  - **不值得。** 因为最近新增 volume 证据更像新的 oversold-bounce raw-alpha family，而不是原 Rank 101 可以再诚实派生出的 `Rank 101b`。

## Final verdict
- **Final verdict:** `keep_park`
- Original `park` verdict remains intact
- Current reading: `soft park`，但已更偏硬；残余价值更像 long-side hold-quality note，而不是新 queue 项
- Recent volume evidence does not rescue Rank 101; it points to a different raw-alpha family instead

## Queue action
- Keep `Rank 101` parked
- Do **not** draft `Rank 101b`
- Do **not** change top-level `docs/TODO.md` scheduling

## File-change / commit note
- This round only updates the park-reframe log, index, and queue
- No selective commit was made because the task only required minimal documentation updates, and the shared workspace may contain unrelated dirty files
