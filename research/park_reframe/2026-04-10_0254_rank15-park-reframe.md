# 2026-04-10 02:54 UTC · Rank 15 park reframe

## Selected rank
- `Rank 15`
- selection note:
  - 本轮按用户约束只看 `Rank 1~37` 的已 `park` 条目；
  - `Rank 15` 最近一次复盘是 `2026-04-02 16:38 UTC`，已超过 7 天窗口；
  - 它属于典型的 `S/R / zone / context` 主题，适合用 4 月初之后新增的 `POC/absorption`、`anchor-open displacement` 一类新证据再问一次：原 rank 是否真的还有独立可救的一刀。

## 原 rank 为什么 park？
基于原始 intake / clean replication 与上次 reframe 记录，`Rank 15` 被 park 的原因没有变：

1. 它把 `support/resistance`、`regime-switch`、`confirmation` 三层东西揉成了一个 entry gate，职责太宽；
2. clean replication 没证明“单线 S/R + regime-switch confirm”这套写法有可独立兑现的净边，更像是样本解释语义，不像可交易 trigger；
3. 真正留下的残余信息，不是“这根 S/R 线本身能救活策略”，而是更上位的 **zone quality / hold-quality / context veto** 语义；
4. 这类残余在 desk 里早就被更诚实的宿主吸收，尤其是 `Rank 12b` 这一类 shared quality gate 路线。

一句话：`Rank 15` 失败的核心不是阈值还没调对，而是 **角色放错了**。

## 它更像 hard park 还是 soft park？
**结论：更像 hard park。**

更精确一点说：
- 对“`Rank 15` 作为 standalone S/R regime-switch confirmation gate”这个原命题，已经是 **hard park**；
- 若只谈更泛化的 `zone / anchor / hold-quality` 信息，这些信息并没有完全死，但它们已经不再属于 `Rank 15` 这条旧 rank 的独立可救空间。

所以这轮不再把它写成“还有一个等待挖掘的 soft residual”；原 rank 的可救语义基本已经被相邻 family 吸收完了。

## 现有证据里有没有“可救信号”？
**有很弱的主题级可救信号，但没有属于 `Rank 15` 本体的可救信号。**

这轮专门拿近期两类新旁证做对照：

1. **`2026-04-05` 的 `1H POC-proximal price/CVD absorption × 15m child execution` digest**
   - 它说明“关键价位附近的吸收/站稳/再推进”主题没死；
   - 但有信息量的是 **POC 邻近 + CVD absorption + child execution** 这种事件锚定结构；
   - 这和 `Rank 15` 原本那种更静态、更泛化的 `S/R line + regime switch confirm` 不是一回事。

2. **`2026-04-09` 的 `anchor-open displacement × minute-vol breakout continuation` digest**
   - 它说明“锚点 + 异常位移 + 续行”主题能成立；
   - 但真正站住的是 **session-bound anchor + same-minute sigma shell**，不是抽象的 S/R regime-switch 语义。

这两条新证据共同指向同一个结论：
- 如果 `zone / anchor` 主题还有边际价值，它也更像 **新的事件锚 raw alpha**，或 **shared quality / context layer**；
- 而不是支持把 `Rank 15` 重新写成另一个 `Rank 15b`。

## 最值得改的唯一一刀是什么？
如果硬要说“最值得改的一刀”，唯一还算诚实的说法也只能是：

> **把旧的单线 S/R regime-switch confirmation，降级成 shared zone-quality / anchor-quality veto。**

但问题是，这一刀已经没有新鲜度了：
- 它本质上就是把 `Rank 15` 的 residual 继续并入 `Rank 12b`、`hold-quality`、`anchor-quality` 那一大类；
- 这不是一个新的窄 reframe hypothesis，而只是重复一句“别再把它当 standalone entry”。

所以这轮虽然能指出“唯一能改的是继续降级角色”，但**这刀已经被相邻提案消费掉了**，不值得再单列新号。

## 是否值得形成新的 derived hypothesis？
**不值得。结论=`keep_park`。**

原因很直接：
1. 没有新的、只属于 `Rank 15` 的单一修改轴；
2. 近期新证据反而继续把主题往 `event-anchored raw alpha` 或 `shared quality layer` 外推；
3. 再写一个 `Rank 15b`，大概率只是重复 `Rank 12b` / 相邻 anchor-quality family 的语言，审计价值很低。

## Trade on / trade off（仅作“不立项”的说明）
本轮不 draft 新假设，因此这里只写“不立项版”摘要：
- **trade on**：保留“关键价位附近的 hold / acceptance / absorption 可能有信息”这个宽主题判断；
- **trade off**：放弃“旧 Rank 15 还能以 standalone S/R regime-switch gate 身份诚实重开”的想法。

## Final verdict
- `keep_park`
- original verdict kept: `park`
- brief note:
  - `Rank 15` 对原命题已更像 hard park；
  - 近期 `POC/absorption` 与 `anchor-open` 新证据没有救活旧 rank，反而进一步说明这类信息若有价值，也应迁移到新的事件锚 raw-alpha 宿主或已存在的 shared quality family；
  - 当前不诚实派生 `Rank 15b`。

## File updates in this run
- created: `research/park_reframe/2026-04-10_0254_rank15-park-reframe.md`
- updated: `research/park_reframe/INDEX.md`
- updated: `docs/PARK_REFRAME_QUEUE.md`

## Commit status
- 未做 commit。
- 原因：`jerry/momentum` 工作区当前存在大量与本轮无关的脏文件（运行态 artifacts / site / state 文件等），这轮按要求只做最小必要改动，不混提。