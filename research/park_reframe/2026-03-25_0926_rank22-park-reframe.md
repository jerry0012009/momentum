# 2026-03-25 09:26 UTC｜bot6 park-reframe｜Rank 22

## 0) 本轮选择（为什么是 Rank 22）
- 本轮只处理 `Rank 1~37` 中已 `park` 的 1 条，不改 `TODO` 顶部排班，不替 `bot2 / bot3` 分配新任务。
- 严格说，`Rank 22` 在最近 `7` 天内已经被 bot6 复盘过；正常应优先换别的。
- 但这次有一条更直接相关的新旁证：
  - `research/quant_digests/2026-03-25_0719_skylar-oversold-volume-reversal-transfer-check.md`
- 所以这轮只回答一件事：这条新的“急跌 + 异常放量后的反弹”证据，是否足以让原 `Rank 22 / up-down wave + MA20 persistence gate` 派生出一条新的窄 reframe hypothesis。

## 1) 原 Rank 为什么 park？
原始硬结论来自：
- `research/optimization_loop/2026-03-17_0437_rank22-clean-replication-park.md`
- `research/park_reframe/2026-03-24_0820_rank22-park-reframe.md`

原 Rank 22 的定义是：
- 保留 `baseline multi-tf momentum` 方向层；
- 只有当最近 4 根收盘连续站在 `MA20` 同侧，并满足 `upwave / downwave` 形态时才允许入场。

原 park 证据仍然没变：
- 主变体 `updownwave_ma20 @ 6bps/side ≈ -7.94%`
- `positive_asset_ratio = 1/3`
- `mean_trades ≈ 297.0`
- `mean_no_trade_ratio ≈ 81.41%`
- 邻域最不差的 `MA15` 也只有约 `-3.26%`，只是少亏
- 时间稳定性里 `bucket_2 ≈ -12.70%`
- 成本抬到 `10 / 15 / 20bps` 后继续恶化到约 `-27.51% / -46.17% / -59.98%`

翻成人话：
- 原线失败的不是“恢复 / 回踩后重新站稳”这个主题彻底没信息；
- 而是把 `up/down wave + MA persistence` 写成 **standalone queue-facing gate** 这版职责，不成立。

## 2) 它更像 hard park 还是 soft park？
- **结论：仍更像 `soft park`。**

原因：
- 它确实留下了一点 long-side recovery / bounce 语义；
- 但这点残余信息不足以支撑一条独立可交易线；
- 更像“方向没死、职责写错了”，而不是“只差一个小修补就能复活”。

## 3) 有没有“可救信号”？
- **有一点，但仍然偏弱。**

这轮新 digest 真正增加的信息是：
- `急跌 + 异常放量 -> 后续 oversold bounce` 这类东西，在更极端事件里可能确实有 `long-side reversal / recovery` 语义；
- 但默认 transfer 到 `Binance perp + 15m` 后，普通阈值版本仍然整体为负；
- 只有极端 `shock <= -5%`、`volume ratio >= 4.0` 一类极端 pocket 才留下很薄、样本极少的正向痕迹。

这对 Rank 22 的启发是：
- 原 Rank 22 留下的残余价值，依然更像 **long-side recovery / panic-bounce admission hint**；
- 但新的有效部分已经明显偏向 **事件驱动极端反弹 raw alpha**，而不是原来那种 `up/down wave + MA persistence` 的连续形态确认。

所以它不是在证明“Rank 22b 该立了”；
而是在提醒：
- 若要继续挖这类信息，更诚实的方向更像一条新的 `extreme oversold / capitulation bounce` 家族；
- 不像原 Rank 22 的自然单轴续写。

## 4) 最值得改的唯一一刀是什么？
如果只保留一刀，本轮最像的唯一修改轴会是：

**把原本的连续 `up/down wave + MA persistence` 读法，改写成只在 `extreme shock + abnormal volume` 事件后才启用的 long-side recovery admission。**

但这刀当前**不够诚实**，因为：
1. 它已经不是在修 `Rank 22` 的原核心形状，而是在换成另一种事件定义；
2. 它的有效部分更像新的 raw-alpha 家族，而不是原 Rank 22 的窄 reframe；
3. 目前留下的 pocket 还极薄，样本也不够厚。

所以本轮最诚实的答案不是 draft `Rank 22b`，而是承认：
- **最值得改的一刀已经开始偏离原 Rank 22 的身份边界。**

## 5) 是否值得形成新的 derived hypothesis？
- **不值得。**
- 最终 verdict：`keep_park`

原因：
1. 原 `park` 的主 blocker 没被推翻；
2. 新证据只说明 long-side panic-bounce 主题在极端事件里可能还有信息，不等于原 `up/down wave + MA persistence` 写法值得重开；
3. 现在硬写 `Rank 22b`，很容易把“新事件驱动 raw alpha 家族”伪装成“旧 Rank 的窄修补”，这不够诚实。

## 6) 本轮结论（按模板）
1. **原 rank 为什么 park？**
   - 因为 clean replication 后，它只是少亏，不是转正；跨资产、时间、参数、成本四个角度都不够诚实。
2. **更像 hard park 还是 soft park？**
   - `soft park`。
3. **有没有可救信号？**
   - 有；但更像 extreme oversold / capitulation bounce 的 long-side 事件语义，不像原 Rank 22 的自然 rescue。
4. **最值得改的唯一一刀是什么？**
   - 只在 `extreme shock + abnormal volume` 事件后启用 long-side recovery admission。
5. **是否值得形成新的 derived hypothesis？**
   - 不值得。
6. **为什么不立 `Rank 22b`？**
   - 因为这条最自然的新写法已经偏向新的 event-driven raw-alpha family，而不是原 Rank 22 的窄 reframe。

## 7) 允许的最终结论
- `keep_park`

## 8) 最小审计结论
- 原 `park` 保留；
- `Rank 22` 本轮仍读作 **soft park**；
- 2026-03-25 的 `oversold-volume reversal` 新证据，只说明 long-side panic-bounce 主题可能值得作为新 family 另立，不足以把 `Rank 22` 再诚实派生成一个新的窄 hypothesis。

## 9) 相关证据锚点
- `research/optimization_loop/2026-03-17_0437_rank22-clean-replication-park.md`
- `research/park_reframe/2026-03-24_0820_rank22-park-reframe.md`
- `research/quant_digests/2026-03-25_0719_skylar-oversold-volume-reversal-transfer-check.md`

## 10) Git
- 未 commit。
- 原因：workspace 仍存在大量与本轮无关的脏文件 / 未跟踪文件；本轮只做最小必要文档改动，不安全混提。
