# 2026-04-03 04:11 UTC · Rank 103 park reframe

## Selected rank
- `Rank 103`
- selection note: 本轮按 queue-facing 轮转从 `80~110` 号段里挑 1 条已 `park` 的 rank。`Rank 84 / 101` 最近 7 天已复盘过，而 `Rank 103` 上次 bot6 复盘停留在 `2026-03-20`，已超过 7 天；同时最近新增的 `turning-point confirmed continuation` 证据，值得再判断一次：它是在救旧 `Rank 103`，还是把主题继续推向新的 structure-aware trend raw-alpha family。

## Original park reason
原始 authoritative 证据：
- `research/optimization_loop/2026-03-20_0034_rank103-confirmed-extremum-intake.md`
- `research/optimization_loop/2026-03-20_0054_rank103-clean-replication-park.md`

原 `Rank 103` 被 park 的原因没有变：它把 **confirmed extremum / honest fib anchor** 往 queue-facing shared gate 方向推进，但最小 clean replication 只证明了“锚点画早会把回踩看浅”，没有证明“改成 confirmed anchor 后就能把 post-cost expectancy 拉正”。

冻结版最关键结果：
- `provisional admit_rate ≈ 6.75%`
- `confirmed admit_rate ≈ 17.53%`
- `bucket_shift_rate ≈ 12.59%`
- `provisional proxy post_cost_expectancy ≈ -4.09bps`
- `confirmed proxy post_cost_expectancy ≈ -4.01bps`
- long 侧只是不那么差：`-4.03bps -> -3.81bps`
- short 侧反而略坏：`-4.15bps -> -4.20bps`

翻成人话：
- 它确实纠正了 measurement；
- 但没把策略质量救活；
- 所以原审计意义必须保留：**失败对象是“把 confirmed extremum honest anchor 当成值得继续占用 queue-facing scout 预算的 shared gate”这件事，不是“anchor honesty / turning-point confirmation 主题永远没用”。**

## Hard park or soft park?
- 本轮判断：`soft park，但已明显偏硬`

为什么不是 pure hard park：
- 它至少留下了一个真实、可复用的 audit 结论：锚点确认顺序会实质改变 Fib 深度归类；
- 这说明 `measurement honesty` 不是伪问题。

为什么又已明显偏硬：
- clean replication 已经把最自然的一刀跑过了，结果仍没把 expectancy 推过 0；
- 最近新证据的新增价值，也越来越落在 **confirmed continuation / event-style raw alpha**，而不是继续支持 `Rank 103` 这种 anchor-correction gate 自己升格。

## Any salvage signal?
有，但更像“主题旁支继续长大”，不是“旧 rank 还能再切一刀”。

本轮最 relevant 的新增证据：
- `research/quant_digests/2026-03-31_2248_turning-point-confirmed-tsmom-alpha.md`
- `research/quant_digests/2026-04-02_0041_largebody-engulfing-reversal-alpha.md`

这些新证据共同在说：
1. 市场里确实存在 **结构确认后的一小段 continuation / reversal pocket**；
2. 更值得交易的是“确认事件之后的 price impulse / snapback”本身；
3. 而不是继续围绕 `confirmed extremum` 去当一个更上游、但仍然 expectancy 为负的 shared gate。

换句话说：
- 可救信号存在；
- 但它在救的是 **confirmed turning-point continuation / event-triggered structure alpha** 这一家族；
- 不是在救旧 `Rank 103` 这具以 honest anchor 为主语的 queue-facing 写法。

## Single best cut
如果只保留唯一一刀，当前最诚实的写法也不是新开 `Rank 103b`，而只是把它继续收口为：

> **demote confirmed extremum from queue-facing shared gate into an offline anchor-correction / verdict-diagnostic layer**

也就是：
- 它可以继续服务 Fib depth、retest 深浅、failure verdict 的 measurement honesty；
- 但不应再把自己包装成值得单独占位的 scout / gate 候选。

为什么这轮不把它写成新的 derived hypothesis：
- 这条“唯一一刀”本质上已经被原 `park / evidence pool` 结论消费掉；
- 再写 `Rank 103b`，只是在把同一句话换个名义重复入队；
- 最近真正新增的 edge 已经转向新的 event-level raw-alpha 主语，不再属于旧 rank 的窄 reframe。

## Derived hypothesis?
- 结论：`keep_park`
- 不新增 `derived hypothesis`

原因：
1. 原 `park` verdict 仍完整成立；
2. 最近新证据没有提供一个仍属于 `Rank 103` 自身、且能把 expectancy 诚实推过门槛的单一修改轴；
3. 当前唯一还诚实的残余价值，只是继续把它留在 `anchor-correction / diagnostic note` 角色层；
4. 若继续派生，会模糊原 `park` 的审计边界，并把新 family 的 raw alpha 错挂到旧 rank 名下。

## Final verdict
- `keep_park`
- original verdict kept: `park`
- short note: `soft park，但已明显偏硬；confirmed extremum 仍有 measurement honesty 价值，但最近新增的 turning-point confirmed continuation / engulfing 证据更像新的 structure-aware event raw-alpha family，不足以诚实派生 Rank 103b`

## File actions
- 新增本轮日志：本文件
- 更新：`research/park_reframe/INDEX.md`
- 更新：`docs/PARK_REFRAME_QUEUE.md`

## Commit
- 本轮默认不做 commit。
- 原因：仓库存在大量共享脏文件，且 `docs/PARK_REFRAME_QUEUE.md` 已有并发修改风险；本轮只做最小必要文档改动，避免混提。
