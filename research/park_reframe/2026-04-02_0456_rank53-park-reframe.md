# 2026-04-02 04:56 UTC · Rank 53 park reframe review

## Scope
- Source rank: `Rank 53 / close-confirmed CHoCH compression gate`
- Original authoritative verdict stays: `park / evidence pool`
- This round only asks: **在不推翻原 park 的前提下，最近新增的 `turning-point confirmed continuation` 证据，是否足以让 Rank 53 再诚实派生一条新的窄 reframe hypothesis。**

## Why this rank this round
- 按 `bot6` 默认轮转，当前优先看 `50~79` 号段。
- `Rank 53` 自 2026-03-18 clean replication 被压回 `park` 后，最近 7 天内未被 `bot6` 复盘。
- 它属于那类“看起来像结构确认有点道理，但 clean replication 只换来砍样本减亏”的旧 parked rank，符合低频复盘优先级。
- 最近新增的最相关旁证是：
  - `research/quant_digests/2026-03-31_2248_turning-point-confirmed-tsmom-alpha.md`
- 本轮真正要回答的是：**这条新证据是在支持 Rank 53 的旧 shared failure-gate 语言，还是其实已经更像一条新的 structure-aware trend raw-alpha family。**

## Read set
- required:
  - `docs/TODO.md`
  - `docs/PARK_REFRAME_QUEUE.md`
  - `docs/RECENT_PAPER_SEEDS.md`
  - `research/quant_digests/INDEX.md`
  - `research/park_reframe/INDEX.md`
  - recent reframe logs:
    - `research/park_reframe/2026-04-02_0246_rank18-park-reframe.md`
    - `research/park_reframe/2026-04-01_2332_rank25-park-reframe.md`
- needed evidence:
  - `research/optimization_loop/2026-03-18_1034_rank53-source-intake-guard-passed.md`
  - `research/optimization_loop/2026-03-18_1102_rank53-clean-replication-park.md`
  - `research/quant_digests/2026-03-31_2248_turning-point-confirmed-tsmom-alpha.md`

---

## 1) 原 rank 为什么 park？
原 `Rank 53` 被 park 的原因很明确，而且这轮没有被推翻。

最小 clean replication 的主读法：
- `breakdown_reclaim_short + liquidity_sweep_veto @ 6bps/side`
  - `mean_total_return ≈ -2.88%`
  - `positive_asset_ratio = 0/3`
  - `mean_trades ≈ 8.0`
  - `mean_trade_count_retention ≈ 39.97%`
- 对照 `base`
  - `mean_total_return ≈ -3.55%`
  - `mean_trades ≈ 20.3`

时间口袋也没有给出像样翻正：
- `bucket_1 ≈ -0.33%`
- `bucket_2 ≈ -1.77%`
- `bucket_3 ≈ -0.79%`

翻成人话：
- `Rank 53` 不是“只差一点 admission wording”；
- 它只是把 shared 结构 gate 收紧之后，**用明显更少的交易换来一点点少亏**；
- 但跨资产仍然 `0/3` 为正，time pockets 也没有干净 pocket。

所以原 `park` verdict 必须保留，不能被这轮新结构动量证据冲掉。

## 2) 它更像 hard park 还是 soft park？
**这轮仍读作 `soft park`，但已经很接近偏硬的 soft park。**

为什么还不是 hard park：
- `close-confirmed CHoCH / no-wick-no-flip` 这类结构语言，本身不是胡说；
- clean replication 里它确实相对 base 少亏一点，说明“别让 wick 直接翻面”有一点点信息量。

为什么已经明显偏硬：
- 这点改善几乎全部来自 `trade_count_retention ≈ 40%` 的砍样本；
- 改善后仍然 `0/3` 资产为正；
- time-bucket 没有留下一个足够诚实的局部 pocket；
- 也就是说，它留下的是很薄的结构语义，不是还差一步就能转成 queue-facing 新对象的 residual。

## 3) 现有证据里是否存在“可救信号”？
**有，但它更像在支持新的 trend raw-alpha family，而不是支持 `Rank 53b`。**

### 可救信号 A：结构确认后 continuation 这个主题并没死
`2026-03-31_2248_turning-point-confirmed-tsmom-alpha.md` 的核心信息是：
- 真正值得先测的是 `confirmed turning-point continuation`；
- 更像一条 **structure-aware trend raw alpha**，主语是“结构确认后的延续”，不是“shared CHoCH failure gate”。

### 可救信号 B：Rank 53 的“close-confirmed，不让 wick 直接翻面”语义确实和它有邻接
这说明原 Rank 53 不是完全读错方向；
但问题在于，邻接不等于还能诚实派生。

因为这条新证据的更自然落脚点已经是：
- 把 turning-point / critical-point exceedance 当作 **新的 continuation 主触发**；
- 或者把它写成更上位的 trend family 骨架。

而 Rank 53 原本的主语是：
- 给现有 base setups 加一个 `close-confirmed CHoCH / liquidity sweep` shared failure gate。

这两者不是同一层。

## 4) 最值得改的唯一一刀是什么？
**如果硬要保留唯一一刀，最诚实的一刀不是继续打磨 shared failure gate，而是把主语收缩成：只交易 `confirmed turning-point exceedance` 后的 continuation pocket。**

也就是：
- 不再试图让 `CHoCH / sweep veto` 充当三条 base setup 的共享翻向/压缩 gate；
- 只保留“结构已确认、关键点被越过后，再做 continuation”这一件事。

但这恰恰说明它已经**不再是 Rank 53 的窄 reframe**：
- 它把角色从 `shared failure gate` 改成了 `structure-aware trend raw alpha / primary trigger`；
- 这更像新的 family-level intake 语言，而不是原 parked rank 的单轴救法。

## 5) 是否值得形成新的 derived hypothesis？
**不值得。结论：`keep_park`。**

理由：
1. 原 `park` blocker（主要靠大幅砍样本减亏、跨资产仍 `0/3` 为正）没有被新证据化解；
2. 新 evidence 支持的是更上位的 `turning-point confirmed continuation` raw-alpha family，而不是 `Rank 53` 的 shared failure-gate 写法；
3. 若现在硬写 `Rank 53b`，实质上是在把“新的趋势结构 raw alpha”误包装成“旧 rank 的窄 reframe”，审计上不诚实。

## 6) trade on / trade off 怎么读？
本轮不新增派生，只做审计式复述：

- `trade on`：
  - 原 Rank 53 若还保留任何残余价值，只能读成一句很窄的话：**不要因为 wick / 单次 sweep 就立刻翻结构叙事，至少等 confirmed close。**
- `trade off`：
  - 一旦把它写得足够有效，往往就会演变成“只做更少、更后验确认的趋势 continuation”；
  - 这时你得到的就不再是 shared gate，而是另一个更慢、更稀疏的 raw alpha；
  - 也就不该再算作 `Rank 53b`。

---

## Final verdict for this round
- `verdict`: `keep_park`
- `original_rank_verdict_kept`: `park`
- `park_type_read`: `soft park，但已明显偏硬`

## Minimal audit note
本轮不重开 `Rank 53`，也不新增 `Rank 53b`。

更诚实的记录是：**最近新增的 `turning-point confirmed continuation` 证据，说明结构确认后的趋势延续本身仍值得作为新的 raw-alpha family 研究；但它并没有把原 `Rank 53` 的 shared `close-confirmed CHoCH / liquidity-sweep veto` 写法变成可诚实再派生的窄修改轴。**

## Git
- 本轮只做最小必要文档改动；未做 commit。
- 原因：共享工作区存在大量与本轮无关的脏文件，当前不适合安全 selective commit。
