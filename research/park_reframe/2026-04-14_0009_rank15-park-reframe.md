# 2026-04-14 00:09 UTC · Rank 15 park reframe

## Selected rank
- `Rank 15`
- selection note:
  - 本轮按用户约束只看 `Rank 1~37` 的已 `park` 条目；
  - 近 `7` 天内这一区间几乎都已被 bot6 低频复盘过，因此这轮只在“有新旁证”的前提下做一次最小重看；
  - `Rank 15` 上轮复盘是 `2026-04-10 02:54 UTC`，且 `2026-04-13` 新增了 `midpoint-split dual-LVN range reversion` 这条更贴近 `support/resistance / volume-profile` 主题的旁证，足够用来再确认一次：旧 rank 是否还值得切出独立窄 reframe。

## 原 rank 为什么 park？
`Rank 15` 被 park 的原因为：

1. 它把 `support/resistance`、`regime switch`、`confirmation gate` 三层职责揉在了一起，主语过宽；
2. clean replication 没证明“单线 S/R + regime-switch confirm”这套写法本身能留下可独立兑现的净边；
3. 残余信息更像 `zone quality / acceptance / hold-quality`，而不是一条独立 entry alpha；
4. 这类残余早已被相邻宿主吸收，尤其是 `Rank 12b` 一类 shared quality gate 路线。

一句话：原 rank 的问题不是参数还没拧对，而是 **角色错位**。

## 它更像 hard park 还是 soft park？
**结论：对原命题更像 hard park。**

更细一点：
- 若问 `Rank 15` 作为 standalone `S/R regime-switch confirmation gate` 能否诚实重开，答案已经接近 `hard park`；
- 若只问 `zone / anchor / acceptance` 主题是否还有信息，答案是“有，但已不再属于旧 Rank 15 的独立可救空间”。

## 有没有“可救信号”？
**有主题级信号，但没有属于旧 Rank 15 本体的可救信号。**

这轮重点对照了两类新旧旁证：

1. `2026-04-05` 的 `1H POC-proximal price/CVD absorption × 15m child execution`
   - 说明关键价位附近的吸收/接受/再推进主题并未失效；
   - 但真正有信息量的是 `POC 邻近 + CVD absorption + child execution` 这一整套事件锚结构，不是旧 Rank 15 那种静态 `S/R line + regime switch confirm`。

2. `2026-04-13` 的 `midpoint-split dual-LVN range reversion`
   - 进一步说明 `LVN / zone / support-resistance` 主题仍能在更具体的 volume-profile 结构里留下边；
   - 但它救活的是 **更窄的 volume-profile raw-alpha 宿主**，而不是旧 Rank 15 这条泛化 S/R regime-switch gate。

这两条证据都在讲同一件事：
- `support/resistance` 主题没有死；
- 但真正能活下来的，是更具体的 `POC / LVN / absorption / event-anchored` 宿主；
- 不是把旧 Rank 15 再包装成一个新的 gate。

## 最值得改的唯一一刀是什么？
如果硬要写“唯一一刀”，也只能是：

> **把旧的单线 S/R regime-switch confirmation，继续降级成 shared zone-quality / acceptance-quality veto。**

但这刀已经没有新鲜独立性：
- 本质上只是把旧 residual 继续并入 `Rank 12b` / `hold-quality` / `anchor-quality` 家族；
- 它不能形成一个新的、bot2 可独立判断的 distinct hypothesis；
- 再写 `Rank 15b`，只会重复“别把它当 standalone entry”这句旧话。

## 是否值得形成新的 derived hypothesis？
**不值得。结论=`keep_park`。**

原因：
1. 没有新的、只属于 `Rank 15` 的单一修改轴；
2. 最新证据继续把主题往 `volume-profile / event-anchored raw-alpha` 宿主外推；
3. 再派生 `Rank 15b` 会与既有 `Rank 12b` / anchor-quality family 高度重叠，审计增量很低。

## Trade on / trade off（不立项版）
- **trade on**：保留“关键价位附近的 acceptance / absorption / zone-quality 可能有信息”这个宽主题判断；
- **trade off**：放弃“旧 Rank 15 还能以 standalone S/R regime-switch gate 身份诚实重开”的想法。

## Final verdict
- `keep_park`
- original verdict kept: `park`
- brief note:
  - `Rank 15` 对原命题已更像 hard park；
  - `2026-04-13` 新增的 dual-LVN range-reversion 旁证，和更早的 POC/absorption 证据一起，继续说明 S/R 主题若有价值，应迁移到更具体的 volume-profile / event-anchored raw-alpha 宿主；
  - 当前不诚实派生 `Rank 15b`。

## File updates in this run
- created: `research/park_reframe/2026-04-14_0009_rank15-park-reframe.md`
- updated: `research/park_reframe/INDEX.md`
- updated: `docs/PARK_REFRAME_QUEUE.md`

## Commit status
- 未做 commit。
- 原因：工作区当前存在大量与本轮无关的运行态 / site / state 脏文件；这轮只做最小必要改动，不混提。