# 2026-03-26 11:57 UTC｜bot6 park-reframe｜Rank 106

## 0) 本轮选择（为什么是 Rank 106）
- 本轮按 `PARK_REFRAME_QUEUE` 的默认轮转，优先继续看 `80~110` 号段。
- 最近 `7` 天的 `research/park_reframe/INDEX.md` 未见对 `Rank 106` 的 bot6 复盘记录，符合“优先换别的”原则。
- `Rank 106 / elephant candle corridor long-bias gate` 属于典型的“原 rank 已 park，但 long-side 似乎还留一点质量残余”的条目，最适合检查：这点残余到底够不够长出新的窄 reframe hypothesis。

## 1) 原 Rank 为什么 park？
原始证据来自：
- `research/optimization_loop/2026-03-20_0228_rank106-elephant-intake.md`
- `research/optimization_loop/2026-03-20_0231_rank106-clean-replication-park.md`

原 Rank 106 的核心写法是：
- 把 `elephant candle corridor` 当成确认 bar 质量门；
- 默认优先服务 `Fib retest_long / EMA continuation long`；
- 同时检查 short 镜像能不能成立。

原 park 的原因很清楚：
- `baseline`：`mean_net_ret ≈ -11.91bps`
- `body_only`：`mean_net_ret ≈ -10.71bps`，只是少亏一点
- `full_corridor`：`mean_net_ret ≈ -12.40bps`
- `full_corridor` 虽把 `fail_back_inside_4bars` 从约 `59.33%` 压到 `45.99%`，但代价是样本只剩 `41.66%` 左右
- side split 更直接：
  - `long baseline ≈ -14.16bps`
  - `long full_corridor ≈ -9.94bps`
  - `short baseline ≈ -9.88bps`
  - `short full_corridor ≈ -14.43bps`

翻成人话：
- 这条线确实抓到一点“强但不过热的 long-side 确认 bar 质量”语义；
- 但它没有把整体 expectancy 拉过成本门槛；
- short 侧还明显恶化；
- 所以它只能留下 `long-side quality filter evidence`，不够继续当 queue-facing 独立候选。

## 2) 它更像 hard park 还是 soft park？
**结论：`soft park`，但偏硬。**

原因：
- 它不是 hard park，因为 long side 的改善不是完全幻觉：`full_corridor` 至少降低了回吐进原区间的比例，也让 long 侧少亏了一截；
- 但它也明显不是“只差最后一补丁”就能重开的状态：
  - overall 仍负；
  - retention 已经砍到不低；
  - short 镜像明确失败；
  - 最自然的角色只是 long-side quality note，不像还能诚实扩成 shared gate。

## 3) 有没有“可救信号”？
**有，但不强。**

本条残余信号主要是：
1. 它更像 `long-side recovery / reclaim` 的确认质量，而不是 breakout-short 的通用 follow-up；
2. 它真正有用的部分不是“大实体越大越好”，而是“强，但别过热、别太长、别变成 exhaustion bar”；
3. 这使它和近几天已经被复盘过的两类残余很像：
   - `Rank 101` 留下的是 `long-side hold-quality residual note`；
   - `Rank 22` 留下的是 `long-side recovery / panic-bounce` 的残余语义。

问题也同样清楚：
- Rank 106 这点可救信息，仍停留在“帮助少做一些差的 long”这一级；
- 它没有证明自己值得成为新的 queue-facing 派生条目；
- 更像被已有的 long-side recovery / hold-quality family 吸收，而不是还需要单独再起一个 `Rank 106b`。

## 4) 最值得改的唯一一刀是什么？
如果只保留唯一主修改轴，本轮最值得保留的一刀会是：

**把 `elephant candle corridor` 从 standalone gate，降级成只服务 `Fib retest / EMA continuation` 的 long-side bounce-quality note，并且只保留“不过热的大实体确认”这一层，不再尝试 short 镜像。**

但这刀当前**不够诚实拿来直接 draft 新 rank**，因为：
1. 它本质上还是在说“long 侧确认 bar 质量可能有一点帮助”；
2. 这类角色已经和 `Rank 101`、`Rank 22` 一类 long-side residual note 明显重叠；
3. 继续单列 `Rank 106b`，很容易只是把“已有 family 里的残余 note”重新包装一遍。

## 5) 是否值得形成新的 derived hypothesis？
**不值得。**

最终 verdict：`keep_park`

原因：
1. 原 `park` 的主 blocker 没被推翻：post-cost 仍然整体为负；
2. 可救信号只够支持“它在 long-side quality 上有一点残余”，不够支撑新的 queue-facing 窄提案；
3. 最自然的单轴改写，其实已经过于接近已有 `long-side hold-quality / recovery` 残余家族，不诚实再派生一个 `Rank 106b`。

## 6) 本轮结论（按模板）
1. **原 rank 为什么 park？**
   - 因为它虽然能降低部分 long-side early fail，但整体仍未翻正，且 short 镜像明显恶化。
2. **更像 hard park 还是 soft park？**
   - `soft park`，但偏硬。
3. **有没有可救信号？**
   - 有；主要是 long-side 的 bounce / reclaim 质量语义。
4. **最值得改的唯一一刀是什么？**
   - 把它降级成只服务 `Fib retest / EMA continuation` 的 long-side bounce-quality note，不再尝试 short 镜像。
5. **是否值得形成新的 derived hypothesis？**
   - 不值得。
6. **为什么不立 `Rank 106b`？**
   - 因为这点残余价值已更像被现有 long-side hold-quality / recovery family 吸收，而不是仍需单独立项。

## 7) 允许的最终结论
- `keep_park`

## 8) 最小审计结论
- 原 `park` 保留；
- `Rank 106` 本轮仍读作 **soft park，但偏硬**；
- 它留下的是 long-side quality residual，不足以诚实派生成新的窄 reframe hypothesis。

## 9) 相关证据锚点
- `research/optimization_loop/2026-03-20_0228_rank106-elephant-intake.md`
- `research/optimization_loop/2026-03-20_0231_rank106-clean-replication-park.md`
- `research/park_reframe/2026-03-25_0457_rank101-park-reframe.md`
- `research/park_reframe/2026-03-25_0926_rank22-park-reframe.md`

## 10) Git
- 未 commit。
- 原因：workspace 仍存在大量与本轮无关的脏文件 / 未跟踪文件；本轮只做 park-reframe 所需最小文本更新，不安全混提。
