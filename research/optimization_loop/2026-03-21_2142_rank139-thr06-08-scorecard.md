# bot3 optimization loop — 2026-03-21 21:42 UTC

slug: `rank139-thr06-08-scorecard`

## TRADING DESK BOARD 对齐（只执行 Next 3：Run1→Run2→Run3）

### Run 1 — EMA due-check（require-due）
执行：`python3 jerry/momentum/scripts/run_ema_paper_trading_guarded_refresh.py --require-due`

结果要点（脚本原样摘要）：
- **无 due-now / overdue**，Paper Seat = `waiting_not_due`（require-due 开启，拒绝伪 refresh）
- 最靠前：Crypto 1d+1wk（BTC/ETH/SOL）约 **2.3 小时** 后到点

结论：Run1 属于 `waiting_not_due`，本轮**不得空转**，按顶板切换到 Run2（Scout Seat）。

---

### Run 2 — Rank139（P2）最小决策包：只做 thr_mult∈{0.6,0.8} 对比 + 轻量 scorecard
执行：`python3 jerry/momentum/scripts/build_rank139_cusum_event_bar_confirm_veto_clean_replication.py`

产物：
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/summary_by_arm.csv`
- `reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/report.html`
- 本次生成时间：**2026-03-21 21:44 UTC**（report 内标注）

#### 关键对比（只摘 thr=0.6 vs 0.8）
（统一：跨 BTC/ETH/SOL + 三个 setup 汇总；成本=6bps/side；hold=8×15m；latency=45m；每 asset×setup 仅取最近 18 个样本）

**baseline（两组 thr 下都一样）**
- trades=141，mean_net@6bps = **-0.1548%**，positive_ratio_net≈37.6%

**thr_mult=0.6**
- `veto_opp_dir`：trades=66（retention=46.8%），mean_net@6bps=**+0.1957%**，no_event_timeout=21.2%
- `confirm_same_dir_only`：trades=52（retention=36.9%），mean_net@6bps=**+0.1909%**

**thr_mult=0.8**
- `veto_opp_dir`：trades=70（retention=49.6%），mean_net@6bps=**+0.3423%**，no_event_timeout=38.6%
- `confirm_same_dir_only`：trades=43（retention=30.5%），mean_net@6bps=**+0.5363%**，positive_ratio_net≈60.5%

#### 我对 desk 决策的读法（先保守、避免“后验筛选”误读）
- `confirm_same_dir_only@0.8` 的提升最大，但 retention=30.5%（砍得更狠），更像**后验挑强势**；需要更谨慎，避免把“筛掉弱交易”误当成“可执行的实时 gate”。
- `veto_opp_dir@0.8` 的 uplift 没那么夸张，但 retention≈50% **更像可执行的 veto**：只把“先走反方向”的坏单踢掉，剩下的仍保持一半左右交易数。
- `thr=0.6` 的 no_event_timeout 更低（21% vs 39%），但净值 uplift 也更小。

**本轮建议的最小落点（不扩张战线）：**
- 作为 P2→P3 的候选 gate，优先把默认参数口径写成：
  - **`thr_mult=0.8 + arm=veto_opp_dir`**（主推，原因=可执行性/retention 更稳）
  - `confirm_same_dir_only` 暂不作为默认执行臂，只保留为“上限参考/对照臂”。

#### Scout Promotion Scorecard（5项 0~3 分）
> 评分只服务“是否值得升到 narrow paper pilot”，不是学术完整性。

1) **成本后 uplift（相对 baseline）**：**2/3**
- baseline -0.1548% → veto@0.8 +0.3423%（改善明确；但仍需更大样本确认）

2) **Retention（交易数保留）**：**2/3**
- veto@0.8 retention≈49.6%（还能用）；confirm@0.8 只有 30.5%（偏像后验筛选）

3) **广度/共享性（跨 setup/资产的一致性）**：**1/3**
- 本轮是汇总表；脚本为 13m guard 采用“最近 18 信号/asset×setup”的小样本策略，**仍缺 per-asset / per-setup 的稳定性读数**。

4) **诚实性（trade on/off + no leakage）**：**3/3**
- gate 只用 entry 后 1m close 的 first-event；时序上成立；脚本写法可审计。

5) **接线/运维复杂度（能否进入 narrow paper pilot）**：**2/3**
- veto_opp_dir 是最直觉的执行 gate；但 `no_event_timeout` 比例偏高（thr=0.8 约 38.6%），需要明确“timeout 如何处理”（继续 baseline？还是视为不通过？）。

**Hard-fail flags（本轮未触发，但需要下轮补一句硬口径）：**
- [ ] lookahead/leakage：未见
- [ ] 规则不清/不可执行：**部分存在（timeout 分支口径需写死）**
- [ ] post-cost collapse：未见（反而 uplift）

---

### Run 3 — 硬结论分支（本轮只落 1 个）
结论：**keep_P2（不直接 promote_P3）**。

原因（一句话）：目前 uplift 可信，但**“timeout 分支 + 分资产/分setup 一致性”**两件事还没写成能上纸的最小 spec。

指定唯一补洞（下一轮只做这一个）：
- **补 1 页“P3 paper spec 草案”**：固定 `thr_mult=0.8`，明确 `no_event_timeout` 的处理（默认=不 veto，走 baseline；或视为不通过），并在同页追加一个最小 per-asset 表（BTC/ETH/SOL）验证 uplift 不是单币 pocket。

---

## 本轮“1主点 + 1紧邻子点”自检
- 主点：Rank139 的 thr_mult 0.6 vs 0.8 对比 + scorecard
- 紧邻子点：给出 Run3 的硬结论与“唯一补洞”
- 未打开多个 Scout 候选：✅
