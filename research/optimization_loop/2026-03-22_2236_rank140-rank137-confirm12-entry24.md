# Bot3 Optimization Loop — Rank 140 / Rank 137（`confirm12_entry24`）显式三臂重跑

- 时间：2026-03-22 22:36 UTC
- 严格顺序：Run1 → Run2 → Run3
- 范围控制：本轮仅 **1 个主点（Rank 140 / pbo-cscv honesty gate）** + **1 个紧邻子点（Rank 137 / state expiry latency budget）**。

## 1) Run 1 = EMA due-check first（Paper Seat）
执行：
```bash
cd /root/clawd/jerry/momentum
python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due
```
结果：`waiting_not_due`（exit code 2，合规）
- 当前无 `due-now / overdue` lane
- 最靠前：`Crypto 1d+1wk（BTC/ETH/SOL）`，约 `1.4 小时` 后到点

结论：Run1 不允许伪造 refresh，立刻切到下一允许动作。

## 2) Run 2 = Hosted P3 continuity（低频、事件驱动）
检查：
- `reports/artifacts/manual_narrow_paper_lanes/manual_narrow_paper_last_run_summary.json`
  - `run_at_utc = 2026-03-22T22:09:51Z`
  - `new_closed_trades_appended = 0`
- `reports/artifacts/manual_narrow_paper_lanes/manual_narrow_paper_open_positions.csv`
  - 仍仅见 `Rank 17 / ETH-USD short`、`Rank 17 / SOL-USD short` 的既有 open inferred 口径
  - 本轮未见新的 `refresh 失步 / ledger 爆雷 / open-position 异常 / red-watch`

结论：Run2 合规跳过，不做近义 continuity 巡检。

## 3) Run 3 = Scout Seat（只选 1 个：Rank 140 / pbo-cscv honesty gate）
### 主点
继续沿顶板当前唯一主点：`Rank 140 / pbo-cscv deflated sharpe honesty gate`。

### 紧邻子点（仅 1 个）
本轮只拿 **Rank 137 / state expiry latency budget** 做 1 次最小显式三臂重跑：
- family：`Rank 137`
- strict arm：`confirm12_entry24`

选择它的原因：
- 它就在当前 Active Scout 排序里，且此前结论是 `single-pocket dependency + post-cost collapse`；
- 这次显式三臂重跑，正好回答一个很窄的问题：**把“有确认窗口 + 有 entry latency budget”单独抽成 kept 臂后，是否真的比被 veto 的那一臂更好？**

## 4) 最小兼容处理
原始文件：
- `reports/artifacts/scout_rank137_state_expiry_latency_budget_15m/trade_log.csv`

兼容动作（只做最小映射，不改原 family 产物）：
- 生成规范化输入：`reports/artifacts/pbo_cscv_honesty_gate/rank137_trade_log_signal_ts_confirm12_entry24.csv`
- 将 `signal_time -> signal_ts`
- 将 `baseline_no_expiry -> baseline`（仅为复用显式三臂脚本）

新产物：
- `reports/artifacts/pbo_cscv_honesty_gate/rank137_trade_log_signal_ts_confirm12_entry24.csv`
- `reports/artifacts/pbo_cscv_honesty_gate/rank137_explicit_three_arm_returns_matrix_confirm12_entry24.csv`
- `reports/artifacts/pbo_cscv_honesty_gate/rank137_explicit_three_arm_returns_matrix_confirm12_entry24.meta.json`
- `reports/artifacts/pbo_cscv_honesty_gate/rank137_explicit_three_arm_scorecard_confirm12_entry24/rank139_pbo_cscv_dsr_scorecard.csv`
- `reports/artifacts/pbo_cscv_honesty_gate/rank137_explicit_three_arm_scorecard_confirm12_entry24/rank139_pbo_cscv_dsr_meta.json`
- `reports/artifacts/pbo_cscv_honesty_gate/rank140_explicit_three_arm_family_board.csv`
- `reports/artifacts/pbo_cscv_honesty_gate/rank140_explicit_three_arm_family_board.meta.json`

## 5) 显式三臂结果（Rank 137 / `confirm12_entry24`）
对齐结果：
- `baseline = 818`
- `gate_kept = 478`
- `gate_veto = 340`
- `kept:veto = 478:340`

人话解释：
- 这不是 `Rank112` 那种极端失衡；
- 也不是小样本 pocket 才勉强拆出来；
- 它是一个 **真拆开了、且 kept 臂占比仍可读** 的 family。

## 6) canonical scorecard 结果
### hard read
- `PBO = 0.0000`
- `lambda_median = 0.75`
- `verdict = guard_passed`

### 三臂读数（net 已扣 12bps roundtrip）
- `baseline`：`trades=818`，`mean_net_6bps ≈ -0.0949%`，`sharpe ≈ -0.0700`
- `gate_kept`：`trades=478`，`mean_net_6bps ≈ +0.1314%`，`sharpe ≈ +0.1172`
- `gate_veto`：`trades=340`，`mean_net_6bps ≈ -0.4130%`，`sharpe ≈ -0.2618`
- `kept - veto mean diff ≈ +0.5444%`

### 人话解释
- 这是当前 `Rank 140` family board 里第一条**不仅 split 可读，而且 kept 臂明显优于 veto 臂**的 family；
- 不是“少亏一点”而已，而是从 baseline 的轻微负值，被 strict kept 臂拉到了正均值 / 正 Sharpe；
- 与此同时，被 veto 掉的那臂明显更差，说明这个 strict arm 的区分方向是对的，而且幅度足够大；
- 就这张单 family 离线 scorecard 而言，它是目前最像样、最接近 honesty-layer canonical 参考样本的一条。

## 7) family board 更新后的读法
新增一行：
- `Rank137 / confirm12_entry24`：`478:340`，`PBO=0.0000`，`guard_passed`

更新后更清楚的结论：
- `Rank137` 目前成了 board 上 **best PBO rank**；
- `Rank112` 之前那个看起来不错的低 PBO，如今更明确只是 `arms 极端失衡` 的误导样本；
- `Rank125/111` 仍保留“split 可解释但 guard 失败”的参考价值；
- **Rank137** 则第一次同时满足了：`split 可读 + kept 明显优于 veto + canonical scorecard 过关`。

## 8) 本轮 hard verdict / next
- ✅ Run1 合规 `waiting_not_due`，未伪造 refresh。
- ✅ Run2 合规跳过，无 hosted P3 status-changing event。
- ✅ Run3 完成 1 个最小硬交付：`Rank137 / confirm12_entry24` 显式三臂 matrix + canonical scorecard，并回写 family board。
- ✅ 当前 `Rank 140` 不再只是“多数 family 都 fail”的示范板；它现在第一次拿到一条 **可当 canonical 正例读法** 的 family。

## 9) 留给后续 run（本轮不展开）
- 下一轮若继续 `Rank 140`，优先不是盲目扩 family 数量，而是先围绕 `Rank137` 这个正例写清楚：它到底是“确认窗口”更关键，还是“entry latency budget”更关键；
- 但本轮不再继续拆 `confirm_window_12`，避免同轮开第二个紧邻子点；
- 也不要因为有了一个正例，就回头重磨 Hosted P3 continuity 或重复旧 family。