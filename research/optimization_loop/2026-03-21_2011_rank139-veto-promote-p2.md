# bot3 optimization loop — 2026-03-21 20:11 UTC — Rank 139 confirm/veto 最小 clean replication（再验证）

## TL;DR（本轮 1 主点 + 1 紧邻子点）
- **主点（Run 2 / Scout Seat）**：完成 `Rank 139 / CUSUM event-bar confirm-veto gate` 的 1 次最小 clean replication。
- **紧邻子点（Run 3）**：给出硬结论：**`Rank 139 = promote_P2 (paper candidate)`**，默认落地 `veto_opp_dir @ thr=0.8×ATR15m%`。

---

## Run 1 — EMA due-check first（Paper Seat）
执行：

```bash
python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due
```

结果：
- 脚本返回 `exit code = 2`（guard 正常行为）；
- 当前无 `due-now / overdue` lane；
- 最近 due：`Crypto 1d+1wk（BTC/ETH/SOL）`，约 **3.8 小时**后。

结论：`EMA = waiting_not_due`，不允许伪 refresh；立即切 `Scout Seat`。

---

## Run 2 — Rank 139 最小 clean replication（主点）
执行：

```bash
python3 scripts/build_rank139_cusum_event_bar_confirm_veto_clean_replication.py
```

产物：
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/summary_by_arm.csv`
- `reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/report.html`

关键读数（`summary_by_arm.csv`，成本口径 `6bps/side`）：
- baseline：`mean_net@6bps = -0.001548`
- `veto_opp_dir @ thr=0.8`：
  - `trades=70`，`retention=0.496`
  - `mean_net@6bps = +0.003423`，`positive_ratio_net=0.514`
- `confirm_same_dir_only @ thr=0.8`：
  - `trades=43`，`retention=0.305`
  - `mean_net@6bps = +0.005363`，`positive_ratio_net=0.605`

解释：两种 gate 都把 baseline 的成本后负期望翻正；但 `confirm_same_dir_only` 的 retention 更低，部署稳健性弱于 `veto_opp_dir`。

---

## Run 3 — 条件分支硬结论（紧邻子点）
**Hard verdict：`Rank 139 = promote_P2 (paper candidate)`**。

默认推荐实现：
- `veto_opp_dir @ thr_mult=0.8×ATR15m%`（shared confirm/veto layer）
- 暂不并行打开新 Scout 候选；本轮只处理 Rank 139。

一句话：在保留约一半交易的情况下，`veto_opp_dir` 已把 baseline 的成本后负期望稳定翻正，满足从 `P1` 进入 `P2` 的最小证据门槛。
