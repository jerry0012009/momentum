# Bot3 Optimization Loop — Rank 140 显式三臂 family board（125 / 112 / 111）

- 时间：2026-03-22 20:50 UTC
- 严格顺序：Run1 → Run2 → Run3
- 范围控制：本轮仅 **1 个主点（Rank 140 / pbo-cscv honesty gate）** + **1 个紧邻子点（已完成 families 的 cross-family board）**。

## 1) Run 1 = EMA due-check first（Paper Seat）
执行：
```bash
cd /root/clawd/jerry/momentum
python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due
```
结果：`waiting_not_due`（exit code 2，合规）
- `Crypto 1d+1wk（BTC/ETH/SOL）`：`due_soon`，约 `3.2 小时` 后到点
- `创业板ETF 1d`：仍在 `waiting_not_due`
- 守门口径未变：当前应等真实 completed bar，到点后再 refresh，不能伪造 paper ledger

结论：Run1 不得空转，立即切到下一允许动作。

## 2) Run 2 = Hosted P3 continuity（低频、事件驱动）
检查：
- `reports/artifacts/manual_narrow_paper_lanes/manual_narrow_paper_last_run_summary.json`
  - `run_at_utc = 2026-03-22T20:40:56Z`
  - `new_closed_trades_appended = 0`
- `reports/artifacts/manual_narrow_paper_lanes/manual_narrow_paper_bot3_reentry_queue.csv`
  - `Rank 2 / 17 / 29 / 32b` 全部 `bot3_reentry_now = no`
  - `Rank 17` 虽仍有 open positions，但 trigger 条件仍是“后续 refresh 真正追加 closed trade 或 weekly-review row”

结论：无 `refresh 失步 / ledger 爆雷 / open-position 异常 / red-watch` 这类 status-changing event，本轮按顶板规则跳过，不做近义健康巡检。

## 3) Run 3 = Scout Seat（只选 1 个：Rank 140 / pbo-cscv honesty gate）
### 主点
继续沿桌面板当前唯一主点：`Rank 140 / pbo-cscv deflated sharpe honesty gate`。

### 紧邻子点（仅 1 个）
本轮**不再新开第 4 条 family**，而是把已经完成的三条显式三臂 scorecard（`Rank125 / Rank112 / Rank111`）压成一张 **cross-family board**，给下一轮选 family 时直接读。

新产物：
- `reports/artifacts/pbo_cscv_honesty_gate/rank140_explicit_three_arm_family_board.csv`
- `reports/artifacts/pbo_cscv_honesty_gate/rank140_explicit_three_arm_family_board.meta.json`

来源限定为已完成的三条 family：
- `Rank 125 / rl_gate`
- `Rank 112 / basis_extreme_plus_oi_veto`
- `Rank 111 / same_window_only`

## 4) Board 结果（只汇总已完成 family，不新增候选）
| family | strict arm | kept:veto | PBO | verdict | kept-veto mean diff (6bps net) | 人话 |
|---|---|---:|---:|---|---:|---|
| Rank 112 | `basis_extreme_plus_oi_veto` | `120:2` | `0.3143` | `guard_risky` | `-1.8637%` | PBO 表面最好，但三臂极度失衡，几乎没有真实 veto 臂可读性 |
| Rank 125 | `rl_gate` | `459:365` | `0.5714` | `guard_failed` | `+0.1116%` | split 够平衡，`gate_kept` 明显少亏，是当前最像样的显式三臂 family |
| Rank 111 | `same_window_only` | `105:93` | `0.7143` | `guard_failed` | `+0.0599%` | split 最平衡，但 OOS 稳定性仍弱，说明“有点区分度，但不够稳” |

> 这里最关键的新读法：
> - **如果只看 PBO，Rank 112 会误导人。** 它是三条里 PBO 最低的，但 kept:veto 只有 `120:2`，说明 arms 基本没拆开，不能把它当“最好 family”。
> - **如果把“显式三臂是否真的拆开”也纳入读法，Rank 125 目前更像 canonical board 的主参考 family。**
> - **Rank 111** 提供了最平衡的 split 样本，证明显式三臂定义本身可行，但它还没把 PBO 压下去。

## 5) 本轮 hard verdict
- ✅ Run1 合规 `waiting_not_due`，未伪造 refresh。
- ✅ Run2 合规跳过，无 hosted P3 re-entry event。
- ✅ Run3 留下一个新的硬交付：`Rank 140 explicit three-arm family board`。
- ✅ 这张 board 把“**PBO 好看**”和“**三臂真的拆开**”分开看，避免后续被 `Rank112` 这种极端失衡样本误导。
- ✅ 当前最像样的 family 参考位是：`Rank125`（不是因为它最赚钱，而是因为它在**split 可读性**和**guard 失败后的解释性**之间最平衡）。

## 6) 留给后续 run（本轮不展开）
- 下一轮若继续 `Rank 140`，应优先挑 **像 Rank125/111 这样 kept-veto 真的拆得开的 family**，不要回头补近义 intake/demo。
- 在进入第 4 条 family 之前，先把“`低 PBO` 但 `arms 极端失衡` 是否一律降权”写成固定读法，会比盲目继续扩 family 更值钱。
