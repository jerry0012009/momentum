# Rank 6b / ETF-US proxy lead-strength shared regime gate / fresh intake first verdict -> background

- time: `2026-04-09 11:19 UTC`
- target: `research/park_reframe/2026-03-19_0848_rank6-park-reframe.md`
- action: 判断 `Rank 6b` 的 `ETF / US proxy lead-strength shared regime gate` 是否已足够从旧 `BTC -> COIN/MSTR direct lag-trade` park 中收敛成独立、queue-facing 的 session lead-strength pocket
- success criterion: 若该对象能压成一个不被既有 `US-session / close-pocket / leader-laggard` family 吸收、且不存在单一 decisive honesty / execution blocker 的独立 pocket，则记 `keep_P1`；否则记 `background / P0`
- verdict: `background / P0`

## Why this is not a new front-slot pocket

1. **角色仍是 shared gate，不是独立 alpha 主语。**
   `Rank 6b` 的核心提议一直是把外部代理领先信号降级成 `allow / veto / sizing` 层，而不是形成独立 entry/exit。仅凭这一点，它天然更像既有宿主的 overlay，而不是新的 queue-facing pocket。

2. **4 月新增证据把主题继续外流到更独立的 raw-alpha family。**
   最近真正站住脚的相关证据，落在：
   - `US ETF midday 30m momentum pocket`
   - `US close-pocket / next-session handoff continuation`
   - `BTC lead × low-liquidity alt lag`
   - 更快、更窄的 `leader-laggard / session-handoff` 宿主

   这些证据都在把“外部市场先发现价格”写成**独立时段 alpha / 更快 lead-lag raw alpha**，而不是支持旧 Rank 6 residual 继续作为一个单独的 shared gate 家族前推。

3. **与既有 residual 高度同轴，缺少新的单轴增量。**
   `Rank 6b` 只是把 `BTC->COIN/MSTR direct lag trade` 降级成 `ETF / US proxy lead-strength gate`；这与已有的 `Rank 14b / Rank 28b` 一类 cross-asset / lead-strength / breadth gate 残余属于同一类“shared regime overlay”语义，没有看到新的、可把它们明确分开的单轴独特性。

4. **honesty / execution realism 也没有被新增证据单独救活。**
   这条线最怕的不是“完全无相关”，而是只在少数 proxy-heavy pocket 看起来有效、或者只靠大幅砍样本才显得干净。当前新增 digest 更像告诉我们“该去做独立的 session pocket / lead-lag raw alpha”，而不是证明“旧 Rank 6 residual 已经形成可泛化的 overlay pocket”。

## System-changing conclusion

`Rank 6b` 的 `ETF / US proxy lead-strength shared regime gate` 仍只是既有 `US-session / close-pocket / leader-laggard` family 已吸收的 shared overlay，4 月新增证据继续把主题外流到更独立的 session-pocket / faster lead-lag raw-alpha 宿主，因此本轮 first verdict 收口为 `background / P0`。
