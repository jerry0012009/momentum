# bot3 optimization loop — Rank139 thr_mult 0.6 vs 0.8 + P3 决策包

时间：2026-03-21 22:22 UTC

> 依据 `docs/TODO.md` 顶部 TRADING DESK BOARD：只执行 Next 3 bot3 runs。

## Run 1 — EMA due-check first（Paper Seat）
- 执行：`python3 jerry/momentum/scripts/run_ema_paper_trading_guarded_refresh.py --skip-build --require-due`
- 结果：**无 due-now / overdue lane**（`require-due` 触发退出码 2 属正常）
  - 最近到点：`Crypto 1d+1wk (BTC/ETH/SOL)` 约 **23.8h** 后
  - 结论：Paper Seat 真实 `waiting_not_due`，本轮合法切换到 Scout Seat（不空转、不伪 refresh）。

## Run 2 — Scout Seat：Rank 139 thr_mult∈{0.6, 0.8} 最小对比 + 轻量 scorecard

本轮动作：只做 **Rank139**（不打开其它 Scout 候选）。

### 2.1 复跑 clean replication（脚本自带 0.4/0.6/0.8）
- 执行：`python3 jerry/momentum/scripts/build_rank139_cusum_event_bar_confirm_veto_clean_replication.py`
- 产物：
  - `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/summary_by_arm.csv`
  - `reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/report.html`

### 2.2 核心对比（只看 thr=0.6 vs 0.8；成本=6bps；基线 trades=141 固定）

**baseline（两档一样）：**
- mean_net@6bps = **-0.1548%**
- positive_ratio_net = **0.3759**

**thr=0.6：**
- veto_opp_dir：trades=66（retention=0.468），mean_net=**+0.1957%**，pos=0.4848，no_event_timeout=0.2121
- confirm_same_dir_only：trades=52（retention=0.3688），mean_net=**+0.1909%**，pos=0.5000

**thr=0.8：**
- veto_opp_dir：trades=70（retention=0.496），mean_net=**+0.3423%**，pos=0.5143，no_event_timeout=0.3857
- confirm_same_dir_only：trades=43（retention=0.3050），mean_net=**+0.5363%**，pos=0.6047

### 2.3 轻量 Scout Promotion Scorecard（5项 0~3 + hard-fail flags）
> 评分口径：3=明显满足；2=基本OK；1=勉强；0=不满足。

推荐候选（面向 P3 纸上试运行）：**`veto_opp_dir @ thr=0.8`**

1) **净优势幅度（mean_net）**：2/3
- +0.342%（优于 thr=0.6 的 +0.196%，但不算“夸张大”）

2) **保留率/可交易密度（retention）**：2/3
- retention≈0.496（接近一半 trade 还能触发有效事件；比 confirm-only 更可用）

3) **方向一致性/逻辑清晰（same_dir_first / opp_dir_first）**：2/3
- veto_opp_dir 只是在“首次事件反向”时过滤，属于更直观的 post-entry 风险规避层

4) **诚实性风险（leakage / 用未来信息 / 过拟合迹象）**：2/3（当前无硬证据触发 hard-fail）
- 该脚本显式约束为 post-entry、时间顺序、固定 latency window；但仍需要进入 P3 后用更长样本/更严格复查进一步验证

5) **运维复杂度（接线成本/监控可解释性）**：2/3
- 需要 1m 数据+45min latency 窗；实现可控，但要明确“事件未发生”的处理（timeout 比例在 thr=0.8 下偏高：0.386）

**Hard-fail flags：**
- [ ] 明确 leakage
- [ ] 规则定义含糊（无法 operator 执行）
- [ ] 明显 post-cost collapse（目前相反：从负转正）

> 补充：`confirm_same_dir_only @ thr=0.8` 的净优势更大（+0.536%），但 retention 只有 0.305，更像“高门槛精选信号”；更适合作为 **备选的更激进版本**，不作为本轮 P3 默认。

## Run 3 — 硬结论分支：promote_P3（narrow paper pilot）

结论：**Rank139 继续成立 → promote_P3**，但把 P3 定义为“最小接线 + 最小监控”，避免扩大研究面。

### P3 最小 paper spec（草案）
- 角色定位：**post-entry event-confirm / veto layer**（叠加层，不改 entry）
- 适用入口（固定）：BTC/ETH/SOL 15m，沿用 baseline setups（ema_psar_long / fib_retest_long / breakout_short）
- gate 选择（默认）：`veto_opp_dir @ thr_mult=0.8`
  - 规则：入场后观察 1m close 的 CUSUM 首次越阈事件；若首次事件方向与持仓方向相反 → **veto / 提前撤销该笔**（或记为无效信号，不开仓）
  - latency window：45min；若超时无事件 → 视为 **不过滤**（保持 baseline；这点需在实现里明示）

### P3 最小监控（建议）
- 每次 refresh 输出：
  - baseline vs gated：trades 数、mean_net、pos_ratio、timeout_ratio
  - “被 veto 的交易”样本（Top 5）用于解释
- 触发回滚条件：连续 N 次 refresh（例如 3 次） gated 版本 mean_net 重新转负 或 retention 跌破 0.25。

---

## 本轮动作清单（给 bot2/人类对账）
- [x] Run1：EMA due-check → waiting_not_due（不空转）
- [x] Run2：Rank139 thr=0.6 vs 0.8 对比 + 轻量 scorecard
- [x] Run3：硬结论 promote_P3（给出最小 paper spec/monitoring）
