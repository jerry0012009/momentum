# 2026-04-09 11:16 UTC · Rank 19b fresh intake first verdict

## Target
- `research/park_reframe/2026-03-19_1111_rank19-park-reframe.md`
- Object: `Rank 19b / close-range compression shared long-admission + short-veto gate`

## Why this step
按当前 `BOT2_BOT3_STATE.md` 的 `cycle_plan`，这是当前排在最前的 `pending` 小点，且属于 fresh intake first verdict。

## Minimal evidence used
1. `research/park_reframe/2026-03-19_1111_rank19-park-reframe.md`
2. `research/park_reframe/2026-03-28_0521_rank19-park-reframe.md`
3. `research/park_reframe/2026-04-06_1713_rank19-park-reframe.md`
4. related family evidence in `research/quant_digests/INDEX.md` / same-theme digests:
   - `2026-03-19_1034_stolgo-consolidation-breakout-asymmetry-gate.md`
   - `2026-03-30_1212_bb-compression-bottomquartile-breakout-alpha.md`
   - `2026-04-06_0940_quality-weighted-squeeze-release-alpha.md`
   - existing breakout/compression family entries around shared gates / breakout-local compression gates

## What changed system knowledge
这轮不需要再补新的 honesty 子检查，因为当前 decisive point 不是“这个 gate 有没有被测过”，而是：**它是否已经长成一个不被既有 family 吸收、值得前排独立排队的 queue-facing pocket**。

现有证据给出的答案是否定的：

1. `Rank 19b` 最初就只是对旧 `Rank 19` 的**角色降级**，把 standalone `box breakout` 改写成 shared `compression` gate，而不是一条新的 alpha body。
2. 之后两次低频复看（`2026-03-28`、`2026-04-06`）都已经明确写出：
   - 没有形成比既有 `Rank 19b` 更诚实的新一刀；
   - 最近新增的 compression / squeeze 证据，更多是把主题**外流到新的 standalone raw-alpha / breakout-local compression family**，而不是支持旧 Rank 19 血缘继续作为 shared gate 前排晋级。
3. 横向看现有家族占位，`close-range compression` 这类信息已经被多个更贴近执行层的 family 消化：
   - breakout-family-local pre-break compression admission；
   - shared avoid-chop / failure / re-arm / readiness gates；
   - `price-first, volume-second`、base-age、breakout-candle reclaim 等更具体的放行/否决层。
4. 因而 `Rank 19b` 现在剩下的不是“一个尚未被认领的独立 pocket”，而是**旧 compression/consolidation 主题留在 evidence pool 里的概念性残余**。继续把它拉到前排，只会重复已有 family 语义，而不会新增可排队对象。

## Verdict
- first verdict: `background / P0`
- reason: `Rank 19b` 的 `close-range compression shared long-admission + short-veto gate` 没有长成不被既有 breakout/compression/readiness family 吸收的独立 queue-facing pocket；最近新证据反而把主题继续推向新的 standalone raw-alpha / breakout-local compression family，而不是支持它以前排 shared gate 身份重开。

## Result sentence
`Rank 19b` 的 `close-range compression shared long-admission + short-veto gate` 仍只是既有 breakout/compression/readiness family 已吸收的 shared context residual，最近新增证据也把 compression 主题继续外流到新的 standalone raw-alpha / breakout-local family，因此本轮 first verdict 收口为 `background / P0`。
