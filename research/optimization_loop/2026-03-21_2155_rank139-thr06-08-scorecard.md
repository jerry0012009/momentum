# bot3 optimization loop log — 2026-03-21 21:55 UTC

> cron: bot3-momentum-auto-opt-13m

## Run 1 — EMA due-check（Paper Seat）
结论：**waiting_not_due**（按规则不伪 refresh，切换到 Scout Seat）。

- 运行：`python3 jerry/momentum/scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
- 输出要点：
  - 所有 lane `next_expected_close_utc` 在未来
  - 最近到点：`Crypto 1d+1wk` 约 **2.1 小时** 后
  - `创业板ETF 1d` / `贵州茅台 1d+1wk` 约 **33.1 小时** 后

## Run 2 — Rank 139：thr_mult ∈ {0.6, 0.8} 最小对比（BTC/ETH/SOL 15m）
数据来源：`reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/summary_by_arm.csv`

### 关键对比（arm=confirm_same_dir_only）
- **thr_mult=0.6**
  - trades=52
  - retention_vs_base=0.3688
  - mean_net@6bps=+0.001909（约 +0.1909%/trade）
  - positive_ratio_net=0.5000
- **thr_mult=0.8**
  - trades=43
  - retention_vs_base=0.3050
  - mean_net@6bps=+0.005363（约 +0.5363%/trade）
  - positive_ratio_net=0.6047

### 读法（用人话）
- `thr_mult` 越高越“苛刻”：留下的交易更少（retention 更低），但平均每笔净收益更高。
- 在本次样本里，**0.8 明显更像“少做但更赚”的 confirm/veto 层**。

### 快速结论
- 若目标是「后置确认层」作为通用的 post-entry veto/confirm：**优先用 thr=0.8 做 P3 的默认候选**。
- 风险点是 retention（~0.305）偏低：需要监控“是否过拟合到少数 pocket / 是否近期崩掉”。

## Run 2b — Scout Promotion Scorecard（轻量 5 项 0~3）
对象：Rank 139（以 `confirm_same_dir_only@thr=0.8` 为默认）

| 项目 | 分数(0~3) | 依据（简述） |
|---|---:|---|
| 1. 可解释性/角色清晰 | 3 | 明确是 **post-entry event-confirm/veto layer**（不是新 alpha）。 |
| 2. 经济性（净收益 vs 成本） | 2 | mean_net@6bps 为正且幅度较大；但 trades 少，需要防 “偶然”。 |
| 3. 覆盖度/稳健性（多资产/多时段） | 1 | 当前为 BTC/ETH/SOL 汇总，仍需补 “近期窗口/分段” 稳定性最小检查。 |
| 4. 操作可接线（paper/tiny-live） | 2 | 作为 **已有 entry 之后的确认/否决** 很容易接线；需要定义最小监控指标。 |
| 5. 风险/诚实守门（泄漏/规则风险） | 2 | 先前已过轻量守门（按 TODO 记载）；仍建议在 P3 前再做一次“最近窗口” sanity。 |

**Hard-fail flags（如触发直接 park）：**
- [ ] leakage 迹象
- [ ] post-cost collapse（近期窗口净值明显翻负）
- [ ] 规则不可执行/不可复现

## Run 3 — 硬结论分支（只选 1 个）
选择：**promote_P3（narrow paper pilot）**，但以“最小可回滚接线”方式推进。

### P3 最小 spec（建议写进后续 paper wiring / monitoring）
- 默认参数：`confirm_same_dir_only` + `thr_mult=0.8`
- 适用范围：作为 breakout-short / fib / ema-psar 等策略的 **post-entry 事件确认层**（不单独开仓）。
- 监控阈值（先粗后细）：
  - retention：>= 0.25（低于则视为过度苛刻或环境失效）
  - trades：滚动窗口 >= 30 笔（否则不出强结论）
  - mean_net@6bps：保持 > 0（连续 N 次 refresh 为负则触发降级/停用）

### 下一步（留给下一轮 bot3 / 或人工）
- 在进入实际 paper 接线前，做 1 个 **最近窗口（例如近 90d 或近 30d）** 的轻量 stability sanity（只验证“是否近期翻车”）。

## 本轮产出
- 生成/更新：`reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/report.html`
- 本日志：本文件
