# Rank 230 / return × relative-volume XS momentum survivor follow-up → keep P1 后转 background

- Time: 2026-03-29 02:21 UTC
- Target: `Rank 230 / return × relative-volume XS momentum`
- Action: 做唯一一次 survivor follow-up；直接回答这条线到底能不能以独立对象升到 `P2`，还是只适合作为 plain XS momentum 的增强器 / quality gate
- Verdict: `keep_P1 后转 background`

## 本轮回答的问题
`Rank 230` 留下来的核心是：
1. 一条值得自己进入 `P2 admission` 的独立前排对象；还是
2. 一个对 XS momentum 家族有信息量、但更适合作为 feature / gate 留在背景层的增量件？

## 这轮使用的最小证据
本轮没有重复造一版同维度回测，而是直接把它和当前 runtime 中已经成立的 XS momentum 前排链条对照收口：

1. **`Rank 230` 自己最诚实的 standalone 证据仍不过线**
   - `2026-03-28_0608_return-relvol-xs-momentum-alpha.md` 已给出更长的 `Binance Spot 15m` extended transfer check：
     - 最优 pocket 约 `k=24, maL=48, rebalance=16 bars`
     - gross mean 仅约 `+0.09 bps/bar`
     - 扣 `4 bps` 后 net 约 `-0.09 bps/bar`
     - `final equity` 约 `0.97x`
   - 这说明 `return × relative-volume` 作为 **短周期 standalone raw alpha**，目前并没有留下足够厚的净后 pocket。

2. **系统里已经有更强、也更独立的 XS momentum 前排对象承接“哪个增量件最值得升格”这个问题**
   - `Rank 213 / large-cap XS momentum × short-leg jump veto` 已在更宽 `30` 币 liquid-perp universe 上回答了 plain XS momentum 的 decisive blocker：
     - 问题核心不是“本体完全坏死”，而是 short leg 被 single-name jump concentration 打穿；
     - `jump veto` 相比 `short cap` / `inverse-vol` 给出更明显、也更可冻结的改善；
     - 因此它已进入 `P3 / connected runner live` 链条。
   - 这意味着当前 XS momentum 家族里，**真正已经证明自己能改变前排层级的增量件** 不是 `rel-volume`，而是 `jump-veto`。

3. **`rel-volume` 作为增量件的系统定位，上一轮其实已被别的对象间接回答过一次**
   - `2026-03-28_0820_rank214_survivor_followup_close_to_background.md` 已明确收口：
     - `rel-volume` 当前更像 feature candidate，不够单独升格；
     - `sentiment` 更像 regime gate；
     - 真正值得占用前排资源的增量件是 `jump-veto`。
   - `Rank 230` 虽然比 `Rank 214` 更聚焦于 `return × relative-volume` 本体，但它并没有提供与上述系统结论相冲突的新证据；反而再次确认了：**这条线目前最诚实的位置仍是 feature family，而不是新的独立 `P2`。**

## 本轮真正改变的系统认知
这次 survivor follow-up 把 `Rank 230` 的前排身份彻底收口了：

> **`return × relative-volume` 值得保留，但保留的方式不是把它继续推成独立 admission 对象；而是把它视为 XS momentum baseline 上的一类 participation-aware feature / quality gate 候选。**

翻成人话：
- 它不是没信息；
- 但它现在留下的信息量，主要是“成交参与度偏离可以作为 XS momentum 的补充维度”；
- 还不是“这条线自己已经强到值得占一个 `P2` 前排坑位”。

## 为什么不是 promote_P2
1. standalone 证据没有过最基本的成本生存线；
2. 当前系统里已存在更强且更独立的 XS momentum 增量主线（`Rank 213`）；
3. 若此时仍把 `Rank 230` 升到 `P2`，就会把一个 **尚未证明自己能独立抬升 baseline** 的 feature family，当成与已成立前排对象并列的新 admission 目标，属于低杠杆重复，而不是新增系统认知。

## 为什么不是直接 P0
- 它留下来的不是空话，而是一条明确可复用的特征骨架：`return × relative-volume`；
- 后续若 desk 要给 plain XS momentum 接 participation-aware 增量层，这条线仍是可直接复用的背景素材；
- 因此最诚实的落点不是“无价值丢弃”，而是 **`keep_P1` 的知识价值保留，但退出前排资源竞争。**

## Runtime implication
- `Rank 230` 的唯一 survivor follow-up 已用完，不再占用 `Surviving candidate slot`。
- 当前系统对这条线的正式定位应收口为：
  - `standalone alpha`: 当前不成立；
  - `feature family / quality gate candidate`: 成立；
  - `front-slot priority`: 结束，转入 `Background pool`。

## Result sentence
`Rank 230 / return × relative-volume XS momentum` 的唯一 survivor follow-up 已诚实收口：更长样本下它作为 standalone short-cycle alpha 仍过不了成本生存线，而当前 XS momentum 家族里真正已证明能改变前排层级的增量件是 `jump-veto`、不是 `rel-volume`，因此本轮应将其写成 `keep_P1 后转 background`，保留为 participation-aware feature family，而不升 `P2`。