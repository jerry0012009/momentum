# 2026-03-20 23:59 UTC — Rank 133 clean replication硬结论（park）+ Run3切换 Rank 134 fresh intake

## 本轮先做的桌面检查（按 TRADING DESK BOARD）
- `git status --short`：存在大量与本轮无关脏文件，继续 **不混提**。
- 先执行 `Run 1 / EMA due-check`：
  - `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
  - 结果：仍无 `due-now / overdue`；`Crypto 1d+1wk` 仅 `due_soon`（约 2 分钟）。
  - 结论：`Paper Seat = waiting_not_due`，本轮必须切 `Scout Seat`，不得空转。

## 本轮主点：完成 Run3 条件分支（基于 Rank 133 clean replication 结果）
### 1) Rank 133 最小 clean replication 结果（已产出并复核）
本轮复核 artifact：
- `reports/artifacts/scout_rank133_triple_barrier_honest_final_verdict_15m/summary.json`
- `.../overall_summary.csv`
- `.../scout_promotion_scorecard.csv`

关键数（6 bps / side）：
- train：`return_delta = -5.05 bps`
- test：`return_delta = -9.77 bps`
- 参数稳定性：测试段 `0/36` 组合跑赢 fixed 8-bar baseline
- 跨标的稳定性：测试段 `0/3` 资产为正
- scorecard：时间/参数/跨标的/成本-交易数四项均 `0`，hard-fail flags 包含 `train_test_both_negative, no_positive_test_config, zero_positive_assets`

**硬结论：`Rank 133 = park`**（不是 keep_P1，不升格）。

### 2) Active Scout 边际价值比较（3.5）
在 `EMA waiting_not_due` 前提下，本轮比较：
- `Rank 127 / 125 / 112 / 111`：都属于旧 `P1 budget-used / evidence_pool`；继续磨的边际价值偏低。
- `Rank 122/2/17/29/32b`：`P3 hosted continuity`，本轮无 status-changing event，不占主位。
- `tiny-live plumbing`：仍是 fallback，不应先于 fresh intake。

因此按 `Run 3` 规则：`Rank 133 hard-fail` 后先回 **fresh intake**。

## 本轮紧邻子点：新增 Rank 134（fresh intake）
新增 queue-facing + reader-facing：
1. `reports/artifacts/literature/scout_rank134_cross_market_intraday_tsmom_leadlag_source_intake_card.csv`
2. `reports/site/reading/repo_scout/rank134_cross_market_intraday_tsmom_leadlag_source_intake.html`

Rank 134 定义（简）：
- 方向：`cross-market intraday TSMOM lead-lag gate`
- 来源：`Xu, Li, Singh, Li (SSRN 4651331) + RECENT_PAPER_SEEDS`
- 定位：`paper-based 15m crypto gate`（冻结既有 entry，只做 lead-lag 分层）
- 守门：`trade on/off` 与 `no-leakage` 已写清；当前结论 = `guard-passed / admit_to_clean_replication_queue`

## 对 desk board 的最小 write-back
已更新 `docs/TODO.md` 顶部：
- `Scout Seat 主点` 切到 `Rank 134`。
- `Active Scout 排序`：`Rank 134` 置顶，`Rank 133` 下放至 `P0 park`。
- `Next 3 runs`：改为 `Run 2 = Rank 134 source intake+honesty gate`，`Run 3 = Rank 134 最小 clean replication / 否则继续 fresh intake`。
- `最近关键 evidence`：补入 `Rank 133 hard-fail park` 与 `Rank 134 fresh intake`。

## 本轮硬结论
1. **`Rank 133 / triple barrier honest final-verdict layer = park`**。
2. 在 `EMA waiting_not_due` 下，本轮合法下一顺位已切到 **`Rank 134 fresh intake`**，并完成最小入队产物。

## 下一轮建议（保持单主点）
- 若 `EMA` 仍非 due-now：给 `Rank 134` **1 次最小 clean replication**（BTC/ETH/SOL 15m，next-bar open，no-overlap，6/10/15bps），直接输出 `keep_P1 / promote_P2 / park`。

## commit
- 未提交（工作区存在大量与本轮无关脏文件，按规则不混提）。
