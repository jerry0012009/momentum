NO_PROGRESS: EMA 距下一次 A 股日频真实 close 仍约 4 小时，breakout heavy rerun 仍在 6 小时冷却窗口；这轮若硬做只会伪造 refresh 或重复旧样本 rerun。

# 2026-03-16 03:02 UTC｜NO_PROGRESS：等 EMA 真 close，breakout 先过 rerun cooldown

## 为什么这次没有形成有效推进
- 先检查了 `git status --short`、`docs/TODO.md`、`docs/AUTO_OPTIMIZATION_LOOP.md` 和最近几轮 optimization logs。
- 当前 deployment-first 排序没有变化：`EMA baseline family = closest to paper`、`support_breakout_v0 = one_more_gate`、`Fibonacci = archived / optional`。
- 与三条收口线直接相关、且最接近 deployment 的未完成主线，仍是 `EMA` 沿同一张 live ledger 继续落下下一轮真实 `market-close refresh / week-1 review`；但这一步必须等新的 completed daily bar 到来，不能伪造 refresh。
- breakout 线虽然本地 cache 继续前推，但最近一次 heavy refresh recheck 刚在 `2026-03-15 23:25 UTC` 完成；当前再跑同类 rerun，只会撞上刚建立的 cooldown 守门，而不会新增 overturn `one_more_gate` 的更硬证据。

## 这轮实际核查了什么
1. 真跑一次 EMA 守门入口：
   - `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
2. 重跑一次 breakout 的 rerun guard：
   - `python3 scripts/build_breakout_revisit_guard.py`
3. 复核当前 guard / artifact 是否允许形成新的 deployment-facing 小步，而不是重复 waiting-window 补丁。

## 验证 / 证据
### EMA 守门结果
- `--require-due` 返回 `code 2`，这是预期的守门信号，不是脚本故障。
- 当前仍没有 `due-now / overdue` lane。
- 最靠前的 A 股日频 lane 现在仍是：
  - `创业板ETF 1d`
  - `贵州茅台 1d+1wk`
  - `沪深300ETF 1d`
- 它们距下一次真实 close 仍约 **4.0 小时**；脚本明确提示：继续 `waiting_next_close`，不要补伪 refresh。

### breakout rerun guard 结果
- `reports/artifacts/support_breakout_v0_h24/avoid_fluctuating_revisit_guard_20bps.csv` 已刷新到 `2026-03-16 03:02:17 UTC`。
- 当前 guard 固定读法：
  - `hours_since_last_heavy_recheck = 3.6h`
  - `rerun_cooldown_hours = 6`
  - `rerun_cooldown_active = yes`
  - `current_cache_latest_bar_utc = 2026-03-16 02:00:00 UTC`
  - `revisit_guard_verdict = cache_advanced_but_recent_recheck_cooldown_hold`
- 这说明 breakout 现在不是“没数据可看”，而是**cache 已前推，但 heavy rerun 刚做完不久，当前更诚实动作仍是 cooldown hold**。

### 为什么这轮不该硬做别的
- EMA 线若继续补 queue / board / source 文案，已经违反当前 steering：source-risk 已清零，下一刀必须等真实 close 后沿 ledger 续写，而不是再补近义部署页面。
- breakout 线若继续 rerun 或回切旧样本，也违反当前 steering：现在默认只接受可能 overturn scope verdict 的证据，而 cooldown 窗口里重复 heavy rerun 既不新增 `pure down coverage`，也不新增 `pre-down bridge coverage`。

## 风险 / 边界
- 这不是系统错误，而是当前最诚实的 deployment-facing 动作就是：**不伪造不存在的新 completed-bar 结果，也不在 breakout 冷却窗口里重复重跑同类 heavy rerun。**
- `git status --short` 显示 worktree 存在大量与本轮无关的既有脏改 / 未跟踪文件；这只是环境观测，不是失败条件，本轮没有把无关改动混进来。

## 下一步建议
1. 下一轮最优先触发点仍是 **A 股日频真实 close 到点**。
2. 到点后默认先跑：
   - `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
3. breakout 线则等当前 `6h cooldown` 走完；若届时 cache 仍领先，再做**一次** heavy rerun 检查即可。
4. 在新的 `pure-test / down-tail` 命中出现前，breakout 继续维持 `one_more_gate / up-flat biased conditional alpha`，不要 reopen 当前旧样本 retrospective slicing。

## Commit hash
- HEAD：`b5a1c26`
- 本轮未提交。

## 未提交原因
- 本轮只新增这份 `NO_PROGRESS` 记录，并轻量刷新了 breakout guard；同时工作区存在大量与本轮无关的既有脏改 / 未跟踪文件，不适合做没有必要的 selective commit。