# EMA runbook 落地（paper/shadow 最小操作规则）

- 时间：2026-03-15 12:29 UTC
- 主点：`EMA / PSAR raw alpha focus`
- 紧邻子点：`alpha_closure_board` 同步口径更新（仅围绕 runbook 完成态）

## 1) 为什么本轮做这个

根据当前 steering：`EMA baseline family` 是最接近 paper 的对象，但缺 runbook。`docs/TODO.md` 里 deployment-facing 的剩余硬门槛中，最直接、且可在单轮完整交付的是：

- 把 `candidate spec / operating spec / monitoring board` 真接成 `paper-trading runbook`，并写清：
  - 数据源
  - 刷新频率
  - 记账口径
  - promote/demote
  - kill switch / rollback

这比继续补近义 board 文案更接近真实 `paper/shadow` 启动前置条件。

## 2) 本轮改动

### A. 代码与产物生成逻辑（EMA 主报告）

文件：`scripts/build_ema_psar_raw_alpha_report.py`

1. 新增函数：`build_ema_paper_trading_runbook(...)`
   - 输入：candidate / operating / monitoring / recent-forward audit
   - 输出：`ema_paper_trading_runbook.csv`
   - 覆盖 8 个 deployment scope（primary / secondary / shadow / stoplist）
   - 每行明确：
     - `data_source`
     - `refresh_frequency`
     - `ledger_rule`
     - `review_rule`
     - `promote_or_demote_rule`
     - `kill_switch_or_rollback`

2. 在 `main()` 中接入 runbook 构建与落盘：
   - 新增 artifact：`reports/artifacts/ema_psar_raw_alpha/ema_paper_trading_runbook.csv`

3. 页面表达新增 Q28（替换旧的 60m gate 解释卡位）：
   - 标题：`如果今天真要把 EMA baseline 从 board 接成一版可执行 runbook...`
   - 落地 runbook 表格（10 列）
   - 在“相关产物”列表加入 `ema_paper_trading_runbook.csv`

### B. TODO 勾选（deployment-facing 任务完成）

文件：`docs/TODO.md`

将以下条目标记为已完成并补结果说明：

- `[x] EMA：把 candidate spec / operating spec / monitoring board 真正接成 paper-trading runbook`

并补充：
- 新增 Q28 与 runbook artifact
- `active_secondary_backstop` 降级触发规则已写死（单 pocket 连续转红或被更严格 honesty 打回 mixed/watch -> 降回 shadow）

### C. closure 页面口径同步（避免“已做完但首页仍说未做”）

文件：`scripts/build_alpha_closure_board_report.py`

只做 runbook 完成态相关同步：
- EMA 状态从 `candidate/operating/monitoring` 升级为 `+ runbook`
- Step 3 文案改为“EMA 已补齐 runbook，下一步应启动真实 shadow/paper 记账”
- summary/track 的 next step 改为“按 runbook 启动 0 真资金账本”

## 3) 生成与最小验证

执行：

1. `python3 -m py_compile`（3 个脚本）通过。
2. 重新生成页面与产物：
   - `python3 scripts/build_ema_psar_raw_alpha_report.py`
   - `python3 scripts/build_alpha_closure_board_report.py`
   - `python3 scripts/build_plans_site.py`
3. 关键结果检查：
   - `reports/site/factors/ema_psar_raw_alpha/report.html` 已出现 Q28 runbook 卡片与 runbook 表格。
   - `reports/artifacts/ema_psar_raw_alpha/ema_paper_trading_runbook.csv` 已生成。
   - `docs/TODO.md` 对应 runbook 条目已 `[x]`。
   - `reports/site/factors/alpha_closure_board/report.html` 已同步“EMA 已有 runbook”的项目级表达。

备注：构图时出现 matplotlib CJK 字形 warning（已有历史现象），不影响 CSV/HTML 产物生成。

## 4) Git hygiene / 提交说明

- 本轮开始前工作区已存在大量与本轮无关的脏改动（含多个报告线与 artifacts）。
- 本轮未做提交，避免把无关脏文件混入。
- 若后续需要提交，建议只对本轮相关文件做 selective commit。

## 5) 本轮结论（对 deployment 的实际帮助）

EMA 线从“closest to paper 的静态描述”推进到“可执行 runbook”：
- 现在不只知道谁是 active/shadow/stoplist，
- 还知道每天/每周该怎么更新、怎么记账、何时升降级、何时立即停/回滚。

这为下一步启动 `0` 真资金的真实 `paper/shadow` 记账提供了直接入口。