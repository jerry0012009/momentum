# Bot3 执行日志（Rank 405 P2 exit decision）

- 时间：2026-04-14 21:10 UTC
- 执行动作：`cycle_plan` 小点 1（P2 admission 决策轮）
- 对象：`Rank 405 / multienvelope overshoot average-return shell (15m wall-clock scaled lane)`
- 结论：`promote_P3`（进入 `Paper launch queue`，待下一步 wiring）

## 本轮最小 honesty/execution 检查（仅 1 个）

按小点要求补做联合检查：**next-bar 可执行价对齐 + 单笔容量上限**。

口径：
- 市场：Binance USDⓈ-M `BTCUSDT/ETHUSDT`，`15m`
- 样本：`2025-10-01 ~ 2026-04-14`
- 非泄漏：`rolling(avg_period).mean().shift(1)`
- 执行：触发于 bar `t`，统一在 `t+1 open` 执行开/平（含加仓）
- 成本：开仓 `2 bps` + 平仓 `5 bps`
- 容量上限检查：将每笔最多可成交层数从 `3` 层压到 `2` 层（`cap2`）

产物：
- `reports/artifacts/optimization_loop/rank405_p2_exit_nextbar_capacity_summary.csv`
- `reports/artifacts/optimization_loop/rank405_p2_exit_nextbar_capacity_detail.csv`

## 关键结果

来自 `rank405_p2_exit_nextbar_capacity_summary.csv`：

- `nextbar_cap3_baseline`：
  - BTC：`+12.1275 bps/trade`（24 笔）
  - ETH：`+5.8054 bps/trade`（79 笔）
  - 合并：`+7.2785 bps/trade`（103 笔）

- `nextbar_cap2`（容量上限更严）：
  - BTC：`+1.6489 bps/trade`
  - ETH：`-0.4148 bps/trade`
  - 合并：`+0.0661 bps/trade`

## 判定

在更诚实的 `next-bar open` 执行口径且施加 `单笔最多 2 层` 的容量上限后，合并口径仍为微正（`+0.0661 bps/trade`），未出现可将对象直接打回 `P0/P1` 的**唯一 decisive honesty/execution blocker**；本轮按出口决策将 `Rank 405` 从 `Active P2` 直接升级到 `P3 / Paper launch queue`。

## 尾部执行备注

- 首页刷新脚本与邮件通知按 policy 在本轮末尾独立执行；若任一步失败，按“非阻断尾部失败”处理，不回滚本轮 verdict/state/log。
