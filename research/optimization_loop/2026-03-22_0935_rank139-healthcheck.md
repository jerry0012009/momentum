# 2026-03-22 09:35 UTC · Rank 139 hosted narrow paper pilot 健康检查

## 本轮按板执行
- **Run 1 / EMA due-check**：读取 `reports/artifacts/ema_psar_raw_alpha/ema_paper_trading_due_guardrail_snapshot.csv`，当前全 desk 仍无 `due-now / overdue`。
  - `Crypto 1d+1wk`：下次 `2026-03-23 00:00 UTC`，`waiting_not_due`
  - `创业板ETF 1d`：下次 `2026-03-23 07:00 UTC`，`waiting_not_due`
  - `贵州茅台 1d+1wk`：下次 `2026-03-23 07:00 UTC`，`waiting_not_due`
  - `沪深300ETF 1d`：下次 `2026-03-23 07:00 UTC`，`waiting_not_due`
  - `美股 1d+1wk`：下次 `2026-03-23 20:00 UTC`，`waiting_not_due`
- 因此本轮**不得空转**，按顶板切到 **Run 2 / Rank 139(P3) hosted narrow paper pilot 低频健康检查**。

## 本轮只做 1 件事：Rank 139 健康检查

### 检查对象
- 监控板：`reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
- refresh 时钟：`reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_refresh_clock.json`
- 当前 policy：`thr_mult=0.8` + `confirm_same_dir_only`

### 结果
1. **ops 快照在持续更新**
   - `narrow_paper_pilot_refresh_clock.json` 的 `generated_at_utc = 2026-03-22 09:02 UTC`
   - 相对本轮执行时间 `09:35 UTC`，说明最新快照约 **33 分钟前**，仍在低频可见性窗口内；没有出现“页面/CSV 长时间不更新”的失联迹象。

2. **没有看到 desk 级爆雷，但确实存在需要盯的 retention / timeout 结构性特征**
   - 跨 9 个 asset×setup 口径，`retention` 区间约 **11.8% ~ 50.0%**，总体符合这类 confirm-only gate 会明显砍单的预期，不算“突然塌穿”。
   - `no_event_timeout_rate` 区间约 **5.6% ~ 27.8%**，当前**没有单腿高到失控**（例如 40%~50% 以上那种明显异常）。
   - 但 `fib_retest_long` 两条腿（BTC / ETH）和 `breakout_short` 的 ETH 腿 timeout 偏高，说明这套 gate 目前更像“保守筛选层”，不是高保留率通用层。

3. **当前最值得盯的相邻子点：BTC `ema_psar_long` retention 偏薄**
   - `BTC-USD / ema_psar_long`：
     - `base_trades=17`
     - `kept_trades=2`
     - `retention=11.8%`
     - `no_event_timeout_rate=11.8%`
     - `opp_dir_first_rate=76.5%`
   - 这不是“timeout 爆雷”，更像**opp_dir_first 明显占优，导致 confirm-only 几乎把 BTC EMA 腿砍成很薄样本**。
   - 目前仍可继续作为 hosted narrow paper pilot 观察，但如果后续几轮这个腿持续低 retention，应该优先怀疑：
     - 这条 gate 只适合更窄的 setup 子集，而不是当前三条 archetype 一起托管；
     - 或者应把监控重点放到 `BTC ema_psar_long` 的“是否长期只剩 1~2 笔保留单”而不是泛看均值。

## 本轮结论
- **Hard call**：`Rank 139` 当前 hosted narrow paper pilot **继续维持**，无须回退成研究态，也没有出现“no_event_timeout 异常偏高 / retention 异常崩塌”的硬爆雷。
- **但要盯的紧邻子点**：`BTC / ema_psar_long` retention 仅 `11.8%`，已经接近“可见但偏薄”的边缘，后续若连续几轮仍维持这种极低保留率，应优先把它当成 scoped-lane 风险，而不是继续用总表稀释掉。

## 本轮产物
- 日志：`research/optimization_loop/2026-03-22_0935_rank139-healthcheck.md`
- 本轮不打开新 Scout 候选，不做 Run 3。
