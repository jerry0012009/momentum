# 2026-03-23 16:48 UTC · Rank 151 runner seed（Paper launch / Run 1）

## 本轮路径判断
- 顶板读取结果：本轮走 **`Paper launch`**。
- 原因：`TRADING DESK BOARD` 已把 `Rank 151 / EWMAC breakout band-pass gate` 放进 **`Paper / 待开启自动运行（launch queue）`**，且 `Next 3 bot3 runs` 的第 1 手明确是：**给 `Rank 151` build runner**。
- 因此本轮不再继续做 Scout 研究，而是兑现 launch queue 的第 1 轮落地预算。

## 本轮主点
给 `Rank 151` 补一个**可执行、可验证、可交付**的最小独立 runner 骨架：
- 能 `init-from-now`
- 能 `refresh`
- 能写 `ledger / status / state / last_run_summary`
- 明确标注当前只是 **`runner seed`**，底层仍基于 frozen honest-gate digest，而不是伪装成已接 live raw-bar execution

## 本轮交付
### 1) 新增独立 runner
新增：
- `scripts/run_rank151_breakout_bandpass_paper_runner.py`

它当前的定位是：
- 专用于 `Rank 151 / breakout-short / band_pass`
- 输入源固定为：
  - `reports/artifacts/quant_digests/ewmac_breakout_alignment_20260323/event_table.csv`
  - `reports/artifacts/quant_digests/ewmac_breakout_alignment_20260323/thresholds.json`
- desk rule 冻结为：
  - `event_side = -1`
  - `q20 < align_score <= q80`
  - `cost = 6bps/side`
  - `variant = band_pass`
- 输出到：
  - `reports/artifacts/paper_rank151_breakout_bandpass_gate/rank151_paper_closed_trades.csv`
  - `reports/artifacts/paper_rank151_breakout_bandpass_gate/rank151_paper_status.csv`
  - `reports/artifacts/paper_rank151_breakout_bandpass_gate/rank151_paper_state.json`
  - `reports/artifacts/paper_rank151_breakout_bandpass_gate/rank151_paper_last_run_summary.json`

### 2) 运行模式写死为“诚实 seed”
这轮刻意没有假装已经接到实时行情或 broker：
- `venue_mode = paper_digest_seed`
- `runner_mode = frozen_digest_runner_seed`
- `note` 明确写入：
  - 当前只是在为下一轮 `scheduler + status page` 铺管线
  - 还不是 raw-bar live recomputation / live order paper runner

这保证第 1 轮是真的在做 launch plumbing，而不是把研究页硬包装成“已经自动运行”。

## 验证
已执行：
```bash
python3 -m py_compile scripts/run_rank151_breakout_bandpass_paper_runner.py
python3 scripts/run_rank151_breakout_bandpass_paper_runner.py --init-from-now --force-reinit
python3 scripts/run_rank151_breakout_bandpass_paper_runner.py --refresh
```

验证结果：
- 脚本可成功编译
- `init-from-now` 成功
- `refresh` 成功
- 状态文件成功生成：
  - `reports/artifacts/paper_rank151_breakout_bandpass_gate/rank151_paper_status.csv`
- 最近一次 summary 成功生成：
  - `reports/artifacts/paper_rank151_breakout_bandpass_gate/rank151_paper_last_run_summary.json`

## 关键读数（当前 seed 快照）
来自 `rank151_paper_status.csv`：
- `closed_trades = 1033`
- `asset_coverage = 3`
- `sample_start_utc = 2025-09-19T12:45:00Z`
- `sample_end_utc = 2026-03-21T23:45:00Z`
- `mean_net_bps ≈ 5.55`
- `win_rate ≈ 43.27%`
- `lifetime_total_return_6bps ≈ 64.39%`
- `new_trades_appended = 0`（符合预期，因为当前底层输入还是 frozen digest）

## 为什么这轮算“最有杠杆的小步”
因为 `Rank 151` 已经通过 admission-bar，继续补研究解释的边际价值很低；真正卡住 launch queue 的，是**没有独立 runner 入口**。这轮直接把最小 runner 接出来，等于把下一轮要做的：
- host-side 调度
- due/status 可见性
- 首页挂链

都变成了有真实文件可接的工程动作，而不再停留在口头 planning。

## 边界 / 风险
1. **这不是 live runner。** 当前仍只基于 frozen digest 事件表，不能误读成已经接入 raw bars / broker paper。
2. 因为底层输入是 frozen digest，所以本轮 `refresh` 没有新增 trades 是正常现象，不是 runner 异常。
3. 若下一轮要把它提升到“正在自动运行”的真实口径，至少还需要：
   - 调度入口
   - 统一 status 可见性
   - 首页入口 / handoff 文档

## 建议的下一步（对应顶板 Run 2）
直接进入：
- **`attach scheduler + status page`**

最小目标：
1. 给 `run_rank151_breakout_bandpass_paper_runner.py --refresh` 接 host-side scheduler
2. 产出一个 launch-facing status 页面/汇总入口
3. 把 `paper_rank151_breakout_bandpass_gate/*` 挂到首页，做到“可自动跑、可看状态、可被 bot2 判健康”

## 提交状态
- 本轮未做 git commit。
- 原因：工作区仍存在大量与本轮无关的既有脏改动；本轮只做最小 runner 文件与 artifact 写入，避免混提。

## 一句话结论
**Rank 151 已从“通过 launch admission-bar 的研究对象”推进到“有独立 runner seed 的 Paper launch 对象”；下一轮可以直接接 scheduler + status page。**
