# 2026-04-09 17:05 UTC — Rank 28 fresh intake first verdict

## 本轮执行对象
- target: `research/park_reframe/2026-04-08_0019_rank28-park-reframe.md`
- action: 作为当前 `cycle_plan` 最前的 pending fresh intake，判断 `Rank 28` 当前保留的 soft residual——`cross-market leader impulse`——是否还能在不偷换宿主的前提下，压成一个比既有 `Rank 28b / lead-lag raw-alpha family` 更独立的新对象。

## 本轮读到的关键证据
1. `research/park_reframe/2026-04-08_0019_rank28-park-reframe.md`
   - 已明确写出：原 `Rank 28` 真正被 park 的原因不是“cross-market 信息不存在”，而是旧的 `15m direct leader-laggard` queue-facing 写法在 clean replication 中成本后直接失败；
   - 该文还已给出最关键结论：没有比既有 `Rank 28b` 更诚实的新一刀，不值得再派生 `Rank 28c`。
2. `research/optimization_loop/2026-03-17_0841_rank28-crossmarket-clean-replication.md`
   - 原 Rank 28 的 direct lag-trade clean replication 已被明确否过，因此本轮不能把旧宿主重新包装成“只是还差一点的 fresh pocket”。
3. 近几天相关新证据检索
   - `2026-04-06_0558_btc-lead-liquidity-lag-alt-alpha.md`
   - `2026-04-07_1436_majorlead-closeslot-crossmarket-itsm-alpha.md`
   - `2026-04-07_1748_binance-okx-spot-leadlag-catchup-alpha.md`
   - 这些新证据共同指向的是更快、更窄、更事件化的新宿主：`BTC->alt lag`、`session-handoff continuation`、`same-underlier cross-venue catch-up`；它们强化了“leader impulse 主题没死”，但并没有把旧 `Rank 28` 留下一个仍归属于原宿主、且不被现有家族吸收的独立 residual pocket。

## 本轮判断
结论：**first verdict = `background / P0`。**

更具体地说：
- 原 `Rank 28` 作为 `15m direct cross-market lag-trade` 已经被 clean replication 否掉；
- 既有 `Rank 28b` 已经吃掉了原宿主里最自然、仍能诚实保留的 queue-facing residual；
- 本轮所谓 `cross-market leader impulse` 残余，继续拆下去只会外流成新的 raw-alpha 宿主（如 `BTC->alt lag`、`same-underlier cross-venue catch-up`、`session-handoff continuation`），而不是形成仍归属于旧 `Rank 28` 的新 pocket；
- 因此当前最诚实的收口不是 `keep_P1`，而是直接承认：旧 Rank 28 的剩余信息已被既有 `Rank 28b` 与更新、更具体的新 lead-lag family 消耗掉，不再值得继续以前排 fresh intake 身份占用预算。

## 为什么不是 keep_P1
`keep_P1` 需要它已经形成一个不被现有 family 吸收、且下一步值得给唯一 follow-up 的独立对象。本轮没有看到：
- 不是新的 raw alpha；
- 不是新的 execution shell；
- 也不是旧 Rank 28 下还能自洽存在的单一 honesty blocker。

真正发生的是：主题仍在，但宿主已经迁移。旧 `Rank 28` 本体不该继续假装拥有一个可排队的新 derived pocket。

## 对 runtime 的影响
- 本轮 verdict 为 `background / P0`，因此**不分配新的正式整数 Rank**；
- 不触发 survivor / P2 / P3 迁移；
- fresh intake 这一步已诚实收口，只更新当前小点结果与相关 runtime truth。

## 一句话 result
`Rank 28` 的 `cross-market leader impulse` residual 并没有压成比既有 `Rank 28b` 更独立的新对象；新增线索都已外流到 `BTC->alt lag / same-underlier cross-venue catch-up / session-handoff continuation` 等新宿主，因此本轮 fresh intake first verdict 直接收口为 `background / P0`。