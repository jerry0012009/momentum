# bot3 optimization loop — 2026-03-22 11:42 UTC — ema-waiting_rank139-health_pbo-cscv-offline-scorecard

> 约束：严格按 `docs/TODO.md` 顶部 TRADING DESK BOARD 的 **Next 3 bot3 runs**（Run1→Run2→Run3）；本轮只做 **1 个主点 + 1 个紧邻子点**；不同时打开多个 scout 候选。

## Run 1 — EMA due-check first（Paper Seat）

- 执行：`python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
- 结果：脚本返回 **waiting_not_due**（无 `due-now / overdue` lane）
  - 最靠前：`Crypto 1d+1wk（BTC/ETH/SOL）` 距离 next close 约 **12.2h**
  - 其次：`创业板ETF 1d` / `贵州茅台 1d+1wk` 距离 next close 约 **19.2h**

结论：Run1 不做伪 refresh；按规则立即切到下一允许动作（Scout Seat）。

## Run 2 — Rank 139（P3）hosted narrow paper pilot：低频健康检查（只做 1 件事）

目标：确认 ops/CSV 仍在更新，并粗扫是否出现“爆雷级”信号（`no_event_timeout` 异常偏高 / retention 崩塌）。

- 观察对象：
  - `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_refresh_clock.json`
  - `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
- 最新 refresh clock：`generated_at_utc = 2026-03-22 11:16 UTC`（本轮检查时仍属近期更新）
- `no_event_timeout_rate`（baseline 口径）大致在 **5.6% ~ 27.8%** 区间（未见“单格爆到不可解释”的异常）
- retention 仍在可解释范围内（不同 setup 有差异，但未出现全盘崩塌）

结论：Rank139 hosted pilot 仍在可持续更新；按 desk 口径继续低频健康检查即可，不抢占更多主资源位。

## Run 3 — pbo-cscv honesty gate：canonical 离线实现 / 可落地产物（本轮主交付）

> 由于 **source intake 已在更早轮次完成**（见 `research/optimization_loop/2026-03-22_1041_pbo-cscv-source-intake.md`），本轮选择另一条合法交付：跑通一份 **offline canonical-ish CSCV/PBO + DSR scorecard**，并把结果落到 artifacts。

- 执行：
  - `python3 scripts/build_pbo_cscv_dsr_scorecard.py \
    --trade-log reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/trade_log.csv \
    --event-col event_0.8 \
    --out-dir reports/artifacts/pbo_cscv_honesty_gate \
    --label "Rank139 thr=0.8 offline honesty gate"`
- 产出：
  - `reports/artifacts/pbo_cscv_honesty_gate/rank139_pbo_cscv_dsr_scorecard.csv`
  - `reports/artifacts/pbo_cscv_honesty_gate/rank139_pbo_cscv_dsr_meta.json`

关键读数（按当前 trade_log）：
- PBO（由 CSCV lambda 阈值统计得到的简化版本）：`pbo ≈ 0.1714` → **guard_passed**
- `confirm_same_dir_only`：`mean_net@6bps ≈ +0.537%`、`sharpe ≈ 0.365`、`dsr_probability ≈ 0.881`
- `veto_opp_dir`：`mean_net@6bps ≈ +0.343%`、`sharpe ≈ 0.268`、`dsr_probability ≈ 0.839`
- baseline：`mean_net@6bps ≈ -0.155%`、`dsr_probability ≈ 0.0036`

结论：这份最小离线 scorecard 至少回答了“在多臂筛选语境下，Rank139 的 confirm/veto 改善是否仍更可信”的问题；可作为后续把 `pbo-cscv` 从 proxy demo 推到 desk 可用 honesty-layer 的第一块可审计砖。
