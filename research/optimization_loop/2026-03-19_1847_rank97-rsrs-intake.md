# 2026-03-19 18:47 UTC — Rank 97 / RSRS right-skew shared veto+sizing overlay intake

## 为什么这轮是它
- 先实际执行了 `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`。
- 脚本继续返回 **`waiting_not_due`**：当前没有 `due-now / overdue` 的 EMA lane；最近 due 约为 `美股 1.2h`、`Crypto 5.2h`、`A股 12.2h`。
- `reports/artifacts/manual_narrow_paper_lanes/manual_narrow_paper_last_run_summary.json` 最新仍是 `new_closed_trades_appended=0`，没有新的 `P3 status-changing event` 可以挤掉 Scout。
- 按 `docs/TODO.md` 顶部 `TRADING DESK BOARD` 当前 `Next 3 bot3 runs`，本轮合法主动作就是按 `7.10` 从 `quant_digests / RECENT_PAPER_SEEDS / validated shortlist` 里再认领 **1 条** 新的 `5m / 15m paper-repo source intake`。

## 本轮主点 + 紧邻子点
- **主点**：完成 `Rank 97 / RSRS right-skew shared veto + sizing overlay` 的 `source intake + 两条轻量诚实守门`
- **紧邻子点**：把 verdict、active Scout 顺序、下一轮 `Next 3` 最小写回到 `TRADING DESK BOARD`

## 本轮先比较 active Scout 候选边际价值（3.5）
当前允许动作按顶板应读成：
1. `Rank 97 / RSRS right-skew shared veto+sizing overlay`
2. `Fib placebo-zone honesty gate`
3. `CLV asymmetric admission layer reserve`
4. `Rank 93 / Rank 90 / Rank 91 / Rank 82 / Rank 80 / Rank 81 evidence_pool`
5. `Rank 96 / Rank 95 / Rank 92 / Rank 94 park`
6. `P3 continuity`
7. `tiny-live plumbing`

把 `Rank 97` 排第一，不是为了扩框架，而是因为：
- 它是 **fresh repo source**，而不是预算已用的旧 evidence pool；
- 它直接服务当前三条主线共同缺的一个执行问题：**支撑阻力强度更像 veto / sizing overlay，还是该被硬写成二元入场键**；
- 相比 `Fib placebo-zone honesty gate`（更像校准 Fib 叙事）与 `CLV reserve`（旧 fresh repo），它更快进入最小 clean replication，并更贴当前 desk 的 shared overlay 主线。

## 本轮 intake 结论
### 1) trade on / trade off 冻结
- **trade on**：只把它降级成 `shared veto + sizing overlay`，不是新的独立 alpha。
  - 先在固定 rolling window 上计算 `rsrs_slope / z-score / R² / modified_score / right_skewed_standard_score`；
  - 首轮只允许把它接成三条现有 base setup 的风险覆盖层，例如：
    - `RSRS_right > q70`：禁止新增 short 或 short 半仓；
    - `RSRS_right < q30`：禁止新增 long 或 long 半仓；
    - 或映射为 `low / mid / high` 三档仓位，而不是硬做统一入场键。
- **trade off**：若要靠未来分位、同根收盘后的后验切换、或事后重算平滑值，才能知道某根 bar 是否该 veto / size-down，就不得把它写成有效 gate。
  - 当前 15m proxy 已提示 `right-skew` 在短周期分布明显偏正，不能把它误包装成多空对称的 shared trigger；
  - 也不能继续把它写成“支撑阻力过/不过”这种二元神谕。

### 2) 轻量诚实守门
- **规则能清楚写成 trade on / trade off**：通过。
- **没有明显 lookahead / repaint / data leakage**：通过，但前提是 desk 迁移统一冻结到：
  - `signal 当根及之前数据 + next-bar open + no-overlap`
  - `n/m` 窗口、`slope / z-score / R² / right-skew` 以及 `q30 / q70` 分位都只用 signal 当根及之前已完成 bars 计算；
  - 不得把未来分位、后验 path 或事后平滑倒灌回 overlay。

## hard verdict
**`Rank 97 = guard-passed / admit_to_clean_replication_queue`**

更直白地说：
- 这条线当前最诚实的读法不是“新的 shared trigger alpha”；
- 而是 **值得做 1 次最小 clean replication 的 shared veto + sizing overlay**；
- 如果最小 clean replication 不能同时改善 `post-cost return / max drawdown / turnover`，就该快速压回 `park / evidence pool`，而不是继续靠新名词续命。

## 本轮产物
### queue-facing artifact
- `reports/artifacts/literature/scout_rank97_rsrs_right_skew_source_intake_card.csv`

### reader-facing 落点
- `reports/site/reading/quant_digests/2026-03-19_1844_rsrs-right-skew-shared-gate.html`
- `docs/TODO.md` 顶部 `TRADING DESK BOARD` 已同步写回 `Rank 97`

## desk board 写回
已把 `docs/TODO.md` 顶部 `TRADING DESK BOARD` 最小刷新为：
- `Rank 97 / RSRS right-skew shared veto + sizing overlay = P1 weak candidate（guard-passed / admit_to_clean_replication_queue）`
- active Scout 顺序：`Rank 97 > Fib placebo-zone honesty gate > CLV reserve > Rank 93/90/91/82/80/81 evidence_pool > Rank 96/95/92/94 park > P3 continuity > tiny-live plumbing`
- `Next 3 bot3 runs`：
  1. `Run 1 = EMA due-check only`
  2. `Run 2 = 若 EMA 仍 waiting_not_due，则给 Rank 97 1 次最小 clean replication`
  3. `Run 3 = 若 Rank 97 clean replication 仍存活，则只给 1 个 truly verdict-changing 的 Light Stability Pack；若 Rank 97 直接 hard-fail / park，则按 7.10 再认领 1 条新的 5m / 15m paper-repo source intake`

## 验证 / 命令
- 已执行：`python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
- 已确认以下文件存在并可读：
  - `reports/artifacts/literature/scout_rank97_rsrs_right_skew_source_intake_card.csv`
  - `reports/site/reading/quant_digests/2026-03-19_1844_rsrs-right-skew-shared-gate.html`
  - `docs/TODO.md`

## 风险 / 边界
- 当前证据只够支持“`RSRS right-skew` 值得作为 overlay 做最小 clean replication”，不够直接证明它是三条主线共享的稳定 gate；
- 当前本地 proxy 已提示它更像 **risk overlay** 而非 **binary trigger**，下一轮必须优先看 `turnover / veto_hit_rate / drawdown` 是否一起更诚实；
- 默认不允许把它提前写成 live challenger，也不应在 clean replication 前继续补 intake 近义文案。

## git / 脏区说明
- 当前 git 工作区仍有大量与本轮无关的脏文件。
- 本轮只新增/改动与 `Rank 97 source intake` 直接相关的文件；
- 因脏区过大，本轮不提交，避免混提。

## 下一步建议
- 下一轮若 `EMA` 仍 `waiting_not_due`，默认只给 `Rank 97` 1 次最小 clean replication：
  - 固定 `BTC/ETH/SOL | 120d | 15m` 本地 cache；
  - 比较 `no_overlay / hard_veto(q30-q70) / half_size_overlay / tiered_sizing_overlay`；
  - 直接回答 `keep_P1 / promote_to_P2 / park`。
- 若这轮 clean replication 直接 hard-fail，则不要回头给旧 evidence pool 续命；先按 `7.10` 再认领 1 条新的 `5m / 15m` paper-repo source。

## Commit hash
- 未提交。
- 原因：当前 git 工作区存在大量与本轮无关的脏文件，混提不安全。
