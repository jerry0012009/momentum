# 2026-03-14 01:12 UTC — V3X-B1 support/resistance readout

## Picked task
- `V3X-B` 中最小未完成子项：在 v3 页面新增“support / resistance 到底有没有区别？”区块。

## What changed
- 更新 `scripts/build_pytrendline_event_validation_v3_report.py`
  - 新增 `build_side_comparison_h24()`，把 `support_*` / `resistance_*` 的 h24 excess 并排展示。
  - 在主报告里新增 dedicated section：
    - 说明 breakout 仍应先按 family-level 解读；
    - 说明 rebound 已出现 side sensitivity；
    - 明确写出 found / not found / reliability。
- 更新 `reports/site/factors/pytrendline_event_validation_v3/report.html`
  - 直接落地上述新 section。
  - 顺手修正 `support_rebound_confirm_1` 旧文案里“绝对收益转正”的错误表述。
- 更新 `docs/TODO.md`
  - 将 `V3X-B` 的第一个子项勾选完成，并补具体结果说明。

## Main readout now shown on page
- **Breakout:** 当前数字上 support / resistance 已经不再是“完全一样”，但由于 A 类几何 / 归属审计尚未 clean，**不能**先把它解释成两条独立 alpha。
- **Rebound:** 当前确实开始出现 side difference，但 `confirm_1` 偏 support，`confirm_2` 偏 resistance，所以只能说“有 side sensitivity”，还不能说哪一边已经稳定胜出。

## Reliability
- `breakout 先别做 side-level verdict` → high
- `rebound has side sensitivity` → medium
- `which side ultimately wins` → low_to_medium
