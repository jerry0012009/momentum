# Rank 139 P3 hosted narrow paper pilot 健康检查（2026-03-22 10:18 UTC）

## 本轮定位
- 按 `docs/TODO.md` 顶部 `TRADING DESK BOARD` 执行。
- `Run 1 = EMA due-check first` 当前未见可直接证明 `due-now / overdue` 的新触发证据；按板上规则，`waiting_not_due` 不空转，立即切到 `Run 2`。
- 本轮只做 **1 个主点**：`Rank 139 (P3)` hosted narrow paper pilot 低频健康检查；不并行打开新的 Scout 候选。

## 检查对象
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_refresh_clock.json`
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/meta.json`

## 新鲜度 / 连续性
- monitoring board 文件时间：`2026-03-22 09:02:25 UTC`
- refresh clock 文件时间：`2026-03-22 09:02:25 UTC`
- refresh clock 记录：`generated_at_utc = 2026-03-22 09:02 UTC`
- 当前策略口径仍为：`thr_mult=0.8`、`arm=confirm_same_dir_only`
- 结论：**ops page / CSV 仍在持续更新，未出现“卡死不刷新”的直接信号。**

## 当前监控快照（只读摘录）
### breakout_short
- BTC: retention `27.8%`，`mean_net_kept@6bps = -0.6127%`，`no_event_timeout = 22.2%`
- ETH: retention `33.3%`，`mean_net_kept@6bps = -0.3240%`，`no_event_timeout = 27.8%`
- SOL: retention `50.0%`，`mean_net_kept@6bps = +0.0822%`，`no_event_timeout = 16.7%`

### ema_psar_long
- BTC: retention `11.8%`，`mean_net_kept@6bps = +0.1490%`，`no_event_timeout = 11.8%`
- ETH: retention `44.4%`，`mean_net_kept@6bps = +0.9122%`，`no_event_timeout = 5.6%`
- SOL: retention `22.2%`，`mean_net_kept@6bps = +3.2965%`，`no_event_timeout = 16.7%`

### fib_retest_long
- BTC: retention `18.2%`，`mean_net_kept@6bps = +0.5910%`，`no_event_timeout = 27.3%`
- ETH: retention `16.7%`，`mean_net_kept@6bps = +1.0340%`，`no_event_timeout = 25.0%`
- SOL: retention `45.5%`，`mean_net_kept@6bps = +0.6592%`，`no_event_timeout = 27.3%`

## 健康判断
1. **连续性正常**：监控板与 refresh clock 同步更新到 `09:02 UTC`，说明 hosted narrow paper pilot 还在产生日常可见落点。
2. **未见新的爆雷信号**：
   - 没看到 `no_event_timeout` 突然飙到失控区间；当前区间约 `5.6% ~ 27.8%`。
   - 没看到 retention 全面塌穿；最低仍是 BTC `ema_psar_long` 的 `11.8%`，但这不是本轮新增异常，而是已知稀疏保留形态。
3. **结构上仍然是“混合但可监控”**：
   - short leg 里 BTC/ETH 仍偏弱，SOL breakout_short 勉强转正；
   - long legs 尤其 ETH/SOL `ema_psar_long` 仍提供更强的 kept-trade 净收益支撑。
4. **因此本轮 verdict**：继续维持 `P3 hosted narrow paper pilot / low-frequency monitoring only`，不回到近义研究打磨。

## 对 desk 的含义
- 这次检查支持 `TODO` 顶部当前口径：`Rank 139` 已经处于“跑起来并看健康度”的阶段，而不是继续争论 `thr_mult` 小数点。
- 只要后续 refresh 继续更新、`no_event_timeout` 不显著恶化、retention 不出现跨 setup 同步塌陷，就不应抢占更多 bot3 主资源。

## 下一步（不在本轮展开）
- 继续按低频节奏看 `status-changing event`：
  - refresh 停更；
  - `no_event_timeout` 明显抬升；
  - retention 或 `mean_net_kept@6bps` 出现跨 setup 共同恶化。
- 若无上述事件，bot3 下一允许动作应回到 `Run 3` 的单点小交付（`pbo-cscv honesty gate` 的 source intake 或 canonical 实现），而不是重复 Rank 139 近义健康检查。
