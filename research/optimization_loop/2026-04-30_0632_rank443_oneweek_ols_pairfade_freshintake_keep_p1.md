# Rank 443 / one-week OLS pair fade × copula gate — fresh intake keep P1

- Time: 2026-04-30 06:32 UTC
- Target: `research/quant_digests/2026-04-26_0028_oneweek-ols-pairfade-copulagate.md`
- Action type: fresh intake first verdict
- Verdict: `keep_P1`
- Rank assigned: `443`

## Why this changed system belief
这条 intake 已经能诚实收束成一句 queue-facing raw alpha：**短窗高相关 crypto pair 的 `1-week rolling OLS residual z-score` 极端偏离后，存在少数 pair pocket 可做的 spread fade；copula 更适合作为二层 admission gate，而不是主 alpha 本体。**

这使它不同于泛泛的“pairs + copula 很高级”包装，也不同于不可执行的 README 胜率故事。当前 digest 已提供一个最小可迁移主语：先测 residual-fade 本体，再问 pair admission / copula gate 是否改善 left tail 与成本后收益。

## Minimal decisive blocker checked
本轮只检查 first verdict 所需的唯一 decisive blocker：**对象能否从统计包装收束成一句后续值得保留 1 次 cheap follow-up 的具体主语。**

结论：可以。
- repo 主体真正可复用的是 `1-week rolling OLS residual fade`，不是 copula headline；
- digest 自带 portability probe，已证明它不是“任何 pair 都有效”的通用模板，而是需要 pair admission 的 pocket alpha；
- 这反而让后续 survivor follow-up 更明确：应围绕 target pair stability / fee-after viability / child execution honesty，而不是继续讨论 README 叙事。

## Why not promote P2 now
还不够直接升 `P2`，因为当前证据仍停留在少量 pair 快检与概念级策略骨架，尚未完成 admission 所需的更明确范围收束（哪些 pair/主题值得保留、成本后是否稳定、child execution 是否真实）。

## Next honest follow-up shape
若后续进入 survivor 唯一 follow-up，最便宜且最能改变结论的方向应是：
1. 只选当前 digest 已显出 pocket 的 pair family；
2. 直接检查成本后稳定性与 max-hold / z-threshold 是否只靠单一 pair 偶然驱动；
3. 明确它是否值得进 `P2` 作为窄范围 pairs admission，而不是“全宇宙 pairs 模板”。

## Result sentence for runtime
`Rank 443 / one-week OLS pair fade × copula gate` 已完成 first verdict 并保留为 `P1`：对象可诚实收束成“少数高相关 crypto pair 的 1-week rolling OLS residual z-score 极端 -> spread fade pocket，copula 仅作二层 admission gate”这条可迁移 raw alpha，因此值得保留 1 次 survivor follow-up，而不是把 README 的高胜率或 copula 包装误当主系统。
