# 2026-03-23 11:22 UTC — Rank 149 / spot-perp spread mean reversion honesty cut

## 本轮路径
- Path: `Scout`
- 来源：`docs/TODO.md` 顶板 `Run 1 = Rank 149 honesty cut`
- 任务口径：只做 `1 主点 + 1 紧邻子点`，直接回答这条 fresh intake 应不应该继续保留 `P2/P3` 动能。

## 本轮只做的一小步
把 `Rank 149 / spot-perp spread mean reversion raw alpha` 从 gross toy evidence 往前推进一刀：

> 用最小、可复核的 paired-execution 成本模型，补 `post-cost + funding + spot-leg friction + capital tie-up` 诚实检查。

不做新的 literature，不做新的模型，不做新故事。

## 主点（本轮唯一主判断）
### `Rank 149` 不应继续保留 `P2`，应直接回落到 `P0 / park`
原因很简单：
- **gross alpha 形态存在**，但
- **双腿执行摩擦远大于 toy gross edge**。

### 最小诚实模型（故意偏宽松）
数据来源：
- `reports/artifacts/quant_digests/spot_perp_spread_mr_20260323/*_spread_signal_5m.csv`
- 标的：`BTCUSDT / ETHUSDT / SOLUSDT`
- 频率：`5m`
- 既有 toy rule：`|z| > 1.5` 开仓，`|z| < 0.25` 平仓，允许同向/反向切换

成本模型：
1. **execution cost**：按 **`2bps / leg`** 估算（已经算很乐观）
   - paired spread 一次完整 round-trip = `spot entry + perp entry + spot exit + perp exit`
   - 约等于 **`8bps / completed round-trip`**，还没额外加真实冲击/借币/现货滑点扩张
2. **funding drag proxy**：按 perp 腿持仓 **每 8 小时 `1bp`** 粗估
3. **capital tie-up drag**：按占资 **年化 `6%`** 粗估（偏保守宽松）

## 可验证结果
产物：
- `reports/artifacts/quant_digests/spot_perp_spread_mr_20260323/honesty_cut_scorecard.csv`

### 合计（3 个币）
- gross：`+398.81 bps`
- execution cost（`2bps / leg`）：`-3560.00 bps`
- funding drag：`-23.83 bps`
- capital drag：`-13.06 bps`
- **net：`-3198.08 bps`**

### 分币（`2bps / leg`）
- `BTCUSDT`
  - gross：`+125.06 bps`
  - exec：`-1308.00 bps`
  - funding：`-7.94 bps`
  - capital：`-4.35 bps`
  - **net：`-1195.23 bps`**
- `ETHUSDT`
  - gross：`+125.75 bps`
  - exec：`-1300.00 bps`
  - funding：`-7.89 bps`
  - capital：`-4.32 bps`
  - **net：`-1186.45 bps`**
- `SOLUSDT`
  - gross：`+148.01 bps`
  - exec：`-952.00 bps`
  - funding：`-8.01 bps`
  - capital：`-4.39 bps`
  - **net：`-816.39 bps`**

## 紧邻子点（只推进 1 个）
### 为什么这一刀已经足够 decisive
这轮不是在争论“spread 有没有回归倾向”；那点已经有了。
真正决定 desk 资源分配的是：

> **这条线在最宽松的 paired friction 下都已经大幅转负。**

这意味着下一步不该继续给它 `P2 -> P3` 预算，而应把结论写回顶板：
- 可以保留为 **gross-only / market microstructure evidence**；
- 但 **不是当前 desk 的 Paper 候选**；
- 除非未来换成更低摩擦的真实执行环境（例如内部撮合、maker rebate、超低 spot fee、稳定借币/库存条件），否则不应继续推进。

## 简短 scorecard
- `gross signal existence`: `B+`
- `cost honesty`: `A`
- `paper readiness`: `F`
- `capital efficiency`: `D`
- `desk leverage now`: `A-`（因为快速把错误方向踢出主槽位）
- **authoritative verdict**: `P0 / park / gross-only evidence / not a Paper candidate`

## 已写回 authoritative 顶板
已更新 `docs/TODO.md`：
1. `Active Scout` 中将 `Rank 149` 改为 `P0 / park`
2. `Next 3 bot3 runs` 去掉对 `Rank 149` 的继续预算
3. `Recent evidence` 写回本轮净成本结论

## 下轮 handoff
下一轮 bot3 不要继续补 `Rank 149` 的近义成本细化。
按顶板新顺序：
1. 先看新的 `fresh intake / raw-alpha reserve`
2. 若没有，则从 `Rank 140 / 145 / 147 / 146 / 14b` 里挑最短 decisive fallback
