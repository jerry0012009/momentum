# 2026-04-09 01:21 UTC · Rank 25c fresh intake first verdict

## 本轮执行对象
- target: `research/park_reframe/2026-03-23_0256_rank25-park-reframe.md`
- action: 作为当前首条 pending fresh intake，判断 `Donchian breakout 主触发 + EMA 仅作 HTF context gate` 是否足够形成新的窄单轴 raw alpha，而不是旧 breakout / trend-shell family 的职责重命名。

## 读取到的最小关键信息
1. 这个 reframe 的来源很明确：原 `Rank 25` 被 park 的主因不是成本或跨资产一起爆雷，而是 **time red-watch** —— 参数邻域反复出现“中段 bucket 亮、两侧 bucket 塌”，说明原写法的时间结构不稳。
2. reframe 想改的唯一一刀也很明确：把 `EMA + Donchian` 从同层 co-trigger，改成 `Donchian breakout` 负责真正入场，`EMA` 仅做更高层 HTF context gate。
3. 但当前证据仍停在“这个岗位改写可能更诚实、值得 clean replication next”，还没有给出任何新的 desk-facing 最小事实，例如：
   - baseline breakout vs breakout+EMA-context gate 的 after-cost 增量；
   - 该 gate 是否只是靠砍掉坏单美化样本，而不是留下更稳定 pocket；
   - 相对既有 breakout / HTF trend gate family，它是否真的形成了一个不被吸收的独立主语。

## first verdict
结论：**本轮不保留前排，直接收口为 `background / P0`。**

换成人话：
- 这条 reframe 现在更像对旧 `Rank 25` 的**职责拆分修正提案**，不是一条已经站住的全新 raw alpha；
- 它解决的是“EMA 不该和 breakout 平级触发”的建模表达问题，但还没有证明“把 EMA 降为 HTF context-only gate”之后，会留下一个足以单独排队的、可诚实执行的交易 pocket；
- 因此它暂时只能留在 `park residual / evidence pool`，不能作为新的前排 fresh intake 占位。

## 为什么不是 keep_P1
因为当前缺的不是一般性补证据，而是**独立性本身**：
- 这条线尚未脱离既有 breakout / trend-shell family；
- 也还没有一个最小、已坐实的 queue-facing 增量事实来支撑它升级到 survivor。

若未来要 reopen，唯一合理入口仍是：
- 用单轴 clean replication 直接做 `baseline Donchian breakout` vs `Donchian breakout + EMA HTF context-only gate` 的诚实 A/B；
- 只要这一步不能留下稳定且成本后为正的新增 pocket，就不应再把它抬回前排。

## 本轮写回 runtime 的一句话
`Rank 25c` 目前仍只是旧 breakout family 的岗位重写提案；在 baseline-vs-context-gate 的独立 after-cost 增量尚未被坐实前，它不足以作为新的 fresh intake 留在前排，因此本轮首判直接收口为 `background / P0`。
