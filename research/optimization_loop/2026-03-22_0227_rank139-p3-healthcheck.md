# bot3 auto (13m) — Run2 / Rank139(P3) hosted narrow paper pilot 低频健康检查

时间：2026-03-22 02:27 UTC（周日）

## Run 1（EMA due-check first）
- 执行：`python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
- 结果：`waiting_not_due`（脚本退出码=2，属于“仍需等待下一根 completed bar”，不做伪 refresh）
- 最近到点：
  - Crypto 1d+1wk（BTC/ETH/SOL）：约 21.5h 后
  - 创业板ETF 1d：约 28.5h 后
  - 贵州茅台 1d+1wk：约 28.5h 后

=> 按顶板规则：Run1 真实 waiting_not_due，不空转，立刻切 Scout Seat。

## Run 2（本轮主点）：Rank139(P3) pilot 健康检查（只做 1 件事）
目标：确认 ops/CSV 仍可用、字段口径没崩、是否出现“爆雷”信号（`no_event_timeout` 异常偏高、retention 崩塌等）。

### 1) 可见性/刷新
- 已重新生成（refresh clock + monitoring board）：
  - `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
  - `reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_monitoring_board.html`

### 2) 关键字段快扫（confirm_same_dir_only @ thr_mult=0.8，6bps/side）
（来自 monitoring board 的 asset×setup 汇总；仅做“是否爆雷”的快速扫描）
- `no_event_timeout_rate`：约 **5.6% ~ 27.8%**（目前未见异常飙升）
- `retention`：约 **11.8% ~ 50.0%**（有分化，但未见“全盘极端坍塌”）
- `mean_net_kept@6bps`：
  - ETH `ema_psar_long`、SOL `ema_psar_long` 等臂为正；
  - BTC/ETH `breakout_short` kept 仍偏负（需要后续在“运行监控”里继续观察，但本轮不做近义研究对比）。

### 3) 本轮结论（健康状态）
- Pilot 面板/CSV：**可用、可刷新**。
- 风险提示：当前 `breakout_short` kept 在 BTC/ETH 仍偏弱，建议后续用“低频监控阈值”盯住（例如 no_event_timeout、retention、mean_net@6bps 的突变），但本轮不扩展研究。

## 下一轮（按权威 Next 3）
- Run 3：`pbo-cscv / deflated sharpe honesty gate` 只做一个小交付（source intake 或 minimal implementation 二选一）。
