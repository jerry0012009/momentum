# Rank 237 / simple-feature XS combo × top-quintile-long / beta-light — first verdict keep_P1

- 时间：2026-03-29 13:24 UTC
- 执行轮次：bot3 13 分钟自动执行
- 对应 cycle_plan 小点：`simple-feature XS long-leg crypto ML alpha`
- 来源：`research/quant_digests/2026-03-29_1122_simple-feature-xs-longleg-crypto-ml-alpha.md`

## 本轮要回答的问题
这条 2024 IRFA digest 是否能被收窄成一个**边界清楚、可单轮证伪**的新对象，还是仍只是“模型更复杂也许更好”的泛 ML 叙事。

## 读取后的关键信息
1. digest 明确把论文真正可 desk 化的主语收敛为：
   - **simple-feature XS combo**，而不是更复杂模型；
   - **long-leg-first attribution**，而不是默认对称 long-short；
   - 更合理的组合骨架是 `top bucket long` / `top bucket long + beta hedge` / `top-bottom long-short` 并排比较。
2. 同一 digest 已经给出一个诚实的负面 transfer check：
   - 在 `2026Q1` 的 `15m` liquid-major Binance perp pocket 里，`simple-feature XS combo` 的 direct sign transfer 不活；
   - `top bucket long`、`top-bucket vs market`、`full long-short` 都是负的。
3. 这意味着当前可保留的不是“liquid perp 每小时重排可直接赚钱”的对象，而是一个**更慢 cadence / 分层 universe / long-leg-first** 的窄版 raw-alpha intake。

## 本轮判断
### 为什么不是 background-only
因为它已经具备独立主语，且 falsifiable spec 很清楚：
- 主体不是“ML 有效”；
- 而是 **simple-feature 横截面组合分数，在更慢 cadence 下是否主要通过 top-quintile long / beta-light 结构保留可交易漂移**。

### 为什么暂不升 P2
因为当前仅有 digest 与一个短周期 liquid-major negative transfer check：
- 它已经足够说明“不能按 `15m/1h` fast transfer 直接写成成立”；
- 但还没完成最小 clean replication 去回答：
  - 更慢 `rebalance/hold` 是否恢复边；
  - `majors / mid-cap liquid alts / wider tradable tail` 的分层里哪一层还活；
  - 收益是否真的主要来自 `top-quintile long / beta-light` 而非 symmetric long-short。

## 结论
`simple-feature XS long-leg crypto ML alpha` 可以被诚实收窄成一个新的 fresh intake，并分配正式编号：

> **Rank 237 / simple-feature XS combo × top-quintile-long / beta-light**

本轮 first verdict：

> **keep_P1**。

它值得保留的原因，不是“复杂 ML 更强”，而是：
- `simple-feature XS combo` 本身是明确的 raw alpha family；
- 当前证据同时说明它**不能**被误写成已完成短周期 transfer；
- 因此最诚实的位置是 `P1`，等待唯一一次 survivor follow-up 去做更慢 cadence × universe segmentation × leg attribution 的最小 decisive 检查。

## 会改变系统认知的一句话
`simple-feature XS long-leg crypto ML alpha` 不应作为宽泛“ML 更复杂更好”叙事留在背景；它应以 **Rank 237 / simple-feature XS combo × top-quintile-long / beta-light** 的窄版对象进入 `keep_P1`，但当前仅限更慢 cadence / 分层 universe / long-leg-first 的后续证伪路径，不能宣称已完成短周期 liquid-perp transfer。
