# Rank 382 P2 admission 出口决策（delay/friction honesty 最小检查）→ promote_P3

- 时间：2026-04-11 17:18 UTC
- 对象：`Rank 382 / liquidity-volatility × illiquidity-level XS alpha`
- 轮次动作：cycle_plan #1（P2 admission 出口决策轮）
- 输入证据：
  - `reports/artifacts/literature/liquidity_volatility_illiqlevel_probe_summary_2026-04-11.csv`
  - `reports/artifacts/optimization_loop/rank382_filladjusted_capacity_check_2026-04-11.csv`
- 新增 artifact：
  - `reports/artifacts/optimization_loop/rank382_p2_exit_honesty_delay_friction_consistency_2026-04-11.csv`

## 最小 honesty 子检查（本轮只做 1 个）
按 cycle_plan 要求，仅补 `delayed confirmation × friction consistency`：

1. **delayed confirmation**：沿用 digest 中已声明的 `t 收盘打分，t+1 执行，持有 1h`（即 lag1 执行口径），未改成同 bar 执行；
2. **friction consistency**：在同一 lag1 口径下，用统一 `10 bps roundtrip` 重算 top25_30d_quotevol 的 24h/3d/7d 窗口净边际，并与上一轮容量约束联立。

## 结果
- `top25_30d_quotevol` 在 `10 bps` 下净边际分别约：
  - 24h: `+2.44 bps/1h`
  - 3d: `+18.88 bps/1h`
  - 7d: `+24.76 bps/1h`
- 容量 realism（上一轮）保持硬约束：
  - `<=0.25% bar ADV` 仍为正（`+2.44 bps/1h`）
  - `0.50% bar ADV` 转负（`-1.56 bps/1h`）

## 单一出口结论（改变系统认知）
`Rank 382` 在 **lag1 执行 + 10bps 摩擦 + <=0.25% bar ADV 容量上限** 的诚实口径下仍保持正净边际，且未出现新的 decisive honesty/execution blocker，因此本轮 `P2 exit decision = promote_P3`（进入 Paper launch queue，待执行接线）。

## 尾部执行
- homepage publish：已按要求单独执行 `bash /root/clawd/jerry/momentum/scripts/publish_homepage_index.sh`，本轮未拿到成功回执（进程无输出后终止），按 policy 记为非阻断尾部失败，不回滚 verdict/state/log。
- 邮件通知：已单独执行发送脚本并成功投递。
