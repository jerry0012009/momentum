# bot3 momentum auto-opt (13m)

- time: 2026-03-22 01:05 UTC
- focus: TRADING DESK BOARD → Next 3 bot3 runs (Run1→Run2)

## Run 1 — EMA due-check first
执行：
```bash
python3 jerry/momentum/scripts/run_ema_paper_trading_guarded_refresh.py --skip-build --require-due
```
结果（结论 = `waiting_not_due`，因此不得空转，立刻切到 Scout Seat / Run2）：
- 当前没有 `due-now / overdue` lane（脚本 exit code=2 属正常路径）。
- 最靠前 lane：
  - Crypto 1d+1wk（BTC/ETH/SOL）：约 **23.6 小时**后到点
  - 创业板ETF 1d：约 **1.3 天**后到点
  - 贵州茅台 1d+1wk：约 **1.3 天**后到点

## Run 2 — Rank139(P3) 接入 hosted narrow paper pilot（最小闭环）
目标：补齐 **paper pilot ledger + monitoring board + refresh clock** 的最小可运行口径，并产出网页落点；显式纳管 `no_event_timeout`。

执行：
```bash
python3 jerry/momentum/scripts/build_rank139_narrow_paper_pilot_minimal.py
```
产出：
- monitoring board（CSV）：
  - `jerry/momentum/reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
- ops landing page（HTML）：
  - `jerry/momentum/reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_monitoring_board.html`

说明：
- 脚本默认 gate 取 desk 设定：`confirm_same_dir_only @ thr_mult=0.8`。
- 页面会展示该 gate 的关键字段（含 `no_event_timeout`），并给出最近 trade-level 的监控表。

## Run 3
本轮未执行（留到下一次 13m run）：
- 若 Rank139 lane 已能稳定跑：做 1 个 fresh intake；
- 否则只补 1 个阻塞点（不扩候选）。
