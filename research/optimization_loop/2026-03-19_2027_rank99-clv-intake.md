# 2026-03-19 20:27 UTC — Rank 99 CLV asymmetric admission layer source intake

## 本轮先核对的 desk 状态
- repo 工作区存在大量与本轮无关的脏文件；本轮未做 commit，也未混提无关改动。
- 先实际执行了：`python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
  - 结果：当前**没有**新的 `due-now / overdue lane`
  - 最近守门顺序：`Crypto 1d+1wk（BTC/ETH/SOL）约 3.5h 后到点 > 创业板ETF / 贵州茅台 / 沪深300ETF 约 10.7h 后到点 > 美股约 23.7h 后到点`
  - 结论：`Paper Seat = EMA / running paper / waiting_not_due`
- `manual_narrow_paper_last_run_summary.json @ 2026-03-19T20:19:15Z`
  - `new_closed_trades_appended=0`
  - 结论：当前没有需要 bot3 抢主资源处理的 `P3` 异常

## 本轮只认领的主点
- **主点：`Scout Seat / Rank 99 / CLV asymmetric admission layer` 的 source intake + 两条轻量诚实守门**
- 紧邻子点：同步把 `Rank 93 / Rank 90 / Rank 91 / Rank 82 / Rank 80 / Rank 81` 继续明确记成 `P1 evidence_pool / budget used`，而不是本轮并开

## 为什么本轮是 Rank 99
这轮重新比较当前 active Scout 的边际价值：
1. **Rank 99 / CLV asymmetric admission layer reserve**
   - `Run 1 / EMA due-check` 已被实际脚本证明仍是 `waiting_not_due`
   - 顶板已把当前默认 Scout 主资源位收口成 `Rank 99 > Rank 93 / Rank 90 / Rank 91 / Rank 82 / Rank 80 / Rank 81 evidence_pool > Rank 98 / Rank 97 / Rank 96 / Rank 95 / Rank 92 / Rank 94 park`
   - 它直接服务三条主线共用的 bar-quality 语义问题：decision bar 到底要不要 `close near the edge`，而且多空是否应不对称处理
2. **Rank 93 / Rank 90 / Rank 91 / Rank 82 / Rank 80 / Rank 81**
   - 当前都属于 `P1 evidence_pool / budget used`
   - 本轮不应回头给旧 `P1` 续命
3. **Rank 98 / Rank 97 / Rank 96 / Rank 95 / Rank 92 / Rank 94**
   - 当前都属于 `P0 park / evidence pool`
   - 只有 fresh source 真实 exhausted 后才允许退回这一层之后的动作

## 本轮冻结的 source-intake 口径
- 候选：`Rank 99 / CLV asymmetric admission layer`
- 来源：`zenoaiclawbot/zenoclaw`
- 核心迁移：
  - 不再把 `strong candle close` 当口号
  - 而是把 repo 里的 `CLV = (close - low) / (high - low)` 冻结成三条主线可复用的 `decision-bar close-location` admission 语义
  - 当前 desk 迁移优先偷的是**方向不对称**读法：short follow-up 更适合 strict CLV；long continuation 不能把 `CLV-only` 误读成充分条件

### 两条轻量诚实守门
1. **trade on / trade off 已可清楚写成规则**
   - `trade on`：
     - short / breakout-short follow-up 侧只在 `aligned_clv_short >= 0.70 / 0.80` 时放行或更偏 full-size
     - long / Fib retest_hold 与 EMA-PSAR continuation 侧默认不允许把 `CLV-only` 当 continuation 充分条件，只允许和 `volume / acceptance / reclaim context` 组成组合臂
   - 首轮 desk 迁移只把它当 shared `admission / veto` 语义：base setup 继续负责方向与价位，CLV 只回答“这根 decision bar 收盘够不够诚实”
2. **无明显 lookahead / repaint / data leakage**
   - `CLV=(close-low)/(high-low)` 本身是因果式、当根即可计算
   - 首轮只允许使用 signal 当根及之前可得的 `high / low / close` 与当下可得 `volume / acceptance / reclaim` 上下文
   - desk 统一执行口径仍是：`signal 当根及之前数据 + next-bar open + no-overlap`
   - 不允许把后续 `4~8` bar continuation、未来波动或事后挑出的漂亮 candle 倒灌回当前 CLV 状态

## 本轮 hard verdict
- **`Rank 99 / CLV asymmetric admission layer = guard-passed / admit_to_clean_replication_queue`**

### 为什么不是继续空转或回头做 P3 continuity
- `Run 1` 已被实际脚本证明仍是 `waiting_not_due`
- 顶板明确要求：若 `Run 1` 只是 waiting，不得空转，必须切到 `Run 2`
- 当前 `P3` 托管位没有暴露需要 bot3 主资源补救的异常

### 为什么也不是先回头做旧 P1 evidence_pool
- 当前板上明确的 active Scout 顺序已经收口为 `Rank 99 > Rank 93 / Rank 90 / Rank 91 / Rank 82 / Rank 80 / Rank 81 evidence_pool`
- `Rank 99` 比旧 `P1` 更直接减少当前主线的一块真实模糊位：什么才算“强收盘”，而且 short / long 是否该区别对待

## 产物
- artifact:
  - `reports/artifacts/literature/scout_rank99_clv_asymmetric_admission_source_intake_card.csv`
- reader-facing:
  - `reports/site/reading/repo_scout/rank99_clv_asymmetric_admission_source_intake.html`
- 顶板已更新：
  - `docs/TODO.md` 中 Scout 分级与 `Next 3 bot3 runs`

## 对顶板的更新结论
- `Run 1 = EMA due-check only（优先盯 Crypto 1d+1wk due_soon）`
- `Run 2 = 若 EMA 仍 waiting_not_due，则只给 Rank 99 / CLV asymmetric admission layer 1 次最小 clean replication`
- `Run 3 = 若 Rank 99 clean replication 仍存活，则只给 1 个 truly verdict-changing 的最小 Light Stability Pack；若 Rank 99 在 clean replication 直接 hard-fail / exhausted，则按 7.10 再认领 1 条新的 5m / 15m paper-repo source intake`
- `P3 continuity` 继续只算低频 sidecar，不得默认抢占 Scout 主资源

## 最小验证
- `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due` 已实际运行，确认当前仍无 due-now / overdue lane
- 读回 `docs/TODO.md`，确认最新 seat 分级与 `Next 3` 已写回
- 读回 intake artifact 与 reader-facing HTML，确认路径已落盘

## 备注
- 本轮没有追新 bar、没有伪造 refresh。
- 本轮没有重跑 clean replication，只做 source intake + honesty gate 冻结。
- 工作区存在大量历史脏文件与未跟踪产物；本轮未尝试整理、提交或覆盖这些无关改动。
