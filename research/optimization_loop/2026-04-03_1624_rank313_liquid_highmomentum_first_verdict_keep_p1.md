# Rank 313 — liquid-major distance-to-high cross-sectional continuation — first verdict keep_P1

- 时间：2026-04-03 16:24 UTC
- 对象：`research/quant_digests/2026-04-03_1510_liquid-highmomentum-rolling-high-crosssectional-alpha.md`
- 执行动作：fresh intake first verdict
- 结论：分配正式 `Rank 313`，first verdict = `keep_P1`，进入 `Surviving candidate slot`

## 这轮实际确认了什么
这条对象的主语已经足够独立，不只是“crypto 里 momentum / reversal 再讲一遍”：

1. **明确宇宙约束**：只在 `large/liquid` 子宇宙成立，而不是全市场混做。
2. **明确特征主语**：`distance-to-high / high-momentum`，不是泛泛的 lag-return reversal。
3. **明确组合形态**：`market-neutral` 横截面多空，而不是模糊的 long-only 追强叙事。
4. **明确最小实验壳**：digest 已写出统一 `5m/15m` 映射、`ret_n` 对照、liquidity split、`4/6/8bps` 成本、`LS / LO+hedge / SO+hedge` 的最小验证框架。
5. **与既有 generic XS reversal / momentum 家族有最小可检验区分**：它的关键系统认知是“**先按 liquidity split 分宇宙，再在 liquid-major 里测 distance-to-high continuation；尾部混入会把符号翻成 reversal**”。

因此，这不是一篇只有文献叙事、没有可交易主语的 digest；它已经形成一条可独立 desk 化的 `liquid-major distance-to-high cross-sectional continuation` raw alpha 候选。

## 为什么这轮不直升 P2
当前公开证据的硬上限也很清楚：

- 核心实证仍是**周频论文**；
- digest 虽然把 `1m/3m/5m/15m` 映射、成本、对照和风险框架写清楚了，但**项目内还没有统一 `BTC/ETH/SOL + liquid majors`、统一 `5m/15m`、统一 `4/6/8bps` 下的 clean first replication artifact**；
- 也还没有直接证明 `hmom` 在 desk 口径下**净后优于普通 `ret_n` 排名**，以及“只在 top liquidity bucket 为正、tail 为负”的 split 是否在当前可交易 universe 上仍成立。

所以这轮最诚实的位置是：**给正式 Rank，进入 `P1`，保留唯一一次 survivor follow-up 去回答“desk 版 first replication 是否真的成立”**，而不是直接写成 `P2 admission`。

## 下一次 survivor follow-up 应该回答的唯一 decisive 问题
在统一 `liquid majors` universe、统一 `5m/15m` 与统一 `4/6/8bps` 成本口径下，`distance-to-high / hmom` 是否同时满足：

1. **top liquidity bucket 仍为 continuation，而 tail bucket 翻面或显著更差**；
2. **`hmom` 净后不弱于、最好优于 `ret_n` baseline**；
3. **优势不是单一标的 / 单一短腿 / 单一时间窗偶然支撑**。

若这一步成立，才更有资格升 `P2`；若 desk 版 clean replication 做不出来，就应直接收口回 `background/P0`。

## Runtime 写回
- Fresh intake slot：切换为 `Rank 313 / liquid-major distance-to-high cross-sectional continuation`
- Verdict：`keep_P1`
- Surviving candidate slot：切换为 `Rank 313`，follow-up budget = `1`

## 一句话结果
`Rank 313` 不是 generic XS momentum/reversal 的换皮摘要，而是一条主语清楚、可直接映射到 liquid-major market-neutral 研究框架的 `distance-to-high` raw alpha；但当前证据仍停在周频论文与 digest 级实验壳，因此本轮先定为 `keep_P1`，不直升 `P2`。
