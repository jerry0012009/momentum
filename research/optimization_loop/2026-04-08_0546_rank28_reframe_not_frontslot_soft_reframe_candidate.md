# 2026-04-08 05:46 UTC · Rank 28 conditional fresh intake closes as not front-slot

## 本轮对象
- target: `research/park_reframe/2026-04-08_0019_rank28-park-reframe.md`
- policy role: `conditional fresh intake`
- question: 4 月初新增的几条 cross-market / lead-lag 证据，是否已经足够把旧 `Rank 28` 从 park residual 收敛成新的正式 fresh intake。

## 读取到的当前 runtime 前提
- 当前 `Fresh intake` 已经被 `Rank 364` 占用，`Surviving candidate` 也仍在前排链条内。
- 本小点只能回答：`Rank 28` 这条 park residual 是否值得形成新的 queue-facing derived hypothesis / fresh intake。
- 按 policy，若新证据仍只是把主题推向别的 raw-alpha family，而不是旧 Rank 28 的单轴窄派生，就不能把它重新拉回前排。

## 本轮证据
核心依据来自：`research/park_reframe/2026-04-08_0019_rank28-park-reframe.md`

该文档已把结论压得很清楚：
1. 原 `Rank 28` 被 park 的根因没有变化：`15m direct cross-market leader-laggard follow` 的 clean replication 在统一成本后没有站住，不只是参数问题。
2. 4 月初新增的三条证据——
   - `BTC lead × low-liquidity alt lag`
   - `major-lead first-slot return × follower close-slot continuation`
   - `Binance spot impulse × OKX delayed catch-up`
   共同指向的是**更快、更窄、更事件化**的宿主。
3. 这些新证据没有形成一个比既有 `Rank 28b` 更诚实的新单轴修改；它们更像外流到新的 lower-TF / event-driven / same-underlier cross-venue raw-alpha family。
4. 因而旧 `Rank 28` 当前唯一仍成立的 queue-facing residual 仍是既有 `Rank 28b`，不应再派生一个 `Rank 28c`。

## 收口判断
本轮不形成新的 fresh intake。

原因不是“完全没信息”，而是：
- 有信息存活，但存活位置已经偏离旧 `Rank 28` 的 residual 边界；
- 新证据没有给出一个新的、唯一的、bot2/bot3 可直接前排执行的单轴改写；
- 因此它仍应留在 `soft_reframe_candidate / not front-slot`，而不是重新抢占前排槽位。

## 对 runtime 的影响
- 不改 `Fresh intake slot`
- 不改 `Surviving candidate slot`
- 不改 `Active P2 slot`
- 只把当前 cycle_plan 小点收口为：`soft_reframe_candidate / not front-slot`

## One-line result
`Rank 28` 的 4 月初新增 cross-market / lead-lag 证据仍只支持主题外流到更快、更窄的 raw-alpha family，而没有形成区别于既有 `Rank 28b` 的新单轴 queue-facing 派生，因此本轮维持 `soft_reframe_candidate / not front-slot`，不作为 fresh intake 重回前排。
