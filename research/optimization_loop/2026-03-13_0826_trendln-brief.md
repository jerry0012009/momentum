# 补齐 trendln brief，完成 shortlist v1 的 brief 全覆盖

## 为什么这次选这个

这轮继续完全沿最近几轮的 E 线程往前推进，没有新开题，也没有去扩新的候选池，而是直接补上一个最靠近、最明确的收口缺口：

- `replication shortlist v1` 里共有 4 个正式对象；
- 前几轮已经先后补了：
  - `Svogun 2022`
  - `pytrendline`
  - `Optimal Stopping`
- 当前唯一还缺的就是：`trendln`

所以这轮最合理的推进，不是再找新材料，而是把 `trendln` 正式补进 `Trendline Replication Briefs`，这样这一版 shortlist 才真正完整。

这轮最值得复用/借鉴的点是：**当 shortlist 已经定稿后，优先把它补齐到“每个对象都有落地 brief”，往往比继续扩新候选更有价值，因为这会直接提高后续研究队列的可执行性和可审计性。**

## 核心结论（中文摘要）

核心结论：**`trendln` 现在已经正式拥有一张 `geometry baseline brief`；至此，`replication shortlist v1` 的 4 个正式对象都已经完成 brief 覆盖。**

证据如何支持这个结论：**本轮已更新 `scripts/build_trendline_replication_briefs_report.py`，重建后的 `reports/site/reading/trendline_replication_briefs/report.html` 中已经出现 `Brief D · trendln (Gregory Morse)`，并明确写出它的 geometry / channel baseline 定位、最小 clean-room 定义、成功标准与风险；同时 `docs/TODO.md` 已把“为每个 replication candidate 产出一张 replication brief”标记为完成，并写明 `shortlist v1 当前已实现 brief 全覆盖`。**

## 本轮做了什么改动

本轮只做一个主点：**给 `trendln` 正式补一张 brief，并完成 shortlist v1 的 brief 全覆盖。**

具体改动：

1. 更新 `scripts/build_trendline_replication_briefs_report.py`
   - 在 `Trendline Replication Briefs` 页面中新增：
     - `Brief D · trendln (Gregory Morse)`
   - 明确它的角色不是 paper replication，也不是 active alpha engine，而是：
     - `geometry baseline`
     - `channel candidate`

2. 在新 brief 中明确：
   - 我们准备复现什么：
     - `extrema -> line search -> line quality` 的 geometry layer
     - 以及 `channel candidate` 这一层
   - 我们用什么数据：
     - 优先复用项目内已标准化的 OHLCV window
     - 不做全市场/全参数暴力扫描
   - 最小 clean-room 定义：
     - `support_slope`
     - `resistance_slope`
     - `support_touch_count`
     - `resistance_touch_count`
     - `channel_width_mean`
     - `channel_width_std`
     - `is_parallel_channel`
   - 成功标准：
     - 不是要求 `trendln` 自己给出收益率
     - 而是证明它能成为 `support-resistance / channel` 的几何 baseline，并可与 `pytrendline / mainline protocol` 做有效对照

3. 同页修正：
   - 清理上一轮遗留的一处重复建议（brief 页面里原本重复出现了两条 `pytrendline` 优先动作）
   - 更新“当前阶段性结论”“我建议的落地顺序”“下一步交付”，使其反映：
     - `trendln` 已经不再是“待补 brief”，而是正式的 `geometry baseline brief`

4. 更新 `docs/TODO.md`
   - 将：
     - `为每个 replication candidate 产出一张 replication brief`
     - 标记为 `[x]`
   - 并补一句结果说明：
     - `shortlist v1 当前已实现 brief 全覆盖`
   - 同时把角色说明中的 `trendln` 更新为：
     - `pure explainability reference / geometry baseline`

5. 重建页面
   - `reports/site/reading/trendline_replication_briefs/report.html`
   - `reports/site/plans/momentum_todo.html`

## 验证 / 证据

本轮是文档 / brief / 页面细化，因此采用最小必要验证：

1. 语法检查
   - `./.venv/bin/python -m py_compile scripts/build_trendline_replication_briefs_report.py scripts/build_plans_site.py`

2. 页面重建
   - `./.venv/bin/python scripts/build_trendline_replication_briefs_report.py`
   - `./.venv/bin/python scripts/build_plans_site.py`

3. 本地 grep 验证
   - `reports/site/reading/trendline_replication_briefs/report.html`
     - 已出现：`Brief D · trendln (Gregory Morse)`
     - 已出现：`geometry baseline`
     - 已出现：`channel candidate`
     - 已出现：`repo deep dive`
   - `reports/site/plans/momentum_todo.html`
     - 已出现：`[x] 为每个 replication candidate 产出一张 replication brief`
     - 已出现：`shortlist v1 当前已实现 brief 全覆盖`

## 风险 / 边界

- 这轮没有新增新的实证实验；
- 它解决的是 **shortlist v1 的 brief completeness**，不是直接新增 alpha 证据；
- `trendln` 这张 brief 的定位仍然是 `geometry baseline`，不应被误读成“我们认为它比 pytrendline 更适合作主线事件源”。

## 下一步建议

1. 既然 shortlist v1 已经 brief 全覆盖，下一步不要再在这一版名单里反复修文案；
2. 更值得做的是：
   - 把 `Optimal Stopping` 的 `touch / provisional_break / confirmed_switch / retest_hold` 真正回挂到 `Trendline Confirmation Protocol`
   - 或者开始找 shortlist v2 候选，但应优先面向：
     - confirmation / retest
     - failed breakout / rebound
     - 有全文 + 有代码 / 伪代码的近 5 年材料

## Commit hash

- 已提交：`docs(momentum): add trendln geometry brief`
- 具体 hash 以本轮提交后的当前 `HEAD` 为准。

## 如果未提交，说明原因

本轮已做安全 selective commit。

我只提交了本轮涉及的 brief 页面 / TODO / 运行记录文件，没有混入当前 repo 里与本轮无关的其它脏文件。