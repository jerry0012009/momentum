# HedgeVision half-life / Hurst pairs shell — fresh intake first verdict: background/P0

- 时间：2026-04-03 19:13 UTC
- 对象：`research/quant_digests/2026-04-03_1827_hedgevision-half-life-pairs-shell.md`
- 执行动作：fresh intake first verdict
- 结论：`background/P0`

## 这一步回答的问题
这条 `half-life / Hurst admission × z-score / time-stop pairs shell`，是否足够独立于当前 `Active P2` 的 `Rank 314 / ORCA tradability-aware cluster pairs`，值得占用前排继续推进？

本轮结论：**不值得。直接收口到 `background/P0`。**

## 为什么不进前排
### 1) 它的主语和当前 Rank 314 高度同轴，不是新的 raw alpha
这份 HedgeVision digest 的核心命题是：
- `spread mean reversion` 仍是 base alpha；
- short-cycle 成败主要取决于 `half-life / Hurst admission + time-stop discipline`；
- `15m` 更像主信号，`5m` 更像执行/管理层。

但这与 `Rank 314` 当前已经在跑的命题高度重合：
- 都是在做 **pairs / relative-value mean reversion**；
- 都把 **admission layer** 放在 `entry band` 之前；
- 都在问 **更快回归 / 更高 tradability / 更诚实的 time-stop 与 turnover 成本**；
- 都落在统一 `5m/15m` shell 下评估 short-cycle 可交易性。

换句话说，这不是一个和 Rank 314 平行的新 front-slot 对象，而更像是 **对 Rank 314 所在线路的另一份 repo 化表达**。

### 2) 它提供的是“该怎么继续测 pairs”的方法论增量，不是足以单独抢前排的对象增量
HedgeVision 这篇最有价值的部分，是把 `half-life/Hurst`、`time stop`、`candidate scoring` 这些 admission 元件组织成了一个可移植壳。

但当前 runtime 已经有一个更前、更具体的对象在做同类问题：
- `Rank 314` 已经从 `P1` 升到 `P2`；
- 当前唯一需要收口的 blocker，是 `refresh cadence × replacement friction × maker/taker realism`；
- 继续把另一条 pairs admission 壳拉进前排，只会在同一证据轴上重复占资源。

按 policy，这类与当前 `Active P2` 高度重合、没有新增 decisive 增量的 intake，不应挤占前排。

### 3) 这篇没有给出足以改写当前系统认知的新 decisive blocker 或新 survivor pocket
digest 里的 public proxy 结论大意是：
- `15m` 上 pairs MR 仍像能继续测；
- 压到 `5m` 后大量 trade 变成 `time exit` / stop 主导；
- 所以下一步重点应该是更严格 admission，而不是继续调 z-score band。

这跟当前系统对 `Rank 314` 的认知基本一致，而不是新增了一个“只有它能回答”的对象级问题。

因此更诚实的处理方式不是给它 `keep_P1`，而是把它记为：
> **pairs admission / half-life-Hurst / time-stop shell 的参考素材，已被当前 Rank 314 主线吸收；对象本身不作为独立前排候选继续推进。**

## 对 runtime 的影响
- 本对象完成 fresh intake first verdict，直接记入 `background/P0`
- 不分配正式 `Rank`，因为本轮 verdict 不是 `keep_P1` 或更高
- `Active P2` 仍保持 `Rank 314` 为当前主线，不引入新的 survivor/front-slot 冲突

## 一句话 result
`research/quant_digests/2026-04-03_1827_hedgevision-half-life-pairs-shell.md` 已完成 fresh intake first verdict：其价值主要是把 `pairs mean reversion × half-life/Hurst admission × time-stop` 壳整理得更清楚，但与当前 `Rank 314` 的 `pairs admission-layer` 证据轴高度重合、未提供足以单独抢占前排的新 raw alpha 增量，因此本轮直接收口到 `background/P0`。