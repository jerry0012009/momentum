# 2026-03-23 03:29 UTC · Rank 14b / EMA-PSAR long family decisive cut

## 本轮按顶板顺序执行

### Run 1 · TRADING DESK BOARD / interrupt check
- 已先读 `docs/TODO.md` 顶部 `TRADING DESK BOARD`
- `Paper / 待开启自动运行 = empty`
- 未见顶板定义的真实 interrupt：
  - 未看到 `running autonomous paper` runner 的 `stale / error / refresh 失步 / ledger 爆雷 / open-position 异常 / red-watch`
  - 未看到 tiny-live / live-shadow plumbing 的 blocking anomaly
- 因此本轮按默认 `Next 3`，只认领 **Run 1 / Rank 14b cheapest decisive cut**

### Run 2 · 主点
只推进 **`Rank 14b / directional-breadth-coherence long-side continuation veto`**。

### Run 3 · 紧邻子点
只做 1 个紧邻子点：
- 不新开 `Rank 125 / 112 / 111`
- 不回头做 autonomous paper routine health-check
- 不扩成多阈值或多 setup 并行
- 只回答：`Rank 14b` 到底只是 `raw_trigger` 上的偶然小正 pocket，还是在相邻 `close_confirmed` 变体里也能保持同一 veto 读法

---

## 本轮实际动作
复用既有数据，不新拉外部网页：
- 交易日志：`reports/artifacts/scout_rank73_psar_close_confirmed_followup_15m/trade_log.csv`
- 现货 5m cache：`reports/artifacts/scout_rank66_exec_tf_switch_alignment_15m/spot_cache/{BTC,ETH,SOL}USDT_120d_5m.csv`
- 规则保持不变：`dir_breadth_1h <= 0.45` 时 veto long entry
- 新比较对象：`ema_psar_long` 下的 `raw_trigger / close_confirmed_n1 / close_confirmed_n2 / close_confirmed_n3`

新增 artifact：
- `reports/artifacts/scout_rank14b_close_confirmed_breadth_cut/summary.csv`
- `reports/artifacts/scout_rank14b_close_confirmed_breadth_cut/asset_summary_6bps.csv`
- `reports/artifacts/scout_rank14b_close_confirmed_breadth_cut/meta.json`

---

## 核心结果

### 1) 不是只在 `raw_trigger` 上成立的偶然 pocket
在同一条 `EMA/PSAR long` family 里，`low breadth veto` 在相邻 `close_confirmed` 变体也保持了同方向改善：

#### 6 bps / side
- `raw_trigger`：`-16.36 -> +3.80 bps`，`retention = 59.62%`
- `close_confirmed_n1`：`-22.70 -> +4.44 bps`，`retention = 60.42%`
- `close_confirmed_n2`：`-30.62 -> +7.31 bps`，`retention = 58.06%`
- `close_confirmed_n3`：`-30.56 -> +8.51 bps`，`retention = 57.38%`

这说明它已经不只是某一个入口定义下的偶然小正 pocket，而是 **在相邻 family 变体里都重复出现的 long-side veto 改善**。

### 2) 但 deployable shared gate 仍然不成立
即便 family-level 读法更干净了，核心硬伤没有消失：
- `trade_retention` 仍只有约 `57%~60%`
- `ETH` 在所有相邻变体里依旧是明显拖累，不像共享跨资产可部署 gate
- `15 bps / side` 全部仍为负
- `10 bps / side` 只有 `close_confirmed_n3` 勉强 `+0.50 bps`，还远不够稳定

### 3) 最诚实 desk 口径
因此本轮要回答的不是“它能不能 promote”，而是：
- 它是否已经摆脱“只是 raw pocket”的怀疑？**是**
- 它是否已经足够干净到能变成 shared deployable long-veto？**否**

最小稳定 verdict：
- **`Rank 14b = keep_P1 / family-level evidence strengthened / no promote`**
- 同时它本轮已经用完顶板允许的这 1 次 cheapest decisive cut，**下一轮默认应退出 primary**，把 Scout 主资源让给 `Rank 125 / 112 / 111` 中更有杠杆的一条

---

## 轻量 scorecard
- `usefulness = medium_to_high`
- `time_stability = weak`
- `cross_asset_stability = weak`
- `cost_trade_stability = weak_to_medium`
- `deployability = low`

### hard-fail flags
- `eth_still_structural_drag`
- `retention_still_only_57_60pct`
- `15bps_all_variants_negative`
- `10bps_only_borderline_for_n3`
- `shared_cross_asset_gate_not_clean`

### recommended_action
- **`keep_P1`**

### why_now
顶板明确要求对 `Rank 14b` 再做且只做 1 次 cheapest decisive cut，回答它到底只是“砍单后少亏/小正 pocket”，还是已经形成更干净的 shared long-veto 读法。本轮用相邻 `close_confirmed` family 变体完成了这个最小回答：**family-level 读法成立，但仍不到 promote/P2 的程度。**

### main_weakness
它现在的问题不再是“有没有信息”，而是**信息不够稳也不够共享**：主要改善仍靠砍掉低质量单和 `SOL/BTC` 侧的提升，`ETH` 没被真正修复，成本再抬高后也缺乏稳定余量。

---

## 对 TODO 顶板的最小 writeback 建议
- `Rank 14b` 从 `current Scout primary` 改成：`keep_P1 / family-level evidence strengthened / budget used / not default primary`
- `Run 1` 默认切到 `Rank 125 / 112 / 111` 中最有杠杆的一条

## 本轮交付
- 日志：`research/optimization_loop/2026-03-23_0329_rank14b-family-cut.md`
- artifact：`reports/artifacts/scout_rank14b_close_confirmed_breadth_cut/*`
