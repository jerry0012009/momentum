# 2026-03-22 09:02 UTC · bot3 auto · Rank139 P3 hosted narrow paper pilot 健康检查

## 本轮按板执行
- `Run 1`：已实际执行 `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`。
  - 结果：`EMA / Paper Seat` 仍是 **`waiting_not_due`**，没有 `due-now / overdue` lane。
  - 最近 due：`Crypto 1d+1wk（BTC/ETH/SOL）`，约 **15.0 小时**后到点。
  - 因此本轮不得空转，已按顶板立刻切到 `Run 2`。
- `Run 2`：只做 **`Rank 139 / hosted narrow paper pilot` 低频健康检查**，不再开新研究对比。

## 本轮动作（唯一主点）
重建并核对 Rank139 的最小监控面板：
- 执行：`python3 scripts/build_rank139_narrow_paper_pilot_minimal.py`
- 更新产物：
  - `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
  - `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_refresh_clock.json`
  - `reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_monitoring_board.html`

## 健康检查结论
### 1) ops page / CSV 仍可正常刷新
- 本轮 09:02 UTC 已成功重建 monitoring CSV 与 ops HTML，说明 **页面链路仍活着**，不是死链接或陈旧不可重算状态。
- 原始 clean-replication 主报告 `report.html` 仍在，ops 面板与主报告的分工清晰：前者做低频健康可见性，后者保留研究结论。

### 2) 当前没有看到“爆雷级”异常，但要盯住几处 timeout 偏高 pocket
按 desk 默认 gate：`confirm_same_dir_only @ thr_mult=0.8`。

**较健康 / 可接受的口径：**
- `ETH-USD / ema_psar_long`：`retention=44.4%`，`mean_net_kept@6bps=+0.91%`，`no_event_timeout=5.6%`。
- `SOL-USD / ema_psar_long`：`retention=22.2%`，`mean_net_kept@6bps=+3.30%`，`no_event_timeout=16.7%`。
- `SOL-USD / breakout_short`：`retention=50.0%`，`mean_net_kept@6bps=+0.08%`，`no_event_timeout=16.7%`。

**需要继续盯的 pocket（但还不到直接判失败）：**
- `BTC-USD / fib_retest_long`：`no_event_timeout=27.3%`。
- `ETH-USD / breakout_short`：`no_event_timeout=27.8%`。
- `SOL-USD / fib_retest_long`：`no_event_timeout=27.3%`。
- `BTC-USD / ema_psar_long`：保留率只有 `11.8%`，虽然 kept 后转正，但样本偏薄。

### 3) 当前更诚实的 desk 读法
- Rank139 这条线 **仍值得留在 P3 hosted narrow paper pilot**，因为：
  - 面板可正常刷新；
  - 默认 gate 不是全线塌掉，而是呈现“部分 asset×setup pocket 有效、部分 pocket timeout 偏高”的可监控状态；
  - 当前最该做的是继续把它当作 **低频托管监控对象**，而不是回头扩写近义研究。
- 但后续若连续几轮都看到 `no_event_timeout` 在多个 pocket 进一步上升，或 `retention` 与 `mean_net_kept@6bps` 同时崩塌，再考虑降级/改监控口径。

## 对下一轮的含义
- 若 `EMA` 仍 `waiting_not_due`，则本轮 `Run 2` 已如实消费完毕。
- 下一轮应按顶板进入 `Run 3`：只认领 **`pbo-cscv / deflated sharpe honesty gate`** 的一个小交付（优先 `source intake + 人话摘要` 或最小实现之一），不要同时打开多个 Scout 候选。

## 可见落点
- Rank139 监控页：`reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_monitoring_board.html`
- 主报告：`reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/report.html`
