# 2026-03-20 20:28 UTC — Rank 128 / MAX(5m) impulse confirmation tier / source intake + 两条轻量诚实守门

## 本轮先核对的东西
- repo：`master`；`git status --short` 仍显示大量与本轮无关的脏文件，**不混提**。
- 最近 optimization loop：最新已留痕是 `2026-03-20 20:16 UTC / EMA 美股 due window 真实续写`。
- `Paper Seat`：这轮再次实际执行 `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`，结果仍是 **`EMA = waiting_not_due`**；当前无 `due-now / overdue` lane，最近 due 已切到 `Crypto 1d+1wk -> 约 3.5 小时后到点`。
- hosted paper lanes：`manual_narrow_paper_last_run_summary.json @ 2026-03-20T20:19:56Z` 为 `new_closed_trades_appended=0`；当前没有新的 `P3 status-changing event` 能插队抢主资源位。

## 为什么本轮合法主动作是 Rank 128
按 `docs/TODO.md` 顶板 `2026-03-20 20:16 UTC` 最新排班：
1. `Run 1 = EMA due-check first`
2. 若 EMA 继续 `waiting_not_due`，转去 `Rank 128 / MAX(5m) impulse confirmation tier` 的 source intake + 两条轻量诚实守门
3. 若 `Rank 128 guard-pass` 且 EMA 仍 `waiting_not_due`，才给它 1 次最小 clean replication

本轮满足第 2 条，因此只认领 **`Rank 128`** 这 1 个主点，不并开其他候选。

## 本轮实际执行
补齐了 `Rank 128` 的 queue-facing intake 产物：
- artifact：`reports/artifacts/literature/scout_rank128_max5m_impulse_confirmation_tier_source_intake_card.csv`
- reader-facing：`reports/site/reading/repo_scout/rank128_max5m_impulse_confirmation_tier_source_intake.html`
- 顶板同步：在 `docs/TODO.md` 追加 `2026-03-20 20:28 UTC` 最新执行补充，把 `Rank 128` 状态推进到 `guard-passed / admit_to_clean_replication_queue`，并把下一轮顺序收紧到 `Rank 128 minimal clean replication -> fresh intake reserve`。

## 两条轻量诚实守门的硬结论
### 1. trade on / trade off
当前最诚实的写法必须是 **long-side continuation-confirmation tier**，而不是 shared 单阈值：
- 首轮只允许挂在 **`ema_reclaim_long`** 与 **`fib_retest_long`** 之后；
- 只在既有 base setup 已成立时，再按 `signal` 前 1 小时的 `MAX(5m return)` 做分层；
- 默认只比较 `baseline / max_high_only / exclude_max_high`，不是单独开仓。

换句话说：
- base setup 继续负责方向与价位；
- `MAX(5m)` 只负责 continuation-confirmation 分层；
- 它不是 breakout-short 的 shared overlay，也不是新的独立 alpha trigger。

### 2. no lookahead / repaint / leakage
当前可通过，但边界要写死：
- `MAX` 只能来自 **`signal 当根及之前、已完成 5m bar`** 的 trailing 1h return 窗口；
- desk clean replication 必须统一冻结到 **`signal 当根及之前数据 + next-bar open + no-overlap`**；
- 分层阈值只能先在训练段冻结（如 tercile / top30），再去测试段验证，禁止全样本事后最优；
- 原论文的 cross-sectional 叙事不能直接搬成单笔交易 desk 收益承诺；当前只能保留“极端短时上冲可能带有 continuation quality 信息”这条可解释旁支。

## 硬结论
**`Rank 128 / MAX(5m) impulse confirmation tier = guard-passed / admit_to_clean_replication_queue`**。

翻成人话：
- 小时内极端上冲在 `EMA reclaim / Fib retest` 语境里，当前更像 **continuation-confirmation 分层**，不是默认 no-chase 否决键；
- 但它现在还只是 paper-based 候选，真正该验证的是 **成本后还能不能留下分层价值**；
- 因此这条线值得拿那 1 次最小 clean replication 预算，但前提是直接比较 `baseline / max_high_only / exclude_max_high`，而不是继续写概念页。

## 本轮产物
### artifacts
- `reports/artifacts/literature/scout_rank128_max5m_impulse_confirmation_tier_source_intake_card.csv`

### reader-facing
- `reports/site/reading/repo_scout/rank128_max5m_impulse_confirmation_tier_source_intake.html`

### desk write-back
- `docs/TODO.md`（新增 `2026-03-20 20:28 UTC` 顶板执行补充）

## 对 desk 的含义
- `Paper Seat`：不变，仍是 `EMA / running paper / waiting_not_due`
- `Live Seat`：继续暂空
- `Scout Seat`：当前 active 顺序应收紧为
  - `Rank 128 = P1 weak candidate（guard-passed / minimal clean replication next）`
  - `Rank 127 = P1 weak candidate（budget used / evidence_pool）`
  - `Rank 125 / 112 / 111 = P1 evidence_pool or budget_used`
  - `Rank 126 / 124 / 123 / 121 / 120 / 119 / 118 / 117 / 115 / 114 / 113 = P0 park / evidence pool`
  - `Rank 122 / Rank 2 / Rank 17 / Rank 29 / Rank 32b = P3 hosted continuity / sidecar only`

## 下一手建议
若下一轮 `EMA` 仍 `waiting_not_due`，默认只允许给 `Rank 128` **1 次最小 clean replication**：
- 三臂：`baseline / max_high_only / exclude_max_high`
- 口径：`BTC/ETH/SOL 120d 5m+15m`、`signal 当根及之前数据 + next-bar open + no-overlap`
- base archetype：先只限 `ema_reclaim_long + fib_retest_long`
- 直接判：`keep_P1 / park / promote_to_P2`

若 `Rank 128` 当场 hard-fail / exhausted，则立刻回 fresh intake reserve，而不是回头继续磨 `Rank 127 / 125 / 112 / 111`。

## Commit hash
未提交。

原因：工作区存在大量与本轮无关的脏文件，当前不适合做安全 selective commit。
