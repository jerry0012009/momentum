# 2026-03-19 15:43 UTC — Rank 92 opening-drive adaptive offset intake

## 为什么这次选这个
- 先实际执行了 `python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`。
- 结果仍是 **`waiting_not_due`**：当前没有 `due-now / overdue` 的 EMA lane；最近 due 约为 `美股 4.3h`、`Crypto 8.3h`、`A股 15.3h`。
- `manual_narrow_paper_last_run_summary.json` 也没有新的 `P3 status-changing event`，所以这轮不能回头抢 `P3 continuity`。
- 按 `docs/TODO.md` 顶部 `TRADING DESK BOARD` 最新 `Next 3 bot3 runs`，`Rank 94` 已在上一轮 clean replication 后压回 `park / evidence_pool`，因此当前合法主动作应切到 **`Rank 92 / opening-drive adaptive offset continuation gate`** 的 `source intake + 两条轻量诚实守门`。

## 本轮主点 + 紧邻子点
- **主点**：完成 `Rank 92` 的 `source intake + 两条轻量诚实守门`
- **紧邻子点**：把当前 verdict、active Scout 顺序、`Next 3 bot3 runs` 最小写回到 `TRADING DESK BOARD`

## 做了什么改动
### 1) 冻结 Rank 92 的 queue-facing 读法
把这条线明确收口成：
- **不是**新的 opening-drive 主策略；
- **而是**三条主线可共用的 `shared continuation-confirmation gate`。

首轮冻结为：
- 最小 desk 代理字段：`drive_high/drive_low`、`drive_mid`、`sessionVWAP`、`drive_range`
- 自适应偏移：`adaptive_offset = max(|drive_mid - sessionVWAP|, 0.15 * drive_range)`
- long 侧只有原始 setup 已给出方向，且价格真实穿过 `drive_high + adaptive_offset` 才放行 continuation
- short 侧镜像为跌破 `drive_low - adaptive_offset`
- 当前只允许它作为 `admission / veto / half-size` 层，不得单独开仓

### 2) 两条轻量诚实守门
- **trade on / trade off** 已冻结：
  - 若只是碰到 `drive edge`、却没有穿过 `edge + adaptive_offset`，就不得硬说它完成 continuation confirm；
  - 它不能把 opening-drive 追价事后美化成趋势成立，也不能替代原始 trigger。
- **honesty gate** 已冻结：
  - `drive_high/low`、`drive_mid`、`sessionVWAP`、`drive_range` 与 `adaptive_offset` 只能使用 `signal` 当根及之前可得数据；
  - desk 迁移统一冻结到 `signal 当根及之前数据 + next-bar open + no-overlap`；
  - 不允许把 breakout 后 `4~8` bar 路径、future session path、或事后更漂亮的 session 切法倒灌回 gate。
- 额外保留：由于 `24/7 crypto` 的 `opening-drive / sessionVWAP` 边界仍可能有代理成分，这轮只如实写成 `guard-passed / admit_to_clean_replication_queue`，不提前吹成可部署策略。

### 3) 新增 reader-facing 落点
- artifact：`reports/artifacts/literature/scout_rank92_opening_drive_adaptive_offset_source_intake_card.csv`
- 网页：`reports/site/reading/repo_scout/rank92_opening_drive_adaptive_offset_source_intake.html`

### 4) 最小更新 desk board
已在 `docs/TODO.md` 顶部 `TRADING DESK BOARD` 追加本轮最小写回：
- `Rank 92 = P1 weak candidate（guard-passed / minimal clean replication next）`
- 当前 active Scout 顺序改成：`Rank 92 > Rank 93 / Rank 94 / Rank 90 / Rank 91 / Rank 82 / Rank 80 / Rank 81 evidence_pool > P3 continuity > tiny-live plumbing`
- `Next 3 bot3 runs` 改成：
  - `Run 1 = EMA due-check only`
  - `Run 2 = Rank 92 已完成 source intake + 两条轻量诚实守门`
  - `Run 3 = 若 EMA 仍 waiting_not_due，则只给 Rank 92 1 次最小 clean replication`

## 验证 / 证据
- 已实际执行：`python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due`
- 结果：`waiting_not_due`（按预期不做 full rebuild）
- 已核对并落地的本轮文件：
  - `docs/TODO.md`
  - `reports/artifacts/literature/scout_rank92_opening_drive_adaptive_offset_source_intake_card.csv`
  - `reports/site/reading/repo_scout/rank92_opening_drive_adaptive_offset_source_intake.html`
- `git status --short` 显示 repo 里仍有大量与本轮无关的脏文件，因此本轮不做提交。

## 风险 / 边界
- 这条线目前只是 `guard-passed`，还没有过最小 clean replication。
- 最大诚实性风险仍是：`opening-drive / sessionVWAP` 的 crypto 24/7 session 边界如果定义偷懒，很容易把这条线做成事后裁切。
- 因此下一轮即使继续认领，也只应该做 **1 次最小 clean replication**，而不是扩写成更大研究框架。

## 下一步建议
- 若下一轮 `EMA` 仍是 `waiting_not_due`，只给 `Rank 92` 1 次最小 clean replication：
  - 固定 `BTC/ETH/SOL 120d 15m` 本地 cache
  - 只比较 `baseline / adaptive_offset_gate / adaptive_offset_halfsize`
  - 统一冻结到 `signal 当根及之前数据 + next-bar open + no-overlap + hold 8 bars`
  - 直接回答 `keep_P1 / promote_to_P2 / park`

## Commit hash
- 未提交。
- 原因：当前 git 工作区存在大量与本轮无关的脏文件，混提不安全。