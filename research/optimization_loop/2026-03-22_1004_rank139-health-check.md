# Rank 139 hosted narrow paper pilot 健康检查

- 时间：2026-03-22 10:04 UTC
- bot：bot3 auto loop（13m）
- 本轮遵循：`TODO.md` 顶部 `Next 3 bot3 runs`
- 本轮主点：`Run 2 = Rank 139(P3) hosted narrow paper pilot 低频健康检查`
- 紧邻子点：`Run 1 / EMA due-check` 仅用于判定是否允许切换主动作；未触发 due-now refresh

## 1) Run 1 先验判定：EMA 仍 waiting_not_due
实际执行：

```bash
python3 scripts/run_ema_paper_trading_guarded_refresh.py --require-due
```

结果：脚本返回 `waiting_not_due`（exit code 2，但属预期守门语义，不是故障）。

最靠前 due lane：
- `Crypto 1d+1wk（BTC/ETH/SOL）`：约 `13.9h` 后到点
- `创业板ETF 1d`：约 `20.9h` 后到点
- `贵州茅台 1d+1wk`：约 `20.9h` 后到点

结论：当前没有真实 `due-now / overdue` lane，因此本轮不得伪造 paper refresh，应立即切到 `Scout Seat`。

## 2) Run 2：Rank 139 hosted narrow paper pilot 低频健康检查
检查对象：
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_refresh_clock.json`
- `reports/artifacts/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_pilot_monitoring_board.csv`
- `reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_monitoring_board.html`

### 2.1 更新连续性
- monitoring CSV 最新生成时间：`2026-03-22 09:02 UTC`
- ops 页面存在：`reports/site/factors/scout_rank139_cusum_event_bar_confirm_veto_15m/narrow_paper_monitoring_board.html`
- 结论：**页面与 CSV 都在持续存在且口径一致，当前 hosted narrow paper pilot 仍有可见性。**

### 2.2 关键监控字段速读
当前固定 gate：`confirm_same_dir_only @ thr_mult=0.8`

按资产×setup 读数：
- `BTC breakout_short`：retention `27.8%`，mean_net_kept@6bps `-0.61%`，no_event_timeout `22.2%`
- `BTC ema_psar_long`：retention `11.8%`，mean_net_kept@6bps `+0.15%`，no_event_timeout `11.8%`
- `BTC fib_retest_long`：retention `18.2%`，mean_net_kept@6bps `+0.59%`，no_event_timeout `27.3%`
- `ETH breakout_short`：retention `33.3%`，mean_net_kept@6bps `-0.32%`，no_event_timeout `27.8%`
- `ETH ema_psar_long`：retention `44.4%`，mean_net_kept@6bps `+0.91%`，no_event_timeout `5.6%`
- `ETH fib_retest_long`：retention `16.7%`，mean_net_kept@6bps `+1.03%`，no_event_timeout `25.0%`
- `SOL breakout_short`：retention `50.0%`，mean_net_kept@6bps `+0.08%`，no_event_timeout `16.7%`
- `SOL ema_psar_long`：retention `22.2%`，mean_net_kept@6bps `+3.30%`，no_event_timeout `16.7%`
- `SOL fib_retest_long`：retention `45.5%`，mean_net_kept@6bps `+0.66%`，no_event_timeout `27.3%`

### 2.3 风险扫描（只回答会不会“爆雷”）
本轮只看 `no_event_timeout` 与 `retention` 是否出现明显失控：

- **没有看到全板级爆雷。**
- `no_event_timeout` 当前大多落在 `5.6% ~ 27.8%`，属于“需要盯但还没坏到失真”的区间；暂未看到单格接近 40%~50% 的异常尖峰。
- `retention` 分化明显，但仍符合保守 confirm gate 的预期：
  - 偏低的是 `BTC ema_psar_long = 11.8%`、`ETH/SOL fib_retest_long` 也不算高；
  - 但不是“全 setup 一起塌”，说明更像 gate 本身偏严，而不是整条 pilot 失去可用性。
- 盈亏层面仍有分化：
  - `BTC breakout_short`、`ETH breakout_short` 继续偏弱；
  - `ETH ema_psar_long`、`SOL ema_psar_long`、`SOL fib_retest_long` 仍提供正向 kept mean_net。

## 3) 本轮 hard verdict
**Rank 139 hosted narrow paper pilot 当前状态 = `继续托管监控 / 无爆雷 / 不加新研究比较`。**

更具体地说：
- `ops page/CSV` 持续可见，说明 pilot 没掉线；
- `no_event_timeout` 没有异常飙升到需要紧急回滚；
- `retention` 虽偏保守，但还没塌到“监控口径失真”；
- 因此本轮最诚实动作是：**维持当前 hosted narrow paper pilot，继续低频健康检查，不回头做近义研究对比。**

## 4) 对 Next 3 的影响
- `Run 1`：仍只在真实 due-now 时才允许 refresh。
- `Run 2`：本轮已完成，结论为 `stable enough to keep hosted monitoring`。
- `Run 3 / pbo-cscv honesty gate`：**留给下一轮**；本轮不并开，避免同时打开多个 Scout 候选。
