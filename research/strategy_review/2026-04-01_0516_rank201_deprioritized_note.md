# 2026-04-01 05:16 UTC — Rank 201 deprioritized note

## 对象
- `Rank 201 / UTC clock seasonality low-switch schedule`

## 当前决策口径
- **不再作为高优先级 deploy / paper 扩投入候选继续前推。**
- 更合适的定位：**保留为 `clock-family research lead`，不保留为当前 desk 的优先 P3 交易开发对象。**

## 原因（只保留最关键三条）
1. **冻结版 5 年验证不成立。**
   - 用当前 paper runner 的固定 schedule（`20:00~22:00 UTC long` / `22:00~00:00 UTC short`）直接回推共同 5 年样本后，月度稳定性显著不足，整体为负，不支持“长期稳定时钟规律”的表述。
2. **当前 P3 证据主要来自近 3 个月 pocket。**
   - 它更像近期出现的可执行时段 pocket，而不是过去多年稳定可复用的 frozen schedule。
3. **跨市场验证只能支持“clock-family 现象存在”，不能支持 Rank201 的 exact schedule 具有普适性。**
   - 美股 / 港股 / 黄金也有各自的 time-of-day pockets，但这些 pockets 是各自市场的 session-relative 结构，不构成对 Rank201 固定 UTC schedule 的直接背书。

## 后续怎么处理最诚实
- 不建议继续把资源优先投入到 `Rank 201 fixed schedule` 的实盘/影子验证。
- 若未来重开，只应以以下身份重开：
  1. `rolling re-selection clock family`
  2. `session-relative cross-market clock study`
  3. `US/HK -> crypto lead-lag / spillover` 验证
- 不应再把当前 frozen schedule 直接叙述成“已被历史和跨市场共同验证”。

## 对当前 shortlist 的影响
- 当用户下一步要求“P3 级、可验证、尽量原始 alpha、优先非趋势/非突破/均值回归”时，**不应继续优先推荐 Rank 201**。
- 更合适的前排备选应转向：`Rank 183 / cbeth-eth-rolling-fair-basis-mr` 这类已完成 `promote_P3` 且属于窄版 relative-value / mean-reversion 的对象。
