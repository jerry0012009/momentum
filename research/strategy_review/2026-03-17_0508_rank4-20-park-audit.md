# Rank 4~20 park audit — 2026-03-17 05:08 UTC

## 任务
复核 `docs/TODO.md` 中 `Rank 4~20` 的当前 `park` / 非 `park` 判定，重点检查：
- 是否存在“证据并没有那么差，但被写成 `park`”的情况；
- 是否存在“描述口径比实际证据更严格”的情况；
- 若存在，则改成更贴近当前 scout 分级体系的 `P1 / P2 / P3 / park`。

## 复核结论

### 1. 确认放宽：Rank 7
- 原判定：`park / evidence pool`
- 复核后改为：`P1 weak candidate / evidence pool`
- 原因：
  - `fixed_priority` 在 `6~20bps` 下保持小幅正向；
  - 时间稳定性 / 跨标的稳定性 / 成本存活 3 项 dry-check 都过了最小门槛；
  - 真正的问题集中在：
    - `no_trade_ratio≈98.6%`，可部署性太弱；
    - 参数邻域稳定性硬 fail，说明不是稳平台。
- 因此它不够进 `paper candidate`，但也不应继续写成已经被彻底判死的 `hard park`。
- 后续动作约束：若重开，只允许 **1 次便宜诚实检查**；检查后必须 `升格 / park` 二选一。

### 2. 维持 park：Rank 4 / 4b / 5 / 8 / 9 / 10 / 11 / 12 / 13 / 14 / 15 / 16 / 18 / 19 / 20
这些条目本轮没有放宽，不是因为保守，而是因为证据和 `park` 基本一致：

- `Rank 4 / 4b`：虽然 `4b` 在窄 pair 上出现过小幅正 pocket，但 recency / time 检查后最近 tercile 与最新月份一起转负，不足以继续放宽。
- `Rank 5`：最佳变体仍为负，且 `funding_8h` 版本四项稳定性全部硬 fail。
- `Rank 8~16`：大都存在至少两到四项明确 blocker（常见是时间 bucket 不稳、参数邻域一碰就碎、跨标的只剩单腿、成本后继续恶化）。
- `Rank 18`：平台票数版虽然比单点 EMA 少亏，但仍然是全资产为负、成本越加越差。
- `Rank 19`：更窄的 `box_breakout_ready` 只是“轻微少亏”，但样本约 `9.3` 笔/资产，且主变体时间/参数仍显著为负，不足以放宽。
- `Rank 20`：相对 baseline 没有净改进，且高成本更差，维持 `park` 合理。

### 3. 不在本轮 park 审计范围：Rank 17
- `Rank 17` 已升级为 `narrow paper pilot approved（ETH+SOL only）`；
- 它不是“被误判为 park”，而是已经被后续证据推升出 park。

## 已落地改动
- 更新 `docs/TODO.md`：
  - `Rank 7` 改为 `P1 weak candidate / evidence pool`
  - 增加 `2026-03-17 Rank 4~20 park audit` 总结块
